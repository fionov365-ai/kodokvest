# РАЗВИТИЕ ПРОДУКТА — единый живой список

**Это главный файл проекта по задачам. Он один, он не датируется и не
заменяется новым.** Все прежние `zadachi-novyi-chat-ГГГГ-ММ-ДД.md` — история;
из них сюда перенесено всё незакрытое. Больше таких файлов не заводим.

## Как этим пользоваться

**Фаундер** в новом чате пишет: **«продолжаем развивать продукт»**. Сессия
обязана прочитать этот файл целиком и предложить следующий шаг из § 2, не
переоткрывая § 8.

**Сессия** в конце работы обязана:

1. отметить сделанное в § 2 галочкой ✅ и номером версии — строку **не удалять**,
   она объясняет, почему так сделано;
2. дописать новое в конец нужного раздела, не переставляя чужое;
3. если появилось решение — записать его в § 7 **вместе с причиной и датой**;
4. если что-то отменено — перенести в § 8 со словом «почему», а не стереть.

⚠️ **Правило, оплаченное ошибкой 08.09.2026:** в одной папке одновременно
работают два чата. **Перед `git add -A` смотреть `git status`** — чужое
подготовленное к коммиту легко утащить или переписать. Начиная крупный экран,
писать об этом здесь вслух.

---

# § 0а. Разделение чатов закончено (09.09.2026, вечер)

Днём в папке работали два чата: один вёл экзамен и приложение, другой —
витрину, переименование и документы. Разделение шло **по файлам**, и это
единственное, что держало.

**С вечера 09.09.2026 работа идёт в одном чате.** Чат витрины закрыт, всё
незакрытое из него перенесено сюда, в § 2 и § 3.

⚠️ Правило на будущее, если чатов снова станет два: делить по ФАЙЛАМ, а не по
темам, и коммитить свой кусок сразу, как он зелёный. Почему именно так —
§ 4, пункты 9 и 10: список исключений в `git add` не спасает, если оба чата
правят один и тот же файл.

# § 0. Где мы сейчас

- Версия **1.139.0**. Курс закончен: 100 уроков, 5 миров, 11 проектов.
- Разделы вне сотни: Сегодня, Игры, Разминка, Визуализатор, «Ты и ИИ»,
  Повторить, Шпаргалка, Портфолио, Своё задание, Мастерская, Витрина работ.
- Экзамен: карта **ЕГЭ 27 из 27**, ОГЭ 13 закрыто, **97 задач** в 15 темах,
  пробный вариант целиком, Робот с русскими словами (**11 задач**).
  ⚠️ Числа пересчитаны 10.09.2026 по самим данным (`js/algo*.js`,
  `js/robot-tasks.js`); в записи стояли прежние 95 и 9.
- Второй контур — взрослый: занятие 20/30/45, отчёт, карта активности,
  «спросите вслух», задание из кабинета, панель репетитора и кабинет школы.
- **Режим: НАПОЛНЕНИЕ без официального выпуска** (решение фаундера 06.09.2026).
  Письма репетиторам не шлём, лицензию и оплату не строим.
- ⚠️ **Оплаты нет, домена нет, трафика нет, ни один чужой ребёнок не дошёл до
  20-го урока.**

---

# § 1. Дороже всего и не сделано до сих пор

Ни одна строка не требует программирования. Каждая решает больше, чем любая
фича. Висит с 03.09.2026 и не сдвинулось.

1. ⬜ **Чужой ребёнок доходит до 20-го урока?** Главный незакрытый вопрос всего
   проекта. Пока ответа нет, любое вложение — ставка вслепую.
2. ⬜ **Прожить «родительские десять минут» на своей семье.** Раз в неделю
   открыть кабинет, выписать четыре строки отчёта, провести разговор.
3. ⬜ **Живое занятие на двух настоящих устройствах:** ребёнок включил —
   взрослый увидел.
4. ⬜ **Три родителя на семейном обучении:** кто у вас ведёт информатику и что
   вы делаете перед аттестацией по ней.
5. ⬜ **Три родителя, платящих репетитору:** знают ли, что было на прошлом
   занятии; чем подтверждается, что деньги не зря.
6. ⬜ **Три репетитора:** сколько часов в неделю уходит на проверку кода руками.

---

# § 2. Очередь работ

Порядок — по пользе, а не по трудоёмкости. Источник каждого пункта указан,
чтобы не пересказывать разбор заново.

## 2.1. Витрина и выход в интернет (разбор — `vitrina-litsenziya-napravleniya-2026-09-09.md`)

| | Что | Почему сейчас |
|---|---|---|
| ⬜ | **Домен.** `kodokvest.ru` занят до 28.04.2027; свободны `kodkvest.ru`, `kodoquest.ru` | всё остальное в этом разделе без него бессмысленно. ~1 000 ₽/год |
| ✅ | `robots.txt`, `sitemap.xml` | сделано 09.09.2026. ⚠️ `robots.txt` на github.io не читается (нужен корень домена) — заработает при переезде |
| ⬜ | Счётчик Метрики | мы не знаем о посетителях ни одного числа. ⚠️ тянет политику ПДн (§ 2.2) |
| ⬜ | Яндекс.Вебмастер: подтверждение, регион, sitemap | — |
| ✅ | Список всех мест с прошитым адресом — `docs/pereezd-na-domen.md` | сделано 09.09.2026: переезд стал заменой строк по списку. Дополнен 10.09.2026 — в нём не хватало трёх страниц справочника и новой `shkole/`; теперь список совпадает с `grep` из § 1 того файла ровно, и это стережёт тест `[страницы]` |
| ✅ | **Адрес следует за экраном** | сделано 10.09.2026, **1.136.0**. ⚠️ **Запись в этой строке была неверной, и это стоило бы сессии впустую:** `location.hash` в `js/app.js` был (23 места), `replaceState` тоже (12). Замер показал другое: читающая половина роутинга существовала — 24 адреса в `HASH_SCREENS`, `hashchange`, ссылки на задачу, работу и проект. Не было **пишущей**: `enterScreen` адрес не трогал, а одиннадцать экранов присваивали хэш руками, россыпью. Отсюда три беды: репетитор не мог продиктовать ссылку на то, что видит; «Назад» работал через раз (с 11 экранов — назад, с остальных 40 — вон из продукта, а на телефоне это системный жест); заголовок вкладки был один на 55 экранов. Теперь таблица одна (`ROUTES`), из неё и чтение, и запись, и заголовок. Свой адрес получили ещё Мастерская, Песочница и Карта пути, а **урок, мир и проект — адрес с параметром** (`#lesson=print-first`): это и есть та ссылка, которую диктуют ученику. Сторожит тест `[адрес]` «кругооборот»; проверен пятью мутациями. Осталось: `?kid=`-ссылка и экраны-оверлеи |
| ✅ | **Вход из поиска сразу в задачу**, имя спрашивать после первой победы | ⚠️ **Запись уточнена 11.09.2026 замером:** отдельная форма «Как тебя зовут?» (`screenRegister`) новичка больше не встречает — при загрузке сначала разбирается адрес (`#lesson=…` открывает урок сразу, без имени), а без адреса `bootWhere()` открывает вывеску. Но на вывеске главное действие по-прежнему «впиши имя» (`landAuthHTML`), а попробовать урок можно только мелкой ссылкой «👀 Посмотреть первый урок». То есть задача жива, только место другое: поменять главное и второстепенное на вывеске и спросить имя после первой победы. **Сделано 11.09.2026, 1.138.0:** на вывеске главная кнопка «Начать первый урок →» (наверху и внизу — одна и та же дверь), под ней «Без регистрации: имя спросим после первой задачи · Уже занимались? Войти по коду»; поле на вывеске осталось одно — для кода. Имя спрашивает карточка победы, и только у гостя (`guestKid`: ни кода, ни имени, не взрослый); «Дальше →» работает и без имени. ⚠️ **Главная находка замера:** `doRegister` первой строкой стирал весь прогресс — перенеси просьбу об имени как есть, и она стёрла бы ту самую победу. Теперь стирается только чужой прогресс (под другим кодом или именем), гостю сохраняется. Сторожит `[вход]`: пять шагов, четыре мутации пойманы. Экран `screenRegister` остался за дверью «Войти → Я ученик» |
| ✅ | Посадочная `/individualnyi-proekt/` | сделано 09.09.2026 |
| ✅ | Посадочная `/semeynoe-obuchenie/` | сделано 09.09.2026 |
| ✅ | Посадочная `/repetitoru/` | сделано 09.09.2026 |
| ✅ | Посадочная `/shkole/` | сделано 10.09.2026, **1.135.0**. Своя причина прийти, а не пересказ страницы репетитора: компьютерный класс без установки, один файл без интернета, вариант всему классу коротким кодом, ноль ПДн. ⚠️ На самой странице честно сказано, что **кабинет один и тот же, что у репетитора** — отдельного школьного нет. На витрине «Три двери» стали **четырьмя**: дверь «Репетитору и школе» вела только к репетитору, школа была названа в подписи и страницы не имела |
| ⬜ | Страницы по номерам заданий `/ege/zadanie-NN/` ×27, `/oge/…` ×15 | карта по номерам готова с 1.110.0 |
| ⬜ | `/ege-informatika/`, `/oge-informatika/` | **последними:** там бесплатный тренажёр Яндекс Учебника, Решу ЕГЭ и Поляков |
| ⬜ | **Видео на витрину:** 5–7 роликов по 1–2 мин («как выглядит занятие», «что видит родитель в отчёте», «как собирается вариант») | ⚠️ **гейт: только после трёх посадочных и после разговоров § 1.** Ролик ставится НА страницу, а страниц нет; и снимать надо обещание, проверенное на живых людях, иначе переснимать. 15–25 часов |
| ✅ | **Главная страница витрины** — `/vitrina/`: числа продукта, три двери, три отличия, границы. После переезда становится корнём домена | сделано 09.09.2026 |
| ✅ | **Ссылки из приложения на витрину** — сделано 09.09.2026, **1.132.0**. В подвале Главного строка «Взрослому: сайт Фионики · репетитору · семейное обучение · индивидуальный проект · контакты». Четвёртой вкладки нет — дорога для взрослого стоит там же, где пояснение про взрослого. ⚠️ Пути **относительные, без ведущей косой черты**: на адресе вида `…/kodokvest/` ссылка `/repetitoru/` ведёт в корень домена, то есть в никуда, и заработает только после переезда. Обе стороны стережёт проверка `[сайт]` в `tests/full-run.js`: и пропажу ссылок, и косую черту |
| 🔸 | **Ужать экран `#about`** — сделан ПЕРВЫЙ шаг, **1.133.0**: убран блок «Чего нет у других» (четыре плитки, 49 строк) и на его месте врезка «Подробнее — на сайте Фионики» со ссылкой. Убран не как устаревший, а как ПЕРЕЕХАВШИЙ: те же три отличия теперь на `/vitrina/`, а две копии одного обещания расходятся с первой же правки. ⚠️ Остальное оставлено **сознательно, решение фаундера 09.09.2026**: пока корень домена не отдаёт витрину, единственная живая ссылка ведёт в приложение, и холодный человек из поиска попадает на этот экран. Карта экзамена, программа курса и «Когда на урок нет сил» витриной НЕ повторены — выкинуть их значило бы стереть их отовсюду. Живое окно редактора и вкладки ролей на месте. **Дальше ужимать — после переезда на домен, и вместе с переносом этих трёх блоков на витрину** |
| 🔸 | Справочник `/baza/`, заведён 09.09.2026. ✅ «Ребёнок не хочет учиться» (16 383 показа/мес), ✅ «Сколько стоит репетитор по информатике» (25 835 показов/мес, цены одиннадцати школ из `market-research.md` § 2 с источниками и датой замера). ✅ «Информатика на семейном обучении» — **1.139.0, 11.09.2026**: учит семья, проверяет школа (273-ФЗ ст. 17 ч. 3, ст. 58 ч. 1 и 10 — сверено по Гаранту), четыре раздела федеральной программы 7–9 базового уровня (edsoo.ru, ред. 2025: 1 час в неделю) и честная таблица «что из этого у нас»: программирование — есть, теория — задачами экзамена, цифровая грамотность — частично, офисные программы — нет. ⚠️ Открытый вопрос форсайта «что спрашивают на аттестации по информатике» закрыт **наполовину**: общий состав предмета теперь известен, а что спрашивает конкретная школа — нет, и статья прямо отправляет за этим в школу. ⬜ Дальше: индивидуальный проект, «как понять, что сделал сам». ⚠️ В статьях с ценами дата замера и источник обязательны — иначе через полгода страница врёт |

## 2.2. Юридический слой (разбор — там же, § 1)

| | Что |
|---|---|
| ✅ | **Правка сертификата:** `js/app.js:8950` печатает «Сертификат об окончании курса» — единственная строка продукта, называющая себя документом об обучении. **Сделано 11.09.2026, 1.137.0:** лист за курс называется **«Курс пройден»** (слово фаундера), внизу мелко «Не является документом об образовании» — вторая половина рекомендации из разбора § 1.3. Листы за миры и разделы остались «Сертификатом»: они не называют себя документом об окончании. Сторожит `[сертификат]` в `full-run.js`: название листа, отсутствие слов документа (окончание, свидетельство, диплом, удостоверение, аттестат) и строка-оговорка; проверено тем, что на старой надписи падают все три |
| ✅ | Убрать обещание «запись работы **для аттестации**»: аттестовать мы не аккредитованы. **Проверено 11.09.2026 — делать нечего:** этой фразы нет ни на страницах, ни в `js/`, ни в `content/`; на `/semeynoe-obuchenie/` прямо написано «Мы не аттестуем и не выдаём документов об образовании». Когда именно она ушла — не установлено |
| ✅ | Политика обработки ПДн — `pravo/politika/`, сделано 09.09.2026. реквизиты подставлены 09.09.2026 (Фионов А. И., самозанятый, ИНН 341802022893, реестр ПДн 77-26-563356) |
| ✅ | Условия использования — `pravo/soglashenie/`, сделано 09.09.2026: доступ к программе, не образовательная услуга. реквизиты подставлены |
| ✅ | `kontakty/` и `o-proekte/` — сделано 09.09.2026. реквизиты и почта подставлены; ⚠️ ящик ещё не заведён (§ 3) |
| ⬜ | Оферта, реквизиты, ИНН — **только вместе с оплатой**. ⚠️ Оплата не с нуля: ЮKassa и автоматические чеки НПД через API «Мой налог» уже работают в айчеке |
| ⬜ | Дополнить уведомление РКН новой целью «расчёты за доступ» (оператор уже зарегистрирован: 77-26-563356) |

✅ **Выяснено 09.09.2026: образовательная лицензия НЕ нужна** (273-ФЗ ст. 2 п. 17
и ст. 91 ч. 1). Не переоткрывать.

## 2.3. Продукт: что строить

| | Что | Откуда |
|---|---|---|
| ⬜ | **«Проект под защиту»** — пакет для индивидуального проекта: паспорт, пояснительная записка (пересказ программы словами уже есть), презентация, речь, **протокол работы** из записи авторства | § 7.2 разбора 09.09. Рынок платит бирже в среднем 2 400 ₽; разовый чек с дедлайном конвертируется лучше подписки |
| ✅ | **Режим экзамена у варианта:** таймер, без подсказок и решения, один заход | **1.127.0**, 09.09.2026 (коммит `02caa5e`). Время идёт, подсказок нет, проверка молчит до итога, заход один. Сторожит `[вариант]` в `full-run.js`. ⚠️ Отметка проставлена 10.09.2026 задним числом: работа сделана параллельной веткой, а в очередь не вернулась — ветка закрылась в тот же день |
| ✅ | **Вариант, назначенный репетитором** всей группе + отчёт по просевшим номерам | **1.128.0**, 09.09.2026 (коммит `392f870`). Уезжает код-семя, а не задания; семя ОДНО на всех (иначе отчёт по группе бессмыслен), а у домашки наоборот — своё у каждого. Отчёт считает только тех, у кого собран именно этот вариант, и учеников поимённо не называет. Сторожит `[группа-вариант]`. Отметка задним числом, как строкой выше |
| 🔸 | **Восстановление доступа.** Потерял код — потерял прогресс | **Разобрано и закрыто иначе, 1.130.0, 09.09.2026** (коммит `cdba405`). ⚠️ Читать буквально: **восстановления нет и не будет** — искать чужой прогресс не по чему, потому что мы не спрашиваем ни почты, ни телефона, а публичный поиск «найди мой код» отдавал бы чужие коды кому угодно. Сделана не дверь, а то, чтобы потеря стала редкой: напоминание после первого сданного урока (гаснет по действию, а не по показу), карточка доступа на печать — из тренажёра и из кабинета репетитора, экран «Не помню код» с честным концом вместо «напишите нам», и показ кода в предупреждении на выходе. Сторожит `[доступ]`. **Осталось от строки:** дорога для того, кто код всё же потерял, — и она упирается в § 7 (аккаунтов не делаем), то есть открывается только вместе с оплатой, где плательщик уже известен |
| ⬜ | Автоматический отчёт письмом после занятия. ⚠️ нужен и таймер на стороне функции: самый ценный отчёт — про то, чего НЕ случилось | `zanyatie-i-vzroslyj.md` § 12; заморожено режимом наполнения |
| ⬜ | Запись авторства по ссылке (`/verify`) + печать как приложение к портфолио | ждёт сервера |
| ⬜ | Напоминание взрослому, что задача от ребёнка его ждёт | ждёт почты |
| ⬜ | **HTML и CSS** — показать печатаемый проектом HTML настоящей страницей | 🟢 из `napravleniya-2026-09-06.md`; не второй язык, а вторая грань информатики |
| ⬜ | Игры-проекты: Морской бой, Тамагочи, Лабиринт, Викторина | 🟢 оттуда же |
| ⬜ | Карта пути ученика; близнецы для миров 4–5 | оттуда же |
| ⬜ | Сквозная линия мастерской: деталь урока 7 стыкуется с деталью урока 23 | ⚠️ переписывание курса — только если механика зацепит живого ребёнка |

## 2.4. Наполнение, где тонко

| | Что |
|---|---|
| ✅ | Числовая половина ОГЭ 14 отдельными задачами в форме именно этого задания — **1.129.0**, 09.09.2026 (коммит `fbe28c0`). Отметка задним числом 10.09.2026 |
| ✅ | Робот: комбинации приёмов (стена с уступами И проходами, обход с возвратом) — там же, **1.129.0**. Задач у Робота стало 11 |
| ⬜ | «Ты и ИИ» вглубь — механики написаны, нужен контент |

## 2.5. Архитектурный долг

| | Что |
|---|---|
| ✅ | **Главный экран разложен на карточки** — **1.131.0**, 09.09.2026 (коммит `ec96c42`): девять карточек, каждая объявляет в `needs` ровно то, что трогает. Сторожит `[главный]`: необъявленное имя даёт `undefined`, и кнопка молча умирает. Порог — не больше 20 имён на карточку. Отметка задним числом 10.09.2026 |
| ⬜ | Домашка и «Ты и ИИ» — **два разреза, а не один**: связаны слабо, живут в одном разделе исторически |
| ⬜ | Остальные разделы по одному за сессию, каждый раз с новым замером |

---

# § 3. Висит на фаундере, не на сессии

| | Что |
|---|---|
| ⬜ | **Сменить `ADMIN_KEY`** — засветился в логе деплоя 05.09. Консоль Yandex Cloud → функция → Редактор → переменные окружения → новое длинное значение → сохранить и задеплоить. Сайт пересобирать не надо |
| ⬜ | **Завести почтовый ящик `fionika.help@yandex.ru`** (переименован вслед за продуктом 09.09.2026). Он уже стоит на страницах «Контакты», «Политика» и «Условия» — до создания ящика письма будут уходить в никуда, поэтому публиковать эти страницы раньше нельзя |
| ⬜ | Решение: включаем ли Метрику. ⚠️ **Барьер ниже, чем считалось:** в айчеке счётчик уже стоит (`METRIKA_COUNTER_ID`, цели, OAuth-токен), то есть для фаундера это не новая категория ни технически, ни юридически. Своя политика у Фионики есть, остаётся дополнить цель в уведомлении РКН |
| ✅ | **Переименование в «Фионика» — сделано 09.09.2026.** Витрина, приложение, `content/`, манифест, `og/` с пересобранной картинкой, `404.html`, `cloud/index.js`, `README.md`, `HANDOFF.md`, `CLAUDE.md`, проверки. Коммиты `1e3274b`, `9e724b6`, `062b49e`. Хвост — `js/app.js` (14 мест со склонением: «Ученик Фионики», «программа из Фионики», «Поставь Фионику»), `package.json`, `icon.svg` и `dist` — закрыт параллельной веткой, коммит `dc923a5`. Русского имени в коде больше нет нигде. Старое имя намеренно оставлено в датированных документах (см. § 5) и в ключах хранилища `kodokvest_*`|
| ⬜ | Купить `fionika.ru` и `фионика.рф` (оба свободны на 09.09.2026) |
| ✅ | Выдача Яндекса по «фионика» — проверена фаундером 09.09.2026: живого бренда нет, только карточки ликвидированных юрлиц |
| ⬜ | **Решение: меняем ли имя продукта.** `kodokvest.ru` занят живым конкурентом в той же нише (замер 09.09.2026 в `market-research.md`). Решать ДО покупки домена |
| ⬜ | Проверить выбранное имя по открытым реестрам ФИПС (товарные знаки) — не делалось ни разу |
| ⬜ | Решение: покупаем ли домен и какой |
| ⬜ | Решение: выходим ли из режима наполнения и когда |
| ⬜ | **Удалить ли тестового ученика `anya-gfjvk` с сервера.** Создан 11.09.2026 моей ручной проверкой 1.138.0 (§ 4.24): имя «Аня» и один пройденный урок, персональных данных нет. Удаляется админ-ключом; можно и оставить — он никому не мешает |

---

# § 4. Правила работы, оплаченные ошибками

1. **Перед `git add -A` смотреть `git status`** — в папке бывает второй чат.
2. Замер выбирает, **где** резать; правильность разреза говорят сборка и тест.
3. Замена имён — только **вне строк и комментариев** (`data-sfx-set` ломается молча).
4. Исключая имя из зависимостей, исключать **каждое вхождение по отдельности**.
5. Чужое изменяемое состояние **читается вызовом, пишется отдельным входом**
   (`A.S()`, `A.session()`, `A.newSession(v)`).
6. Списки экранов собираются по регистрации в `KVSCREENS`, а не по имени файла.
7. **Щёлкать по кнопкам руками:** ошибки в `onclick` тесты не видят.
8. Число без даты и ссылки — не число (правило `market-research.md`).
9. ⚠️ **Пустой вывод — не ноль.** 09.09.2026 `grep -c "Кодоквест" js/app.js` вернул
   пустую строку, я прочитал это как «вхождений нет» и объявил, что файл
   переименовывать не нужно. На самом деле их было 12, и среди них строка,
   которую видит ребёнок. Считать вхождения тем, что печатает число всегда
   (`python3 -c "print(open(f).read().count(w))"`), и не делать вывода из
   молчания команды.
10. ⚠️ **Список исключений в `git add` защищает только от известного чужого.**
   Тот же день: коммит по именам файлов всё равно утащил незакоммиченную
   работу соседа, потому что мы правили ОДНИ И ТЕ ЖЕ файлы, а `git add`
   берёт файл целиком. Перед массовой правкой по каталогу — спросить, не
   сидит ли кто-то в этих же файлах.
11. ⚠️ **Поправка к правилу 9, от параллельной ветки: причина не в «пустом
   выводе», а в `grep`.** `js/app.js` весит 1,2 МБ, и grep считает его
   ДВОИЧНЫМ: `grep -c` печатает пусто и выходит с кодом 1, `grep -n` молчит.
   То есть молчала не статистика, а поиск целиком — по этому файлу нельзя
   искать вообще ничего без ключа `-a`. Правильно: `grep -ac "Фионика"
   js/app.js`. Я потерял на этой же ловушке минут двадцать, решив, что список
   тем раздела живёт только в собранном `dist`. Пиши `grep -an` по app.js
   всегда, привычкой.
12. **Очередь работ между двумя ветками, чтобы не переписывать друг друга.**
   Договор простой: у файла в один момент один хозяин, и хозяин объявляется
   здесь. Сейчас:
   — за витриной, страницами, доменом, `README`, `CLAUDE.md`, `cloud/` и этим
     файлом — та ветка, что вела переименование;
   — за `js/app.js`, `js/variant.js`, `js/home.js`, `js/account.js`,
     `js/algo-*.js`, `js/robot-tasks.js`, `tests/full-run.js`, `css/style.css`
     и `dist` — параллельная (за день: режим экзамена, вариант группе,
     наполнение ОГЭ 14 и Робота, восстановление доступа, версии 1.127–1.130.1).
   ⚠️ **Следующее в очереди — архитектурные разрезы D.12–D.14** из
   `docs/zadachi-novyi-chat-2026-09-08.md`: Главный экран на карточки, разрез
   «домашка + Ты и ИИ», остальные разделы. Они переписывают `js/app.js`
   целиком, и делать их можно, только когда в этом файле больше никого нет.
   Если в `js/app.js` нужно что-то от витрины — напиши это одной строкой
   здесь, и параллельная ветка сделает это заодно, а не параллельно.
   И два правила, оплаченных сегодня: **коммитить свой кусок сразу, как он
   зелёный** (не копить до конца волны) и **добавлять свои файлы по именам**.
13. ⚠️⚠️ **Правило полной ширины (10.09.2026, четвёртая жалоба подряд).**
   Абзац занимает ВСЮ ширину своего блока. `max-width` на тексте запрещён —
   и в `css/style.css`, и в `css/pages.css`. Ширину держит одно место:
   `.wrap`, 1080 px.
   Почему это правило, а не вкус: потолок 62–70ch обрывал строку на 606–739 px
   при доступных 1044, текст уезжал на вторую строку, справа пустовала
   половина блока — и страница от «заботы о читателе» становилась ДЛИННЕЕ.
   Фаундер сказал это четыре раза разными словами: «сверху широко, ниже
   почему-то узко», «текст обрезан», «как плевок почти по центру», «опять
   текст режется».
   **Длину выбирает ТЕКСТ, а не колонка.** Строка кажется длинной — сокращай
   текст (лид раздела держим короче 130 знаков). Разбор и список снятых
   потолков — `docs/pravila-sajta.md` § 14; стерегут два теста в `full-run.js`
   («нет потолка» и «лид короче 130 знаков»), оба проверены мутацией.
   ⚠️ Новый абзац смотреть на 1440 px: он обязан доходить до правого края.
14. ⚠️ **Число, написанное на странице руками, стареет молча.**
   10.09.2026: на `/repetitoru/` со вчерашнего дня стояло «сегодня это 1 065
   сверок», а сверок стало 1 178 — строка была правдой ровно сутки, и заметить
   это было нечем. В приложении такого не бывает: там числа вывески
   СЧИТАЮТСЯ (`aboutCounts`), а страница сайта считать не умеет — в ней нет
   ни строчки кода.
   Правило: **на странице живёт либо число, которое сверяет тест, либо факт
   без числа.** «Все примеры сверяются с настоящим python3» не стареет
   никогда; «1 065 сверок» устарело за день. Числа, которые остались
   (уроки, проекты, упражнения про ИИ), сверяет `[числа-сайта]`.

15. ⚠️ **Ряд карточек, который не кончается ровно, — это дыра справа.**
   Четыре карточки по 260 px не влезали в колонку 1044 на 38 пикселей и
   ложились 3 + 1: пустая треть справа и лишний ряд высоты. Так стояли шесть
   сеток из восьми, и никто этого не назвал, потому что жаловались всегда на
   ТЕКСТ. Это то же правило симметрии, просто повёрнутое на бок.
   Разбор и лечение — `docs/pravila-sajta.md` § 15; стережёт `[ряд карточек]`.

16. ⚠️⚠️ **`node tests/full-run.js` без сборки проверяет ВЧЕРАШНИЙ файл.**
   Самая дорогая грабля 10.09.2026 — час впустую. Тест грузит не `js/*.js`, а
   `dist/kodokvest.html`, и `npm test` собирает его ПЕРЕД прогоном. Запустишь
   `node tests/full-run.js` голым — увидишь сборку на один прогон старше.
   Выглядит это так, будто **тест мигает**: правка не видна, следующий прогон
   показывает результат предыдущего, мутация «не ловится», а потом ловится не
   та. Я успел объявить проверку нестабильной и переписать её дважды, прежде
   чем понял, что нестабильности не было вовсе.
   **Всегда `npm run test:full` или `npm test`.** Голый запуск — только если
   сборка заведомо свежая.

17. ⚠️ **Руками проверяешь — сначала сними service worker.**
   Того же дня: правка в `js/app.js` есть, тесты зелёные, а в браузере ничего
   не изменилось — старый worker отдавал старый скрипт из кэша. Проверять
   надо так: `navigator.serviceWorker.getRegistrations()` → `unregister()`,
   `caches.keys()` → `delete`, перезагрузка. Иначе смотришь на прошлый выпуск
   и правишь то, что не сломано.

18. ⚠️ **Защита от класса ошибок стоит в ОДНОМ месте.**
   Проверку «пока разбираем адрес, писать адрес нельзя» я поставил дважды —
   и в `setRoute`, и в `routeFor`. Выглядело безобидно, а сделало правило
   непроверяемым: снимаешь защиту в одном месте, второе молча держит, тест
   говорит «всё хорошо». Дубль защиты — это дыра в проверке, а не запас.

19. ⚠️ **Решение по ВРЕМЕНИ в асинхронном коде — гонка. Решай по СОСТОЯНИЮ.**
   Первая версия адреса заводила флаг «сейчас разбираем ссылку». Экраны
   дорисовываются асинхронно, продолжение приходит уже со сброшенным флагом —
   и один и тот же заход давал то один адрес, то другой. Заменено правилом,
   которое не смотрит на часы: **стираем только ЧУЖОЙ адрес — тот, что
   записан в таблице за другим экраном**; незнакомый не трогаем, он либо
   поставлен самим экраном (адрес с параметром), либо пришёл со ссылкой.

20. ⚠️ **Ранний выход уносит с собой всё, что стоит после него.**
   `setRoute` выходил, если адрес уже нужный, — а следом за выходом стоял
   заголовок вкладки. И получилось, что урок, открытый ПО ССЫЛКЕ, сидел во
   вкладке без имени: ровно тот случай, ради которого адрес и заводили.
   Тест не увидел (он ходил кнопками, где адрес меняется), нашлось нажатием —
   правило 7 в чистом виде. Заголовок теперь ставится ПЕРВЫМ и всегда.

21. ⚠️ **Надпись кнопки говорит, ГДЕ человек, а не какая роль у устройства.**
   10.09.2026: фаундер вышел из кабинета, а в шапке осталось «Кабинет
   репетитора» — потому что надпись читалась по `isAdminDevice()`, а роль
   после выхода намеренно остаётся (выход закрывает замок, но не «съезжает с
   квартиры»). Выход выглядел несработавшим. Чинится надписью, а не ролью:
   замок закрыт — «🔒 Войти в кабинет».

22. ⚠️ **Переносишь действие в другое место потока — сначала прочитай, что
   оно СТИРАЕТ.** 11.09.2026 задача звучала безобидно: «спросить имя не на
   входе, а после первой победы». Но `doRegister` первой строкой звал
   `resetProgressLocal()` — на входе это было правильно (прогресса ещё нет),
   а после победы стёрло бы ровно то, ради чего имя и спрашивают. Тест бы
   этого не увидел: он регистрировал на пустом прогрессе. Нашлось только
   чтением функции ДО правки.

23. ⚠️ **Правило 11 касается и `grep -r` по каталогу.** 11.09.2026:
   `grep -rn "Посмотреть первый урок" .` нашёл фразу только в `HANDOFF.md` и
   молча пропустил `js/app.js` — а там их было три, включая текст помощи.
   Рекурсивный поиск по проекту — тоже только с `-a`: `grep -ran`.

24. ⚠️⚠️ **Собранный `dist/kodokvest.html` пишет в НАСТОЯЩЕЕ облако.** В нём
   прошит адрес рабочей функции Yandex Cloud (`js/cloud-config.js`), и
   локальный сервер этого не меняет. 11.09.2026 при ручной проверке «имя после
   победы» я вписал имя — и страница десять раз сохранила тестового ученика
   `anya-gfjvk` на рабочий сервер. Вреда нет (имя и один урок), но так же
   легко насорить сотней записей. Руками проверяешь регистрацию или вход —
   сначала `CLOUD_CONFIG.url = ""` в консоли страницы (тест делает то же,
   подставляя `srv.invalid`), после — `Cloud.forgetCode()` и очистка
   хранилища. Запись `anya-gfjvk` на сервере оставлена: удаление — админ-ключом
   и решением фаундера (§ 3).

25. ⚠️ **Число прописью — это число, спрятанное от теста.** На
   `/semeynoe-obuchenie/` с 09.09.2026 стояло «девяносто пять задач» словами:
   проверка `[числа-сайта]` ищет цифры и его не видела, а задач к 10.09 стало
   97. Страница врала два дня при зелёных тестах. На страницах сайта числа —
   только цифрами; число задач теперь тоже в проверке.

---

# § 5. Карта документов: где что искать

| Файл | О чём |
|---|---|
| **`docs/RAZVITIE.md`** | **этот файл: что делать дальше** |
| `README.md` | как устроен продукт: экраны, движок, правила интерфейса |
| `HANDOFF.md` | история сессий и грабли |
| `docs/market-research.md` | замеры рынка, цены конкурентов, Wordstat, **журнал решений** |
| `docs/vitrina-litsenziya-napravleniya-2026-09-09.md` | Яндекс, лицензия, конкуренты, ниши, чем брать |
| `docs/foresight-2027.md` | ставки и стратегия вдолгую |
| `docs/napravleniya-2026-09-06.md` | что берём и что не берём из направлений |
| `docs/zanyatie-i-vzroslyj.md` | взрослый контур, занятие, ПДн |
| `docs/vyveska.md` | варианты вывески и почему выбран А |
| `docs/pravila-sajta.md` | правила оформления сайта |
| `docs/arhitektura-2026-09-08.md` | разрезание `app.js`, договор модулей |
| `docs/zadachi-novyi-chat-*.md` | **архив**, читать только за подробностями |

⚠️ **В датированных документах продукт называется «Кодоквест», и это не ошибка.**
Они — журнал замеров и решений: там записано, например, что имя «Кодоквест»
оказалось занято живым конкурентом. Переписать их задним числом значит
уничтожить смысл записи. Старое имя остаётся в истории; новое живёт в
продукте, на страницах и в этом файле.

---

# § 6. Приоритет монетизации (решение 03.09.2026, уточнено 09.09.2026)

**C → A.** C — лицензия кружку или репетитору, 15–30 тыс. ₽/год, один договор =
10–30 детей. A — подписка родителю **990–1 490 ₽/мес** (не 490: она конкурирует
не с тренажёром за 690 ₽, а с частью репетитора за 5–9 тыс. ₽). B (платное
сопровождение) снят — ручная работа на клиента.

Добавлено 09.09.2026: **разовая покупка с дедлайном** («Проект под защиту»,
1 500–2 500 ₽) стоит впереди подписки — у неё есть дата, страх и документ на
выходе, а у подписки на «просто учить Python» нет ничего из трёх.

⚠️ Экономика привлечения не чинится автономностью: только через репетитора
(привлечение делится на 10–30 детей) или через органику. Директ на родителя не
сходится втрое-вшестеро.

---

# § 7. Решения, которые НЕ переоткрываются без слова фаундера

| Решение | Дата |
|---|---|
| Режим наполнения: без выпуска, писем и оплаты | 06.09.2026 |
| **Главная витрина — статические страницы, а не экран `#about`.** Корень домена отдаёт витрину, приложение уезжает на `app.` (совпадает с `next-tasks.md` § 3) | 09.09.2026 |
| **Имя продукта — «Фионика»** (проверки: домен, знак, ЕГРЮЛ, выдача) | 09.09.2026 |
| **Видео в уроки не добавляем** — подтверждено повторно 09.09.2026 | 03.09 / 09.09.2026 |
| Образовательная лицензия не нужна; продаём доступ к ПО | 09.09.2026 |
| Взрослый сегмент и аналитика данных — не идём | 09.09.2026 |
| Вывеска — вариант А: «информатика» в заголовке | 03.09.2026 |
| **Сначала урок, имя — после первой победы.** Вывеска не просит представиться; имя спрашивает карточка победы у гостя. Регистрация гостя его прогресс НЕ стирает. Почему: человек из поиска пришёл посмотреть, а мы просили анкету раньше, чем показали, зачем | 11.09.2026 |
| **Лист за курс — «Курс пройден»**, с мелкой строкой «Не является документом об образовании». Слово «сертификат об окончании» не возвращать: без лицензии мы стоим ровно на том, что продаём доступ к программе, а не обучение с документом | 11.09.2026 |
| Аккаунты с паролями не делаем; регистрация по имени | 1.13.0 / 1.18.0 |
| Pyodide только в песочнице, не в курсе | 02.09 / 06.09.2026 |
| Голосовой ввод и Whisper — не строим | 03.09 / 06.09.2026 |
| Приложения в магазины — не делаем, PWA хватает | 03.09.2026 |
| Живой ИИ-помощник и чат-бот — нет | 03.09.2026 |
| Второй язык общего назначения — нет (вместо него Робот) | 08.09.2026 |
| Scratch, Java, C++ — нет | 06.09.2026 |
| КуМир в браузере — нет, ниша занята пятью бесплатными сайтами | 09.09.2026 |
| Электронные таблицы и ОГЭ 13 — честная граница продукта | 08.09.2026 |
| Детский лидерборд, ленты, дуэли, push ребёнку — красные линии | — |
| Широкая платформа по всем предметам — размывает ров в ноль | — |
| Модели монетизации с ручной работой на каждого клиента | 03.09.2026 |

## ⚠️ Видео: почему нет — разобрано заново 09.09.2026

Вопрос задан второй раз, разобран по числам, решение прежнее.

**Токены и деньги преградой не являются:** сценарии на все сто уроков — около
600 тыс. токенов, то есть **1 000–1 400 ₽** даже с двумя переписываниями. Дешевле
домена. Узкое место не здесь.

| Что ломается | Число |
|---|---|
| **Вес** | 3 мин скринкаста = 7–9 МБ (ужатых 3–4). Сто уроков = **350–900 МБ** против 2,86 МБ всей сборки. В 120–300 раз тяжелее продукта |
| **Хостинг** | GitHub Pages: репозиторий до 1 ГБ, трафик ~100 ГБ/мес → около 280 полных просмотров в месяц на весь сайт |
| **Внешний хостинг** | ⚠️ VK Видео / Rutube / YouTube ломают офлайн **и приводят чужие трекеры к детям** — то есть рушат «ноль ПДн», на котором стоит вся конструкция, включая ответ про лицензию |
| **Устаревание** | за две недели прошли 1.40 → 1.126. Каждая правка урока делает ролик неправдой. У текста цена правки — секунда, у ролика — перезапись, переозвучка, перезалив. Сто роликов = сто вечных обязательств |
| **Время** | 45–75 часов автоматом (генератор скринкастов 20–40 из них), 50–100 часов живой записью |

**Что вместо и уже работает, весит ноль:** чтение вслух (`A.speak`), визуализатор
с перемоткой, пересказ программы словами, «что сделала каждая строка» после
запуска. Это интерактивнее ролика и не устаревает — считается из текущего кода.

**Если когда-нибудь понадобится голос в уроке** — аудио, а не видео: Opus
24 кбит/с моно, 3 мин = 0,5 МБ, сто уроков ≈ 50 МБ, грузить по требованию, вне
`SHELL` и вне однофайловой сборки. В семь раз легче и ничего не ломает.

**Видео на витрину — другое дело и не отменяется** (см. § 2.1): 5–7 роликов по
1–2 минуты на посадочные страницы. Там вес не важен, офлайн не ломается, и есть
отдельная выдача Яндекс.Видео.

---

# § 8. Что закрыто в последние дни (чтобы не делать дважды)

| Что | Версия |
|---|---|
| Статья справочника `/baza/informatika-na-semeynom-obuchenii/`: закон об аттестации, четыре раздела предмета, что из них у нас; вписана в оглавление, `sitemap.xml`, список переезда и проверку чисел. На `/semeynoe-obuchenie/` числа переведены в цифры: «девяносто пять задач» → 97 | 1.139.0 |
| **Сначала урок, имя после победы:** на вывеске «Начать первый урок →» вместо поля имени; карточка победы гостя спрашивает имя; `doRegister` стирает только чужой прогресс; проверка `[вход]` | 1.138.0 |
| Лист за курс «Курс пройден» вместо «Сертификата об окончании курса» + строка «Не является документом об образовании»; проверка `[сертификат]` сторожит все три | 1.137.0 |
| **Адрес следует за экраном:** одна таблица `ROUTES` вместо читающей `HASH_SCREENS` и одиннадцати ручных присваиваний; свой `<title>` у каждого экрана; «Назад» и «Вперёд» ходят по экранам; у урока, мира и проекта адрес с параметром (`#lesson=print-first`) | 1.136.0 |
| Проверка `[адрес]` «кругооборот»: адрес, записанный при заходе, обязан открыть тот же экран. Пять мутаций | 1.136.0 |
| Посадочная `/shkole/`; на витрине четыре двери вместо трёх; `/shkole/` вписана в шапки и подвалы всех страниц, в `sitemap.xml` и в список переезда | 1.135.0 |
| Ряд из четырёх карточек кончается ровно, без дыры справа — одно правило в `pages.css` чинит шесть сеток из восьми (`pravila-sajta.md` § 15) | 1.135.0 |
| Три новые проверки: ссылки между страницами и карта сайта (`[страницы]`), числа на страницах против самих данных (`[числа-сайта]`), полнота ряда карточек. Все проверены мутацией | 1.135.0 |
| ⚠️ Правило полной ширины: у текста нет `max-width` ни в приложении, ни на страницах сайта; лид короче 130 знаков; стерегут два теста (`pravila-sajta.md` § 14) | 1.134.0 |
| Надпись в шапке говорит, ГДЕ человек: замок закрыт — «Войти в кабинет», а не «Кабинет репетитора» | 1.134.0 |
| Тонкие темы: все 15 групп доведены до пяти задач, 95 задач, порог стережёт тест | 1.121.0–1.126.0 |
| Администратора нет: только репетитор или школа | 1.124.0 |
| Экзамены вышли из-под «по желанию» | 1.123.0 |
| Пробный вариант экзамена целиком (`js/variant.js`) | 1.120.0 |
| Архитектурный долг, семь разрезов: `app.js` 19 790 → 18 366 строк | 1.108.0–1.119.0 |
| Робот с русскими словами, ОГЭ 15.1 закрыт, 9 задач | 1.113.0–1.114.0 |
| Карта экзаменов: ЕГЭ 27 из 27 | 1.110.0–1.112.0 |
| Роль переименована: наставник → репетитор | 1.105.0 |
| «Спросите вслух»: вопросы родителю из трассы | 1.76.0 |
| Мир «Экзамен» целиком: кодирование, логика, графы | 1.77.0 |
