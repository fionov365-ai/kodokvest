# Задачи для нового чата (07.09.2026, вечер)

Предыдущий файл — `docs/zadachi-novyi-chat-2026-09-06.md`: он закрыт почти
целиком, но читать его всё ещё стоит. Там записаны ПРАВИЛА, которые действуют
дальше, а не только список дел. Контекст сессии — `HANDOFF.md`, начинать с него.

Версия продукта **1.95.0**. Всё закоммичено и залито, `npm test` и
`npm run audit` зелёные, живой сайт на этой же версии.

## ⚠️ Режим прежний: НАПОЛНЕНИЕ, без официального выпуска

Решение фаундера 06.09.2026 в силе: письма репетиторам не шлём, продажи не
начинаем, **лицензию и оплату (3.8) не строим**. Переоткрывается только словом
фаундера.

## Что сделано 06–07.09.2026 (версии 1.79.0 → 1.95.0)

| Что | Версия |
|---|---|
| Настоящий Python по кнопке — только в песочнице, своя копия в `vendor/pyodide/` | 1.79.0 |
| Пять игр-проектов: по одной на каждый мир | 1.80.0 |
| Мир «Экзамен» закрыт по ТИПАМ (52 задачи, 11 групп) | 1.81.0 |
| Домашка накрыла весь курс (44 близнеца, 13/6/8/11/6 по мирам) | 1.82.0 |
| Карта пути: сто уроков одной дорогой | 1.83.0 |
| Пять находок фаундера на живом сайте (логотип, просмотр урока взрослым, витрина) | 1.84.0 |
| Выпускной мира: «что ты теперь умеешь» глаголами | 1.85.0 |
| Свой проект с именем: из песочницы в «Моё» и ссылкой | 1.86.0 |
| Витрина: код слева, живые числа, настоящие значения вместо решёток | 1.87.0 |
| Огонёк стал уговором + мобильная починка вывески | 1.88.0 |
| «План и факт» в группе + две починки в карточке ученика | 1.89.0 |
| Выровнены колонки «Угадай вывод» на вывеске (находка фаундера) | 1.89.1 |
| Застрявший ребёнок стал виден взрослому (вопрос фаундера) | 1.90.0 |
| Лестница выхода из затыка в самом уроке | 1.91.0 |
| Заметка наставника к уроку (и решение не строить совместную доску) | 1.92.0 |
| Пометка наставника к строке кода | 1.93.0 |
| Пример на вывеске рисует; потолок высоты вывода (4 находки фаундера) | 1.94.0 |
| Первый экран: выравнивание по верху, стрелка на число | 1.94.1 |
| Первый экран на телефоне: переносы кода и панель символов | 1.94.2 |
| Поля правки чисел на телефоне: 15×23 → 32×23 | 1.94.3 |
| Вывеска короче на четверть: 16 → 13,4 экрана телефона | 1.95.0 |

**Весь список «делать в режиме наполнения» из прошлого файла закрыт.**
Подробности каждой работы — в `HANDOFF.md`, там же записаны найденные ошибки
и почему решения приняты именно такими.

## ⚠️ Что обязано остаться верным (проверяется тестами, не намерением)

1. **Два контура.** Pyodide не попадает ни в `SHELL` в `sw.js`, ни в
   однофайловую сборку; без нажатия кнопки не грузится ни один байт; курс,
   экзамен, разминки и все сверки с python3 — всегда на `mini`.
   Стережёт `[два контура]` в `tests/full-run.js`.
2. **Красная линия «страх потерять серию».** Числа серии, рекорда и запаса
   щитов ребёнку не показываем нигде. Щит работает молча. Стережёт `[щит]`
   и `[уговор]`.
3. **Одно имя — одна функция.** В `js/app.js` не должно быть двух функций с
   одним именем: JavaScript на это не ругается, побеждает объявленная позже,
   и код молча зовёт не то. Стережёт `[имена]`. Тот же класс ошибки бывает и
   в CSS — `.wrap` уже занят классом страницы.
4. **Код ребёнка не виден взрослому без согласия.** Обещание записано
   РЕБЁНКУ в его же профиле: «сам экран и то, что ты печатаешь, им не видно»;
   код показывается, только когда он сам нажал «Показать экран наставнику».
   ⚠️ Черновики уроков лежат на сервере вместе с прогрессом, то есть показать
   их технически легко — поэтому запрет стережёт тест `[пометка]`.
5. Остальные красные линии без изменений: ИИ-«друг» и болталка; бесконечные
   ленты; сбор данных ребёнка; push ребёнку; лидерборды; скрытое наблюдение.

## Порядок для нового чата

### 1. Первое живое занятие — это не код

⚠️ Самое важное и уже трижды отложенное. Продукт наполнен, а **ни один чужой
ребёнок не провёл в нём ни одного занятия**. Из прошлого файла, раздел
«Проверки без кода», не сделано до сих пор:

1. Прожить «родительские десять минут» на своей семье (отчёт готов).
2. Проверить живое занятие на двух настоящих устройствах: ребёнок включил —
   взрослый увидел.
3. Сменить `ADMIN_KEY`, если ещё не сменён (засветился в логе деплоя 05.09).

Пока этого нет, любая новая функция строится вслепую.

### 2. Остаток по корзинам, в порядке пользы

- [x] **3.5 «План и факт» в группе** — сделано 07.09.2026 (1.89.0). Строка в
      каждой строке группы, счёт в сводке за неделю и блок в редакторе рамки.
      Считается от дня, когда рамку сохранили; допуск — одно занятие; «впереди»
      видно так же, как «отстаёт»; без рамки план не выдумывается.
      ⚠️ Попутно найдены и починены две ошибки в карточке ученика у наставника
      (экран падал при дате «успеть к»; остаток уроков считался по прогрессу
      самого наставника) — подробности в `HANDOFF.md`.
- [x] **Б. Лестница выхода из затыка в самом уроке** — сделано 07.09.2026
      (1.91.0). Три ступени: 4-я попытка → подсказка, 8-я или подсказки
      кончились → решение, решение открыто и 14-я попытка → отложить. Кнопка
      ступени нажимает ТУ ЖЕ кнопку урока, поэтому цена не выросла. Считается
      по текущему заходу, а не по журналу. Приходит на всех пяти путях
      неудачи, а не только на «Ещё не то».
- [ ] **В. Счёт затыков по урокам в `op=stats`** — топ уроков, где больше
      всего попыток без решения. Это детектор НАШИХ ошибок: если на уроке 34 у
      восьми детей из десяти по пятнадцать попыток и ни одного решения, дело
      не в детях. Данные уже в каждом снимке. (1 день)
- [x] **3.7 Заметка наставника к уроку** — сделано 07.09.2026 (1.92.0). Едет
      по коду вместе с прогрессом, ребёнок видит её ПЕРВЫМ блоком над теорией.
      По умолчанию предлагается урок, где он застрял. Обратного канала нет
      намеренно (болталка — красная линия), снятая заметка хранится
      надгробием, иначе воскресает.
      ⚠️ Этим же закрыт второй вопрос фаундера про «рабочее пространство
      репетитора»: **совместную доску строить не надо** — у репетитора уже
      есть видеозвонок с демонстрацией экрана, и наш транспорт (опрос раз в
      5 секунд) её не потянет. Разбор в `HANDOFF.md`.
- [ ] **4.3б Первое занятие после паузы — короче** (правка `zanPlan`; отложено
      сознательно в 1.61).
- [ ] **2.5 Оформление за прогресс**: наборы цветов и значков по мирам (1 день).
- [ ] **Близнецы для мира 2** — сейчас там 6 задач на 20 уроков, это самый
      слабый мир банка. Остальные: 13 / 6 / 8 / 11 / 6.
- [ ] **Генерация задач экзамена от кода ученика** — остаток пункта 1.1б,
      отдельная работа.

### 2б. Из вопросов фаундера 07.09.2026 — что осталось

- [ ] **В. Счёт затыков по урокам в `op=stats`** — уже записан выше.
- [ ] **Живая подсветка во время занятия** — отложено сознательно. Требует
      обновления облачной функции руками (канала взрослый → ребёнок нет: во
      время урока устройство ребёнка только шлёт и никогда не читает) и всё
      равно даёт задержку 10–20 секунд из-за кэша бакета. Для «увидел ошибку —
      подсветил» годится, для «смотри, я сейчас покажу» — нет, и для этого у
      репетитора есть видеозвонок.
      ⚠️ Через прогресс это делать НЕЛЬЗЯ: `op=save` — слепая перезапись без
      слияния, и сохранение взрослого откатит ребёнку до 25 секунд работы.

### 3. Решения, которые ждут фаундера

- ⚠️ **`behavior:"smooth"` молча не работает** во встроенной панели
  предпросмотра — ни в `scrollIntoView`, ни в `window.scrollTo`. В `js/app.js`
  от него зависят **52 места** (прокрутка наверх при смене экрана). В обычных
  Chrome и Safari smooth работает, поэтому по одному наблюдению 52 вызова не
  переписывались. Если фаундер видит на своих устройствах, что экран не
  прокручивается наверх, — это оно.
- ⚠️ **`numpy` и `pandas` не докачаны**: в `vendor/pyodide/` лежит только ядро.
  Если понадобятся — это отдельные файлы (numpy ~7 МБ) и +памяти в гите.
  Пакеты обязаны быть от ТОЙ ЖЕ версии Pyodide (314.0.6).
- ⚠️ **Обновлять Pyodide не надо** и не планируется: он крутится в песочнице
  браузера и исполняет код самого ребёнка. Каждое обновление стоит ещё ~5 МБ
  в истории гита навсегда.
- **Демо на вывеске бесплатное и без ограничений**: с вывески можно пройти
  весь курс, не заводя профиля. Сегодня это так по устройству продукта
  («Сейчас бесплатно» написано прямо на странице). Ограничивать — вопрос не
  починки, а выпуска.

## Как здесь принято работать (короткая версия)

- Правку проверять **глазами в браузере**, а не только тестами: половина
  находок этой сессии — «код исправен, а работает не то».
- ⚠️ **Всё, что посетитель или ребёнок может править сам, обязано иметь
  потолок.** Редактируемый код на вывеске — это приглашение ввести что
  угодно; единственная защита от «что угодно» — ограничение на нашей стороне,
  а не надежда на разумность вводящего. Найдено 07.09.2026: фаундер ввёл в
  демо-окно 1700 звёзд, и страница выросла до 600 000 пикселей.
- Новый экран без теста — экран, который тихо сломается. Тест **проверять
  мутацией**: сломал код — тест обязан упасть.
- Число в README и на вывеске, которое считается из данных, руками не писать.
- Перед `git add` смотреть `git status`: в одной папке бывают параллельные чаты.
- После зелёных тестов — коммит и `git push`: фаундер смотрит живой сайт.
