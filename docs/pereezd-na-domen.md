# Переезд на свой домен: список всех мест с адресом

Заведён 09.09.2026 вместе с первыми статическими страницами; страницы
справочника и `shkole/` дописаны 10.09.2026. Смысл файла один:
**в день покупки домена переезд должен быть заменой строк по списку, а не
поиском по репозиторию.**

Сегодня адрес площадки — `https://fionov365-ai.github.io/kodokvest/`.

## 0а. ⚠️ Имя сменилось, путь — ещё нет

Продукт называется **«Фионика»** (решение 09.09.2026). Но адрес площадки
по-прежнему `fionov365-ai.github.io/kodokvest/`, и **папка в путях остаётся
`kodokvest`** до переезда: переименовывать репозиторий сейчас — значит сломать
все уже разосланные ссылки ради косметики. Путь уйдёт вместе с доменом, одним
движением.

Почта тоже переименована: `fionika.help@yandex.ru` (ящик ещё не заведён).


## 0б. ⚠️ Что НЕ переименовывается никогда

**Латинские ключи хранилища браузера:** `kodokvest_theme`, `kodokvest_code`,
`kodokvest_python`. Переименовать их — значит стереть прогресс, тему и код
ученика у каждого, кто уже занимался: браузер просто не найдёт старые записи.
Ключи остаются со старым словом навсегда, их никто не видит.

То же касается `dist/kodokvest.html`, имени в `package.json`, кэша в `sw.js` и
папки `/kodokvest/` в путях — это адреса и идентификаторы, а не вывеска.

## 0. Что переезжает само и трогать не надо

Ссылки ученику и родителю строятся в браузере из `location.origin +
location.pathname`; манифест ходит по «.», service worker — по «./», стили и
скрипты подключены относительно; ссылки между статическими страницами —
относительные (`../`). Всё это переживёт переезд без единой правки.

## 1. Файлы, где адрес прошит намертво

| Файл | Что менять | Чей |
|---|---|---|
| `index.html` | три строки карточки ссылки: `canonical`, `og:url`, `og:image` | приложение |
| `robots.txt` | строка `Sitemap:` | статика |
| `sitemap.xml` | **все** `<loc>` | статика |
| `individualnyi-proekt/index.html` | `canonical`, `og:url`, `og:image` | статика |
| `semeynoe-obuchenie/index.html` | то же | статика |
| `repetitoru/index.html` | то же | статика |
| `shkole/index.html` | то же | статика |
| `baza/index.html` | `canonical` | статика |
| `baza/rebenok-ne-hochet-uchitsya/index.html` | `canonical` | статика |
| `baza/skolko-stoit-repetitor-po-informatike/index.html` | `canonical` | статика |
| `o-proekte/index.html` | `canonical` | статика |
| `kontakty/index.html` | `canonical` | статика |
| `pravo/politika/index.html` | `canonical` | статика |
| `pravo/soglashenie/index.html` | `canonical` | статика |

Проверка одной строкой — она же приёмка переезда:

```
grep -rl "fionov365-ai.github.io" --include="*.html" --include="*.xml" --include="*.txt" . | grep -v node_modules | grep -v dist/
```

После переезда список обязан стать пустым.

## 2. ⚠️ Что заработает только после переезда

- **`robots.txt`.** На github.io он читается только из корня домена
  (`fionov365-ai.github.io/robots.txt`), а мы лежим в папке `/kodokvest/`.
  Сегодня файл лежит впустую и это нормально: он написан заранее.
- **Вебмастер.** Подтверждать права имеет смысл на своём домене; поддомен
  бесплатного хостинга ранжируется плохо.
- **Метрика.** Ставить вместе с доменом, не раньше: до него считать нечего.

## 3. Порядок в день переезда

1. Купить домен (свободны `kodkvest.ru`, `kodoquest.ru`; `kodokvest.ru` занят
   до 28.04.2027).
2. Привязать к GitHub Pages: файл `CNAME` в корне репозитория + записи DNS.
3. Пройти по таблице § 1 и заменить адрес.
4. `grep` из § 1 — пусто.
5. Вебмастер: подтвердить, отдать `sitemap.xml`, указать регион.
6. Метрика: поставить счётчик — **только после решения фаундера**, он тянет за
   собой политику и дополнение уведомления РКН (`RAZVITIE.md` § 2.2).
7. Проверить, что старый адрес не остался в живых ссылках, которыми уже
   делились: страницы приложения строят ссылки сами, но письма и сообщения —
   нет.

## 4. Про российский хостинг

GitHub Pages — американский хостинг, и технические журналы обращения (IP)
лежат у него. Пока мы не собираем персональных данных, это терпимо; при
включении оплаты и приёме контактов покупателя вопрос переезда на российский
хостинг возвращается (`next-tasks.md` § 3, к юристу до платного запуска).
