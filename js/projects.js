/* ============================================================
   Проекты Кодоквеста — по одному в конце каждого мира.

   Чем проект отличается от урока: он МНОГОШАГОВЫЙ и все шаги строят
   ОДНУ И ТУ ЖЕ программу. Код переезжает из шага в шаг — ребёнок не пишет
   четыре отдельные задачки, а достраивает свою вещь и в конце уносит её
   целиком (кнопка «Забрать в песочницу»).

   Раздел ВНЕ сотни уроков: звёзд и XP не даёт, в счётчик «N из 100»
   не попадает. Прогресс — в S.projects. Но карточка проекта показывается
   прямо на карте мира после последнего урока, чтобы мимо неё не пройти.

   Правила для содержания (их проверяют тесты):
     - solution каждого шага — ПОЛНАЯ программа на конец этого шага;
       она обязана запускаться, что-то печатать и совпадать с python3;
     - solution шага обязан отличаться выводом от solution прошлого шага,
       иначе шаг ничего не добавляет;
     - starter первого шага обязан запускаться (ребёнок жмёт «Запустить»
       и видит работающую, пусть и неполную, программу) и НЕ проходить
       проверку — иначе задания нет;
     - у шагов 2+ стартовый код не пишется: туда переезжает код ребёнка
       с прошлого шага (а если его нет — solution прошлого шага);
     - в проекте можно пользоваться только тем, что объяснено в этом мире
       и раньше.
   ============================================================ */
window.PROJECTS = [
{
  id: "project-w1",
  world: 1,
  emoji: "🃏",
  title: "Карточка героя",
  tagline: "программа, которая рисует карточку персонажа",
  intro: "Собери программу, которая печатает карточку героя: рамка, имя, полоски силы и звание. " +
         "В конце ты сможешь подставить любое имя и любые числа — и карточка нарисуется сама.",
  steps: [
    {
      title: "Рамка и имя",
      brief: "Напечатай верх карточки: линия, имя большими буквами, линия.",
      list: [
        "Линия — это 26 знаков «=», но писать их руками нельзя: умножай строку на число",
        "Имя печатай большими буквами и с двумя пробелами в начале",
        "Всего три строки вывода"
      ],
      starter: 'имя = "Аня"\n\n# сверху и снизу — линия из знаков =, между ними имя большими буквами\nprint(имя)\n',
      solution: 'имя = "Аня"\nprint("=" * 26)\nprint("  " + имя.upper())\nprint("=" * 26)\n',
      needCode: ["* 26"],
      needMsg: "Линию нужно получить умножением строки на число, а не выписать знаки руками.",
      hints: [
        "Строку можно умножить на число: «-» * 5 даст пять минусов подряд.",
        "Большие буквы делает .upper() — он не меняет саму переменную, а возвращает новую строку.",
        "Два пробела перед именем — это обычная склейка: \"  \" + имя.upper()."
      ]
    },
    {
      title: "Полоска силы",
      brief: "Добавь функцию полоска(значение) и строку с силой героя.",
      list: [
        "Функция полоска(значение) возвращает строку из 10 знаков: сначала «#» по числу значения, потом «.» до десяти",
        "После имени добавь линию из 26 знаков «-»",
        "Под ней — строка «  Сила      » и полоска для силы 7",
        "Заведи переменную сила = 7 и передай её в функцию, а не число прямо в вызове"
      ],
      solution: 'def полоска(значение):\n    return "#" * значение + "." * (10 - значение)\n\n\nимя = "Аня"\nсила = 7\nprint("=" * 26)\nprint("  " + имя.upper())\nprint("-" * 26)\nprint("  Сила      " + полоска(сила))\nprint("=" * 26)\n',
      needCode: ["def полоска"],
      needMsg: "Полоску должна собирать функция полоска(значение) — так её потом получится вызвать три раза.",
      hints: [
        "Функция объявляется до того, как её зовут: сначала def, потом остальная программа.",
        "Внутри полоски две части: «#» столько раз, сколько значение, и «.» столько раз, сколько осталось до десяти.",
        "Сколько осталось до десяти — это 10 минус значение. Скобки тут обязательны: 10 - значение считается первым."
      ]
    },
    {
      title: "Все характеристики циклом",
      brief: "Покажи три характеристики через цикл и посчитай итог.",
      list: [
        "Заведи два списка: названия = [\"Сила\", \"Ловкость\", \"Ум\"] и значения = [7, 5, 9]",
        "Строки характеристик печатай циклом, а не тремя print подряд",
        "Названия должны выстроиться в столбик: добавь к названию пробелы до длины 10",
        "Итог посчитай циклом по значениям и напечатай «  Итого: 21» между линиями «-» и «=»"
      ],
      solution: 'def полоска(значение):\n    return "#" * значение + "." * (10 - значение)\n\n\nимя = "Аня"\nназвания = ["Сила", "Ловкость", "Ум"]\nзначения = [7, 5, 9]\n\nprint("=" * 26)\nprint("  " + имя.upper())\nprint("-" * 26)\nfor i in range(3):\n    подпись = названия[i] + " " * (10 - len(названия[i]))\n    print("  " + подпись + полоска(значения[i]))\nитог = 0\nfor з in значения:\n    итог += з\nprint("-" * 26)\nprint(f"  Итого: {итог}")\nprint("=" * 26)\n',
      needCode: ["for "],
      needMsg: "Три строки характеристик нужно напечатать циклом — иначе четвёртую характеристику придётся дописывать руками.",
      hints: [
        "Чтобы идти по двум спискам сразу, ходи по номерам: for i in range(3), а внутри названия[i] и значения[i].",
        "Длина названия у всех разная — узнать её даёт len(). Пробелов надо добить до десяти: 10 минус длина.",
        "Итог собирается копилкой: заведи итог = 0 до цикла и внутри цикла прибавляй к нему очередное значение."
      ]
    },
    {
      title: "Своя карточка из двух строк",
      brief: "Заверни всё в функцию карточка(имя, сила, ловкость, ум) и напечатай две разные карточки.",
      list: [
        "Функция карточка(имя, сила, ловкость, ум) печатает карточку целиком",
        "Отдельная функция звание(итог) возвращает «легенда» при 24 и больше, «боец» при 15 и больше, иначе «новичок»",
        "В строке итога покажи и число, и звание: «  Итого: 21 — боец»",
        "В самом конце — два вызова: карточка(\"Аня\", 7, 5, 9) и карточка(\"Боря\", 3, 4, 2)"
      ],
      solution: 'def полоска(значение):\n    return "#" * значение + "." * (10 - значение)\n\n\ndef звание(итог):\n    if итог >= 24:\n        return "легенда"\n    elif итог >= 15:\n        return "боец"\n    else:\n        return "новичок"\n\n\ndef карточка(имя, сила, ловкость, ум):\n    названия = ["Сила", "Ловкость", "Ум"]\n    значения = [сила, ловкость, ум]\n    print("=" * 26)\n    print("  " + имя.upper())\n    print("-" * 26)\n    for i in range(3):\n        подпись = названия[i] + " " * (10 - len(названия[i]))\n        print("  " + подпись + полоска(значения[i]))\n    итог = 0\n    for з in значения:\n        итог += з\n    print("-" * 26)\n    print(f"  Итого: {итог} — {звание(итог)}")\n    print("=" * 26)\n\n\nкарточка("Аня", 7, 5, 9)\nкарточка("Боря", 3, 4, 2)\n',
      needCode: ["def карточка", "def звание"],
      needMsg: "Нужны обе функции: карточка(имя, сила, ловкость, ум) и звание(итог). Иначе вторую карточку придётся печатать копипастой.",
      hints: [
        "Всё, что раньше печаталось само по себе, теперь уезжает внутрь функции — то есть получает отступ.",
        "Имя и числа больше не переменные сверху: они приходят в функцию как параметры, поэтому и карточки получаются разные.",
        "Звание удобно считать отдельной функцией: три ветки if / elif / else, и каждая возвращает своё слово."
      ]
    }
  ],
  finale: "Готово: у тебя своя программа-генератор карточек. Поменяй имена и числа в двух последних строках — " +
          "и получишь карточки для кого угодно. Забери код в песочницу, чтобы он остался с тобой."
}
];
