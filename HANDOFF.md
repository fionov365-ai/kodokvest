# Передача проекта: контекст для новой сессии

Этот файл живёт в корне репозитория и обновляется в конце каждой сессии.
Его содержимое можно целиком скопировать в новый чат как первое сообщение.

Состояние: **готово 100 уроков из 100 + восемь разделов вне сотни:**
«Сегодня» (дневной стрик со щитом + задача дня + расписание занятий), «Игры» (5 игр),
«Разминка» (14 упражнений: 7 «угадай вывод» + 7 «собери из блоков»),
«Визуализатор» (пошаговый разбор памяти с перемоткой и стрелками),
**«Ты и ИИ»** (19 упражнений: включая «вынеси вердикт» и «докажи, что ИИ неправ»),
**«Повторить»** (интервальный повтор трудных уроков), **«Шпаргалка»** (115 команд,
оверлей поверх любого экрана) и **«Портфолио»** (все собранные программы в одном
месте плюс шесть печатаемых сертификатов) и **«Своё задание»** (ребёнок сам
составляет задачу, движок считает правильный ответ, задание уезжает ссылкой
другу). Разминок теперь 21, и они открываются
по прогрессу. Есть тип
«предскажи память». Плюс **одиннадцать проектов**: по одному в конце каждого мира и
«Напарник» в разделе «Ты и ИИ». Плюс **регистрация по имени** (экран входа +
профиль). С 1.40.0 появился **второй контур — взрослого**: занятие на 20/30/45
минут вместо потока уроков, отчёт из четырёх строк, карта активности по дням и
часам, рамка занятий и задание ребёнку из кабинета. Курс закончен, идёт этап
«НАПОЛНЕНИЕ без официального выпуска» (решение фаундера 06.09.2026:
письма репетиторам не шлём, лицензию и оплату не строим — сначала контент;
список задач — **`docs/zadachi-novyi-chat-2026-09-07.md`**, он свежий;
предыдущий, `...-09-06.md`, закрыт почти целиком, но в нём записаны правила).
Версия 1.88.0.
**Наверху три вкладки — «Главное», «Тренировки», «Моё»** (было одиннадцать
кнопок), и каждый экран объясняет, что он такое и зачем нужен.
**Тема по умолчанию светлая** (тёмная — в один клик), на каждом экране есть
**«?» с подсказкой про это самое место**, на Главном — **поиск по урокам**,
а по адресу `#help` — **полная инструкция**.
На экране урока после запуска видно, что сделала каждая строка, рядом с
подсказками есть «Ревью кода» (свой линтер по дереву), на экране «Повторить» —
бестиарий ошибок, в визуализаторе — пересказ программы словами, в портфолио —
галерея рисунков и файлы `.py`. Сайт ставится на домашний экран и работает
без сети; на телефоне под редактором есть панель символов.

**Открытых вопросов к фаундеру на 1.18.0 не осталось** — все четыре, что висели,
он закрыл. Решения записаны в «ЧТО СДЕЛАНО В ПОСЛЕДНИЕ СЕССИИ» и в «ЧТО МОЖНО
ДЕЛАТЬ ДАЛЬШЕ». Не переоткрывай их без его слова.

**~~Появился один новый вопрос (1.19.0): что делать с Pyodide.~~ ЗАКРЫТО
02.09.2026 решением фаундера: не подключаем.** Ниже — как выглядел спор; сам
вопрос переоткрывать не надо. Документы про него расходились. `README.md` говорит «решение принято: не подключаем» (Flask и requests
в браузере не работают, 35–40 МБ купили бы четыре урока про pandas). А пункт 5
списка конкурентов ниже говорит «довести до рабочего состояния, перед продажей
нужен настоящий Python». Формально это не противоречие — README про КУРС, пункт 5
про ШКОЛУ, — но читаются они как спор. Прежде чем браться за Pyodide, спроси
фаундера, какая из двух рамок сейчас в силе.

**Про «аккаунты»:** фаундер сначала выбрал «полноценные аккаунты с логином/
паролем», но после честного разбора согласился на **средний вариант — регистрацию
по имени** (сделано в 1.13.0). Полные аккаунты с паролями сознательно НЕ делаем
(потребовали бы серверной авторизации, которой у статического сайта нет).
**Подтверждено ещё раз в 1.18.0:** вопрос задан прямо, ответ — оставить как есть.

# КАК ТЕПЕРЬ ОБНОВЛЯЕТСЯ САЙТ (важно, изменилось!)

Больше **не через архивы и не через GitHub Desktop**. Рабочая папка — это НАСТОЯЩИЙ
клон репозитория: `/Users/aleksandrfionov/Documents/GitHub/kodokvest` (привязывай
именно её). Старые папки-дубли (`kodokvest-site 2` и т.п.) и мусор из репозитория
вычищены.

**GitHub Desktop больше не используем** — он виснет на `.lock`-файлах. Цикл теперь
такой: правим файлы в клоне → фаундер выполняет в Терминале три команды одной
строкой:

```
cd ~/Documents/GitHub/kodokvest
find .git -name '*.lock' -delete
git add -A && git commit -m "описание" && git push
```

GitHub Pages пересоберёт сайт за минуту.

Фаундер нетехнический: перед git-действиями объясняй по шагам. Push, удаления и
изменения публичной инфраструктуры — только после его подтверждения.

---

Проект «Кодоквест» — игровой тренажёр Python для подростка 11–14 лет.

# ЧТО СДЕЛАНО В ПОСЛЕДНИЕ СЕССИИ (свежее — сверху)

**Карусель карточек на телефоне (1.96.0).** Идея фаундера: «на телефоне
карточки чтобы вправо-влево листались». Со второй попытки — первая описана
записью ниже и была откачена.

Три ряда стали листаться вбок: четыре довода «Чего нет у других» (лежали
стопкой на 1571 пиксель), карточки «Между делом» (816) и полоса чисел (541).

Листание — **родная прокрутка с прилипанием** (`scroll-snap-type:x mandatory`),
без библиотек и без обработчиков жестов: инерцию, отскок у краёв, работу
колесом и с клавиатуры браузер даёт сам. Скрипт делает ровно две вещи —
включает раскладку и рисует точки.

⚠️ **Карточка занимает 86% ширины, а не 100%.** Это главное в конструкции:
край следующей ВИДЕН. Карусель без выглядывающего края читается как обычный
блок, листать её никто не догадается, и три карточки из четырёх не прочитает
никто. Точки под рядом — вторая половина того же ответа: по ним видно, сколько
карточек всего и где ты сейчас. Точка ещё и кнопка: пальцем в неё попасть
проще, чем доскроллить.

Числа идут по две в кадр (46%), а не по одной: листать восемь штук по одной
долго, а прочитать их можно и мельком.

⚠️ **Раскладку задаёт СКРИПТ свойствами элементов, а не правила CSS**, и это
решение, а не небрежность. Первая попытка делала всё правилами, и карточки
упорно делились поровну по 79 пикселей — при том, что правило лежало в
загруженном листе, селектор совпадал и медиазапрос подходил. У свойства
элемента этого класса сомнений нет вовсе, а цена ошибки высокая: сломанная
карусель превращает четыре главных довода страницы в полоски. Заодно и отказ
мягкий: **не отработал скрипт — страница осталась ровно такой, какая была**.
В CSS осталась только косметика: спрятанная полоса прокрутки и точки.

⚠️ **Карусель обязательно СНИМАЕТСЯ** на широком экране и при повороте
телефона (слушаем `matchMedia`). Иначе ряд остался бы шириной в одну карточку
там, где карточки прекрасно стоят рядом.

Что проверено и как: раскладку в скрытой панели предпросмотра измерять нельзя
(см. запись ниже), поэтому проверял то, что от перерисовки не зависит, —
**свойства, которые скрипт проставил элементам**. На 375: у всех трёх рядов
`display:flex`, `overflow-x:auto`, `scroll-snap-type:x mandatory`, карточки
86%/46% с `flex:0 0 auto`, точки видны и по числу карточек. На 1400: все
свойства сняты, точки спрятаны.

Тесты: раздел 11б — класс на ряду, точки по числу карточек (не по
захардкоженному), карусель снята там, где экран широкий, повторный заход не
задваивает точки, и правило `.swdots[hidden]{display:none}` на месте: без него
`display:flex` перебивает встроенное поведение `hidden`, и точки видны на
широком экране. Проверено мутацией: четыре поломки — четыре падения.

⚠️ **Как оно выглядит — не проверено**, и это честная граница: панель
предпросмотра скрыта, страница в ней не рисуется. Выпущено на слово фаундера
«выпускай, посмотрю с телефона». Если смотрится не так — откат одной командой.

**⚠️ Карусель карточек на телефоне: попытка, откат и главные грабли сессии.**
Идея фаундера 07.09.2026 — «на телефоне карточки чтобы вправо-влево листались».
Резерв огромный: `.bigs` (четыре довода «Чего нет у других») лежит стопкой на
1571 пиксель, `.landgrid` — 816, полоса чисел — 541. Вместе около двух экранов.

Сделал: родная прокрутка с прилипанием (`scroll-snap-type:x mandatory`),
карточка 86% ширины — чтобы **край следующей был виден**, иначе листать никто
не догадается, — точки под рядом по числу карточек, точка кликабельна.
Библиотек и обработчиков жестов не нужно: инерцию, отскок и клавиатуру даёт
браузер сам.

**Откатил, не выпустив.** Замеры показывали, что карточки в `.bigs` и
`.landgrid` остаются шириной 79 пикселей — то есть делятся поровну, как будто
правила нет. При этом ряд чисел работал (161 пиксель = 46%), точки строились,
горизонтальной прокрутки страницы не появлялось.

Проверено по очереди и всё оказалось в порядке: правило есть в файле на
сервере, правило есть в ЗАГРУЖЕННОМ листе стилей, селектор совпадает
(`el.matches` истинно), медиазапрос подходит (`matchMedia().matches` истинно).
Не помогли ни `!important`, ни задание ширины свойством элемента из скрипта —
а свойство элемента перебить нечем в принципе.

⚠️ **Отсюда единственное разумное объяснение, и оно про инструмент, а не про
код: панель предпросмотра скрыта, страница в ней не перерисовывается, и
`getBoundingClientRect` отдаёт размеры от ПРОШЛОЙ раскладки.** То есть я
несколько раз подряд мерил результат правки, которая ещё не применилась.
Косвенно это подтверждается тем, что ровно так же вели себя снимки экрана:
после программной прокрутки они приходили пустыми.

**Что это значит на будущее:**

1. Пока панель предпросмотра скрыта, **менять CSS и проверять результат
   замерами нельзя**. Числа врут молча и выглядят убедительно. Годится только
   то, что не зависит от раскладки: наличие правила, совпадение селектора,
   текст, структура DOM.
2. Правку вёрстки надо смотреть **в настоящем браузере** — своём или на
   телефоне фаундера, — и только потом делать выводы.
3. Карусель, скорее всего, была рабочей. Переделывать её с нуля — работа
   примерно на полчаса; главный открытый вопрос не в коде, а в том, как она
   выглядит, и ответить на него может только человек, который видит экран.

Страница при этом уже короче: слияние разделов в 1.95.0 дало 16 → 13,4 экрана,
и это выпущено. Карусель добавила бы ещё примерно два экрана.

**Вывеска короче на четверть (1.95.0).** «Давай сократим страницу, она
слишком длинная». Было 13 006 пикселей — **16 экранов телефона**, стало
10 857 — **13,4 экрана**. Ни один аргумент не выброшен: убрана повторная
навигация, сжаты тексты и снят один блок, который сам себе противоречил.

**1. Два раздела про одни и те же пять миров стали одним. Главная правка.**

Подряд стояли «Программа: 5 миров, 100 уроков» (пять карточек стопкой) и
«Пять программ, которые ребёнок унесёт с собой» (пять вкладок по мирам). То
есть **на странице было две одинаковые навигации по одному и тому же курсу**,
и вместе они занимали почти пять экранов телефона (1683 + 1165 = 2848 px).

Теперь один раздел и одни вкладки: выбрал мир — видишь и чему он учит (уроки,
босс), и какую программу ребёнок соберёт в конце, и она тут же выполняется.
Стало 1387 px — **вдвое меньше**. Правило фаундера «вкладки вместо кучи
карточек» тут сработало дважды: и вместо карточек, и вместо второго раздела.

⚠️ На вкладке теперь имя МИРА, а не имя проекта: вкладки водят по курсу, а
проект — то, что лежит внутри мира. Подписывать навигацию названием
содержимого значило бы обещать, что вкладок пять «программ», тогда как под
каждой ещё и двадцать уроков.

**2. «Четыре вещи» ужаты вдвое по тексту** (2075 → 1827 px). Все четыре
довода на месте, вырезаны вторые формулировки того же: «прогресс копится и
догоняет сервер», «расхождение падает тестом у нас, а не всплывает у ребёнка»,
«скрытыми проверками, которых в условии не видно». Каждая из них — уточнение
уточнения, и родитель до них не доходит.

**3. Снят блок «Каждый день — одна маленькая задача»** с подробным пересказом
щита и дней подряд. ⚠️ Две причины, и обе не про место. Первая: пункт 4.4
сделан — число серии убрано у ребёнка отовсюду (1.88.0), и **продавать на
витрине механику, которую внутри намеренно приглушили, нечестно**. Вторая: это
механика удержания, а не довод, по которому родитель выбирает курс. Сам факт
«каждый день одна разминка из пройденного» остался одной строкой.

Разделы после правки выровнялись: самый большой 2,2 экрана вместо 2,6,
остальные 1,5–1,8. Раньше разброс был 0,7–2,6.

Тесты: в разделе 11в — `#worldinfo` с описанием мира рядом с его проектом,
список уроков внутри, вкладки подписаны мирами, и **карточек мира на странице
ровно одна**: пять сразу означало бы, что старый раздел вернулся. Проверено
мутацией: две поломки — две падения.

⚠️ Что ещё можно сократить, если понадобится (по убыванию): «Четыре вещи»
1827 px — можно снять четвёртый пункт «Читает вслух», это доступность, а не
довод выбора; «Честно про границы» 1265 px — четыре пункта можно ужать до
двух строк каждый. Ниже 12 экранов уже придётся выкидывать аргументы, а не
воду.

**Поля правки чисел на телефоне (1.94.3).** Сплошная проверка вывески на
мобильном по просьбе фаундера.

Сначала прогнал весь первый экран и всю страницу автоматом: у каждого элемента
сверил, не вылезает ли он за экран и не обрезано ли содержимое. **Ни одного
случая — ни на 375, ни на 320.** Горизонтальной прокрутки нет.

Нашлось другое: **в витрине «что создают ученики» 17 полей правки чисел, и
самое узкое — 15×23 пикселя.** Пальцем в такое не попасть, а приглашение
«поменяй число» стоит прямо там. Стало 32×23, площадь 345 → 754.

⚠️ **Растёт только ширина, и это не лень.** Высота строки кода 22 пикселя, а
поле уже 23. Пробовал добавить вертикальные отступы с отрицательным полем
(область нажатия растёт, вёрстка стоит) — получилось 38×41, но **поля соседних
строк начали наезжать друг на друга, четыре пары**. Тап в полосе наложения
уходит не в то поле, а это хуже мелкой цели. Пробовал раздвинуть строки кода —
высота задана глубже по дереву и правилом не перебивается. Оставил ширину.

`background-clip:content-box` держит серую заливку на самом числе, иначе поле
превращается в широкую плашку посреди кода.

⚠️ **Грабли проверки, из-за которых я трижды получил «правка не применилась»:**
при локальной проверке CSS отдаёт **service worker** — он кэширует
`css/style.css` по имени версии из `sw.js`. Снятия воркера и очистки caches
мало: остаётся обычный кэш браузера. Надёжный приём — подменить адрес стиля:

```js
document.querySelector('link[rel=stylesheet]').href = 'css/style.css?v=' + Date.now();
```

На живом сайте этой беды нет: версия в `sw.js` бампается каждым выпуском, и
кэш сбрасывается сам.

Ещё замер, не беда, но к сведению: **вывеска на телефоне — 16 экранов** (13 000
пикселей). Самый длинный раздел — «Четыре вещи, ради которых это и делалось»,
2125 пикселей, 2,6 экрана. При правиле «сайт не длинный» это стоит померить
глазами.

Тесты: два правила в разделе 11б — поля не мельче заданного и у них НЕТ
вертикальных отступов (обратная проверка: она ловит именно ту починку, которая
сталкивает строки). Проверено мутацией.

**Первый экран на телефоне (1.94.2).** Проверка мобильной версии после
1.94.1 — по просьбе фаундера «посмотри, как выглядит на мобильной». Нашлось
две беды, обе от того же рисунка.

**1. ⚠️ Строка с числом переносилась, и стрелка указывала в пустоту.** Было
`лепестков = 24   # ← поменяй это число` — 38 знаков. Замер: в редактор на
телефоне влезает **29 знаков при экране 375 и 23 при 320**. Строка ломалась
пополам, «это число» уезжало на перенос, и стрелка показывала уже ни на что.
То есть правка, сделанная РАДИ понятности, на телефоне понятность и убивала.

Починка: стрелка отдельной строкой сверху (`# поменяй это число ↓`, 21 знак),
и переменная переименована `лепестков` → `лучей`. Второе тоже по замеру, а не
по вкусу: `for i in range(лепестков):` — 26 знаков, тоже не влезало.

⚠️ Тест теперь проверяет ДЛИНУ КАЖДОЙ СТРОКИ примера (не больше 23 знаков) и
что строка со стрелкой стоит впритык к строке с числом. Первый же прогон
поймал две строки, о которых я не подумал, — то есть правило работает.

**2. Панель символов занимала три ряда телефона.** Это клавиатура-помощник
для ребёнка, который пишет код на планшете, — там она полезна. На вывеске она
съедала четверть первого экрана и отодвигала вниз рисунок, ради которого сюда
и пришли. А посетителю она не нужна: единственное, что он тут меняет, — число,
и цифры есть на любой телефонной клавиатуре. Спрятана в `.dwed`, как до этого
была спрятана колонка номеров, и по той же причине.

Замер после обеих правок: окно демо 751 → 626 пикселей, вся шапка вывески
1609 → 1484. Переносов в коде нет ни при 375, ни при 320; горизонтальной
прокрутки нет.

⚠️ **Мобильную версию нельзя проверять глазами через встроенную панель
предпросмотра**: когда панель скрыта, страница не рисуется, и снимок экрана
после программной прокрутки приходит пустым или устаревшим. Мерить надо
числами через `javascript_tool` — размеры, переносы, ширину прокрутки. Снимок
годится только для верха страницы.

Проверено мутацией: три поломки — три падения.

**Первый экран: выравнивание и «какое число менять» (1.94.1).** Две находки
фаундера сразу после 1.94.0, обе — следствие того же рисунка.

**1. Половины первого экрана начинались на разной высоте.** У `.lhin` стояло
`align-items:center`. Пока обе половины были примерно одной высоты, это сходило
с рук; как только справа появился рисунок, окно стало заметно выше — и
заголовок «Информатика без репетитора» уехал вниз от верхнего края окна почти
на двести пикселей. Стало `align-items:start`, замер после правки: верх обеих
колонок совпадает ровно.

⚠️ Класс ошибки тот же, что у обрезанной подписи в шапке окна: **правка в
одной половине разъезжается с вёрсткой, рассчитанной на прежние пропорции.**
Меняя высоту чего-то одного, смотри на соседей.

**2. «Надо явно показать, какое число менять, иначе кто не знает — не
поймёт».** Подпись под кнопками говорила «поменяй 24 на 6», но человек, не
писавший кода, не связывает цифру в подписи с цифрой в программе — это две
разные цифры в двух разных местах экрана.

Теперь указано ПРЯМО В КОДЕ, на той же строке:

```
лепестков = 24   # ← поменяй это число
```

Стрелка стоит на том же расстоянии от глаза, что и само число, и заодно
показывает, что такое комментарий, ничего про него не рассказывая. Подпись под
кнопками переписана под это: «поменяйте число в первой строке».

⚠️ В витрине ниже по странице для той же цели есть другой приём — `landNums` и
`landLiveCode` превращают числа в коде в поля ввода с пунктиром снизу. В шапке
он не годится: там стоит НАСТОЯЩИЙ редактор (`makeEditor`), тот же, что
откроется ребёнку, и подменять его полями значило бы соврать про «это тот же
редактор». Комментарий решает ту же задачу, ничего не подменяя.

Тесты: в разделе 11б добавлены проверки на `align-items:start` у `.lhin` (в
CSS и в сборке одним файлом, плюс обратная — что `center` не вернулся) и на
стрелку в коде примера, стоящую на строке с числом. Проверено мутацией.

**Пример на вывеске теперь рисует (1.94.0).** Четыре находки фаундера на
одном демо-окне в шапке главной — за один заход. Разбирать их порознь не имеет
смысла: три из четырёх лечатся одним решением.

**1. Подпись в шапке окна обрезалась посередине слова.** «это не картинка —
код можно править пр…». У `.dwtag` стоял `white-space:nowrap`, а окно режет по
`overflow:hidden`: в узкую колонку фраза не влезала. Замер на 1400px — тег
вылезал за окно на 80 пикселей. Лечится `flex-wrap:wrap` у шапки и обычным
переносом у подписи: фраза остаётся целой, шапка при нехватке места становится
в две строки.

**2. ⚠️ «Я ввёл 1700 звёзд, и сайт уехал просто».** Самая серьёзная из
четырёх. Пример печатал лесенку звёздочек, и 1700 давали 1700 строк вывода —
страница вырастала примерно до 600 000 пикселей. Мы САМИ зовём посетителя
«поменяй любое число» и сами же наказываем за большое.

⚠️ Чинится не примером, а окном: у `.dwout` теперь потолок высоты и своя
прокрутка. Код здесь правит ПОСЕТИТЕЛЬ, и страница обязана оставаться целой
при любом коде, который он напишет, а не только при нашем. Замер после
правки: те же 1700 строк — страница 6304 пикселя вместо 600 000.

**3. «И что запускать и менять число, если ниже звёздочки?»** Окно выполняет
код само при открытии (решение старое и правильное: вывеску читают, а не
изучают, и без автозапуска большая часть уйдёт, не увидев доказательства). Но
рядом с уже готовым результатом кнопка «▶ Запустить» вызывает ровно один
вопрос — какой и задал фаундер. Кнопка стала «▶ Нарисовать заново».

**4. «Можем пример сделать лучше? Может рисунок, чтоб рисовался? Что-то вау.
И чтоб люди могли менять и картинка менялась».** Пример заменён на мандалу:

```
лепестков = 24

color("#7c5cff")
for i in range(лепестков):
    circle(90)
    right(360 / лепестков)
```

Пять строк, одно число, названное словом. Поменял 24 на 6 — вместо мандалы
шестилистник; связь «правлю число → меняется картинка» видна без единого
объяснения.

⚠️ И рисунок закрывает пункты 2 и 3 заодно, а не только 4:

- он появляется штрихами, то есть САМ показывает, что код выполняется прямо
  сейчас, — звать «запусти» больше незачем;
- у холста постоянный размер, растянуть им страницу нельзя;
- а у черепашки есть собственный предел, и он уже написан по-человечески:
  5000 лепестков дают «Слишком много линий — черепашка устала», а не
  повисшую вкладку. То есть у рисунка защита от абсурдного числа встроена, а
  у печати её не было вовсе.

Устройство: `landDemoRun` заведён ОТДЕЛЬНО от `landRun`. Тому нужен только
текст (он обслуживает вкладки ниже по странице), здесь главное — холст.
Сводить их в одну функцию с флагом значит завести ветку, которую никто не
проверит.

Тесты: раздел 11б в `tests/full-run.js`. Высоту и обрезку в jsdom не измерить,
поэтому стерегутся правила, которыми они лечатся: нет `nowrap` у подписи, есть
`flex-wrap` у шапки, есть `max-height` и `overflow:auto` у вывода (и в CSS, и
в сборке одним файлом), пример рисует и НЕ печатает, сломанная версия правда
падает, пять тысяч лепестков дают ошибку словами, кнопка не зовёт «Запустить»,
подпись под окном не осталась от звёздочек. Проверено мутацией: четыре
поломки — четыре падения.

⚠️ Урок на будущее: **всё, что посетитель может править сам, обязано иметь
потолок.** Редактируемый код на вывеске — это приглашение ввести что угодно, и
единственная защита от «что угодно» — ограничение на нашей стороне, а не
надежда на разумность вводящего.

**Пометка наставника к строке кода (1.93.0).** Вопрос фаундера: «а чтобы
преподаватель подсвечивал код или писал подсказки в рабочем пространстве?»

⚠️ **Главное, что выяснилось при разборе: код ребёнка наставнику НЕ ВИДЕН, и
это обещание, данное самому ребёнку.** В его профиле написано дословно: «Сам
экран и то, что ты печатаешь, им не видно» и «Твой код и вывод — только если
ты сам нажмёшь „Показать экран наставнику“». То же на вывеске: «Подглядывать
через плечо тренажёр не умеет и уметь не будет».

Черновики уроков (`S.drafts`) при этом лежат на сервере вместе с прогрессом —
то есть технически показать код ребёнка взрослому было бы легко. Именно
поэтому запрет пришлось записать в тест: соблазн есть, а нарушение выглядело
бы как удобная функция.

**Что построено вместо.** Взрослый помечает строки НАШЕЙ ЗАГОТОВКИ урока —
того же текста, который увидит ребёнок, открыв урок. Тыкает в строку 2, пишет
фразу; ребёнок открывает урок и видит её, а сама строка отмечена точкой в
колонке номеров.

⚠️ **Номер строки сам по себе врёт.** Взрослый пометил третью строку, ребёнок
дописал две строки выше — и номер показывает не туда. Поэтому вместе с номером
хранится ТЕКСТ строки, а точка ищется по нему заново при КАЖДОЙ отрисовке
редактора:

- строка стоит на своём номере — точка там;
- строка переехала — точка едет за ней;
- строки больше нет — номера не даём вовсе, остаётся фраза с цитатой.

По той же причине **номера строки нет в карточке заметки**: он устареет от
первой же правки. Ребёнок узнаёт строку по цитате, а где она — по точке,
которая врать не может. Соврать «строка 3», показав на чужую, хуже, чем не
называть номер.

Устройство: пометки живут в той же записи `notes[урок]`, что и заметка из
1.92.0 (`{ t, by, at, marks:[{ln, src, t}] }`), едут тем же путём, сливаются
тем же правилом «свежая побеждает». До пяти пометок на урок. У редактора
появилось третье состояние строки в колонке номеров — `note`, слабее ошибки и
слабее текущей строки: пометка это «посмотри сюда», а не «здесь сломано».
⚠️ В отличие от `_errLine` и `_curLine` оно НЕ сбрасывается при наборе: ошибка
относится к последнему запуску, а пометка — к уроку.

**Чего сознательно НЕ сделано и почему — разбор транспорта.** Фаундер выбирал
между этим вариантом и живой подсветкой во время занятия. Живая требует:

- обновления облачной функции руками (канала взрослый → ребёнок нет вовсе:
  устройство ребёнка во время урока только ШЛЁТ и никогда не читает);
- и всё равно даёт задержку 10–20 секунд. Быстрее не выйдет: у бакета свой
  кэш в каждом экземпляре функции, соседние запросы отдают разное больше
  десяти секунд (замерено на бою 05.09, из-за этого зритель трансляции помнит
  самое свежее `serverAt` и назад не откатывается). Опрос хоть раз в секунду
  этого не обойдёт — сервер сам отдаёт устаревшее.

⚠️ И отдельно: писать пометки ЧЕРЕЗ ПРОГРЕСС во время занятия нельзя. `op=save`
на сервере — слепая перезапись без слияния. Сейчас это безопасно, потому что
взрослый пишет редко и осознанно; во время занятия писали бы оба каждые
несколько секунд, и сохранение взрослого откатило бы ребёнку до 25 секунд
работы, включая только что сданный урок.

Тесты: раздел «пометки к строкам» внутри 10б3 — поиск строки на своём месте,
после сдвига, после удаления, со сдвинутым отступом; запись только с пометкой
не считается пустой; номера строки в карточке нет; точка вживую едет за
строкой и исчезает вместе с ней; и красная линия — черновик ребёнка не
показывается взрослому. Проверено мутацией: три поломки — три падения (первая
попытка мутации на красную линию прошла мимо, потому что читала `kidTarget`,
которого в тесте нет; повторил через снимок — тест поймал).

⚠️ Попутно тест поймал сам себя: первый прогон падал, потому что в этом уроке
от соседнего раздела остался черновик, а уход с экрана сохраняет код обратно —
убрать черновик заранее нельзя. Заготовка ставится в редактор уже после
открытия урока.

**Заметка наставника к уроку (1.92.0).** Корзина 3.7 — и заодно ответ на
второй вопрос фаундера 07.09.2026: «можем ли мы сделать рабочее пространство,
где репетитор объясняет ребёнку на экране».

⚠️ **Совместную доску мы НЕ строим, и это решение, а не отсрочка.** Две
причины, обе записаны прямо в коде:

- **По делу.** Репетитор уже сидит с ребёнком в видеозвонке с демонстрацией
  экрана, и объясняет он там лучше, чем сделаем мы. Повторять Zoom значит
  соревноваться там, где мы заведомо слабее. Наше преимущество в другом:
  запустить код ребёнка, показать расхождение и передать задание.
- **Технически.** Связь у нас — облачная функция с опросом раз в пять секунд,
  и у хранилища уже замерен кэш, из-за которого кадры трансляции прыгают
  (зритель поэтому помнит самое свежее `serverAt` и назад не откатывается).
  Совместный редактор на этом транспорте не поедет: нужно постоянное
  соединение, другой хостинг и другие деньги.

Заметка делает то, чего видеозвонок не умеет: **работает, когда репетитора
рядом нет — а это почти всё время ребёнка.** Взрослый пишет две-три фразы к
уроку 34, они уезжают по коду вместе с прогрессом, и ребёнок видит их, когда
откроет этот урок. Хоть через неделю.

Как устроено:

- `S.notes = { "<урок>": { t, by, at } }`, рядом с домашкой в `PROGRESS_MAPS`,
  едет тем же путём. Слияние как у домашки — побеждает более свежая запись.
- **Показ ребёнку — ПЕРВЫМ блоком колонки объяснения, до теории.** Если
  наставник написал «начни со второго примера, первый мы разобрали», прочитать
  это после теории уже поздно.
- Сторона взрослого — в карточке ученика, рядом с домашкой. Список уроков не
  весь курс, а **пройденные плюс текущий**: заметка к уроку, до которого
  ребёнок не дошёл, — записка в пустоту.
- ⚠️ **По умолчанию выбран урок, где ребёнок ЗАСТРЯЛ** (из 1.90.0), и он
  помечен в списке словами «тут застрял». Это связывает А и 3.7: взрослый
  читает в отчёте «застрял на уроке «Переменные»» — и пишет заметку ровно
  туда, не выбирая ничего руками.

Три ограничения:

1. **Одна заметка на урок.** Не переписка и не лента: две правки одного текста
   это один текст. Новая заменяет прежнюю.
2. **Обратного канала нет,** и это сказано ребёнку и взрослому прямо: «ответить
   на неё ребёнок не может — это записка, а не переписка». Болталка — красная
   линия продукта, и заводить её с чёрного хода через «заметки» нельзя. Вид у
   заметки тоже не чатовый: записка на полях, рамка слева, никакой кнопки
   рядом.
3. **Дошло ли — видно по уже имеющимся данным.** Взрослому показывается,
   открывал ли ребёнок этот урок ПОСЛЕ того, как заметка написана: журнал и
   так синхронизируется. Расписки о прочтении нет намеренно — иначе пришлось
   бы заводить новое наблюдение и, по красной линии «ребёнок всегда видит,
   когда его видят», ещё и объявлять о нём ребёнку. Слишком тяжело для двух
   фраз на полях.

⚠️ **Снятая заметка хранится НАДГРОБИЕМ** (пустой текст со свежим временем), а
не удалением ключа. Иначе на устройстве ребёнка остаётся прежняя заметка с
непустым текстом, и при следующем обмене она воскресает — ребёнок читает то,
что взрослый уже стёр. Тот же приём, что у выключения трансляции. Стережёт
тест, проверенный мутацией в обе стороны слияния.

Тесты: раздел 10б3 в `tests/full-run.js` — список уроков, выбор по затыку,
показ первым блоком, заметка одного урока не течёт в другой, слияние в обе
стороны, надгробие на месте, «дошло ли» по трём состояниям, красная линия про
переписку. Проверено мутацией: шесть поломок — шесть падений.

**Лестница выхода из затыка (1.91.0).** Пункт Б из того же разбора вопроса
фаундера «что делать, когда ребёнок уперся в урок».

Выход из трудного урока был в продукте с самого начала: подсказки выдаются по
одной и объясняют приём на других числах, «Показать решение» подставляет
рабочий код за одну звезду вместо трёх. **Но никто про этот выход ребёнку не
говорил.** Неудачные попытки нигде не считались: на пятой попытке экран
отвечал ровно то же, что на двадцать пятой, — «Ещё не то». Обе кнопки стоят в
углу с первой секунды и читаются как «сдаться». Десятилетний их не нажмёт — он
ткнёт «Проверить» ещё двадцать раз и закроет вкладку.

Три ступени, каждая называет СЛЕДУЮЩИЙ шаг:

| Когда | Что говорит |
|---|---|
| 4-я попытка, подсказки не брал | «Тут застревают многие» + кнопка «💡 Взять подсказку» |
| 8-я попытка или подсказки кончились | «Открыть решение — это не проигрыш» + кнопка «Показать решение» |
| решение открыто, 14-я попытка | «Сегодня этот урок не идёт — так бывает» + «Отложить» |

⚠️ Четыре правила, без которых лестница превращается в укор:

1. **Виноват урок, а не ребёнок.** «Тут застревают многие», «это про урок, а
   не про тебя» — и ни одного «ты не справился». Виноватый не возвращается;
   то же правило, что у напоминания молчащему.
2. **Ничего не запирается и не дорожает.** Кнопка ступени не делает ничего
   своего — она нажимает ТУ ЖЕ кнопку урока, что стоит в углу. Значит цена
   подсказки ровно та же, что была, и разойтись эти два пути не могут.
3. **Каждая ступень называет следующий шаг, а не сделанный.** «Возьми
   подсказку» тому, кто взял их все, читается как «плохо старался» — тот же
   промах, что и в совете взрослому. После взятой подсказки даётся два хода
   тишины, иначе лестница дёргает за рукав.
4. **Раньше четвёртой попытки лестницы нет.** Одна-три попытки — нормальный
   ход работы, и лезть туда с утешением значит мешать.

⚠️ **Считается по ТЕКУЩЕМУ заходу (`session`), а не по журналу.** Журнал копит
попытки за все дни, и ребёнок, вернувшийся назавтра со свежей головой, получил
бы «отложи и вернись» на первом же нажатии. При этом сама `stuckStep` ничего
из `session` не берёт — всё приходит доводами, чтобы тест видел её целиком.

⚠️ **Путей неудачи в `runCheck` пять**: «Почти» (нет нужной конструкции),
«Так нельзя» (запрещённая конструкция), ошибка Python, «Ещё не то» и «работает,
но это не починка». Ребёнку всё равно, на каком из них он застрял, поэтому все
пять заведены в один локальный `fail()`, и лестница приходит на любом. Иначе
она появлялась бы ровно там, где мы про неё вспомнили.

Тесты: раздел 10б2 в `tests/full-run.js` — таблица ступеней целиком, все четыре
правила словами, и вживую на уроке с проверкой по коду: ступень не приходит
раньше четвёртой попытки, приходит на ветке «Почти», а её кнопка списывает
подсказку ровно как обычная (журнал +1, не +2). Проверено мутацией: пять
поломок — пять падений.

Осталось из того же разбора: **В** — счёт затыков по урокам в `op=stats`,
детектор НАШИХ ошибок в уроках.

**⚠️ Попутно: 41 ложный провал теста, которого не было.** Прогоняя набор, я
получил `Найдено проблем: 41` в `tests/homework.js` — движок и python3 печатали
разное на сорока задачах сразу. Выглядело как крупная поломка движка. На самом
деле в тот момент шли ДВА `npm test` одновременно (мой и фоновый), а
`tests/homework.js` держал два общих места на всю машину:

- файл эталона лежал под постоянным именем `kodokvest-hw-check.py` в общей
  временной папке. Соседний прогон перетирал его между `writeFileSync` и
  `execFileSync`: движок печатал свою задачу, а python3 — соседнюю;
- задача `hw-file` пишет и читает `scores.txt`, а рабочей папкой был КОРЕНЬ
  РЕПОЗИТОРИЯ. Два прогона писали в один файл, и каждый прогон мусорил в
  проект — следы этого до сих пор лежат в `.gitignore`: `a.txt`, `t.csv`,
  `scores.txt`.

Починено так же, как это давно сделано в `tests/engine-vs-python.js` и
`tests/content-vs-python.js`: своя папка через `mkdtempSync`, python3
запускается из неё, папка уносится в конце. Проверено: четыре одновременных
прогона — все зелёные, в репозитории после них пусто.

⚠️ **Урок: расхождение сразу в сорока местах — это почти наверняка не сорок
ошибок, а одна общая.** Прежде чем чинить движок, посмотри, что у прогонов
общего. И: `tests/homework.js` был единственным из трёх python-тестов, где
временную папку не завели, — то есть образец в проекте уже был, просто одно
место до него не дошло.

**Застрявший ребёнок стал виден взрослому (1.90.0).** Пункт А из разбора
вопроса фаундера 07.09.2026: «что делать, когда ребёнок уперся в урок и никак
не может его решить, либо у нас может быть ошибка».

Разбор показал, что выход у ребёнка есть — подсказки по одной и «Показать
решение» на одну звезду вместо трёх, — **но взрослый о затыке не узнавал
никак**. И `weekFacts` (недельный отчёт), и `groupRow` (строка группы) считали
ТОЛЬКО решённые уроки: у обоих стояло «нет `solvedAt` — пропустить». Отсюда
три следствия:

- в отчёте родителю про урок, на котором ребёнок сидит третий день, не было
  ни слова;
- наставник видел «тяжело шёл урок» только про уроки, которые ребёнок
  всё-таки взял, то есть про уже случившийся успех;
- ⚠️ и хуже всего: если остальные уроки недели прошли чисто, отчёт печатал
  «Все уроки недели прошли без подсказок и без подглядывания в решение» — про
  неделю, просиженную в тупике. Формально правда, по смыслу ложь.

При этом на вывеске обещано прямо: «отчёт за неделю — ... где застрял».
Обещание не выполнялось.

Данные лежали всё это время: попытки, время и последнее касание пишутся и по
нерешённым урокам. Не хватало того, чтобы их кто-то прочитал.

Что появилось:

- **`stuckIn(st)`** — нерешённые уроки с попытками, отсортированные по цене.
  Работает на любом снимке. Порог `STUCK_PRICE = 6`, а не 4, как у «тяжело
  далось»: цена сданного урока окончательная, а цена урока, который ребёнок
  сейчас проходит, ещё растёт, и четыре попытки — это нормальный ход работы.
  Шесть — это шесть попыток подряд, или четыре плюс подсказка, или открытое
  решение, после которого урок так и не сдан.
- **⚠️ Окна в неделю у затыка НЕТ, и это не забывчивость.** Курс — цепь:
  следующий урок открывается только сданным предыдущим. Значит нерешённый
  урок с попытками — не «что было в среду», а место, где ребёнок стоит прямо
  сейчас, сколько бы времени ни прошло. Такой факт не устаревает.
- **`stuckAdvice(log)`** — что делать, по тому, что ребёнок УЖЕ пробовал. Три
  ветки. Один совет на все случаи не годится: «возьмите подсказку» ребёнку,
  который взял их все, читается как «плохо старался», и взрослый после такого
  совета перестаёт верить отчёту целиком.
- Затык виден в трёх местах: строкой в отчёте родителю, блоком `.wrstuck` в
  карточке недели (ВЫШЕ «тяжело далось» и «дальше по программе») и пометкой
  `stuck` в группе. В группе он же поднимает строку: молчание 100, затык 40 —
  этот ребёнок ещё ходит, но стоит на месте, и если не поговорить, он станет
  следующим молчащим.
- Починена строка «все уроки недели прошли без подсказок»: теперь она требует
  ещё и отсутствия затыка. Хорошая новость не печатается поверх плохой.
- **Курс — цепь, поэтому застрявший урок И ЕСТЬ следующий по программе.**
  Отчёт называл его двумя абзацами с двумя разными объяснениями; теперь во
  второй раз это «Дальше по программе — тот же урок N «…»».

Попутная уборка: формула цены урока (`lessonPrice`) была списана трижды —
в `weekFacts`, в `groupRow` и в новом коде. Сведена в одну функцию.

Тесты: раздел «где застрял» внутри 10б в `tests/full-run.js` — порог в обе
стороны, сданный урок не затык, урок без попыток не затык, затык месячной
давности не теряется, отчёт называет беду И выход, совет зависит от
состояния, хорошая новость не печатается поверх плохой, порядок блоков в
карточке, пометка и вес в группе. Проверено мутацией: девять поломок — девять
падений.

⚠️ Осталось из того же разбора: **Б** — лестница выхода в самом уроке (сейчас
ничто не считает неудачные попытки: на пятой и на двадцать пятой экран говорит
одно и то же «Ещё не то», а кнопка «Показать решение» стоит с первой секунды и
выглядит как «сдаться»); и **В** — счёт затыков по урокам в `op=stats`, чтобы
видеть, когда ошибка НАША, а не детская.

**Выровнены колонки в «Угадай вывод» на вывеске (1.89.1).** Жалоба фаундера
07.09.2026: «не симметрично расположены блоки, один выше другого на всех трёх
задачах».

Причина оказалась структурной, а не в отступах. Справа над полем ответа стояла
подпись «Что напечатает программа?», слева над кодом — ничего. Обе колонки
начинались на одной высоте честно (`align-items:start` работал), но глаз
сравнивает не колонки, а РАМКИ: рамка кода вставала вровень с текстом правой
подписи, то есть на 40 пикселей выше поля ответа. Две одинаковые с виду
коробки стояли рядом со сдвигом.

Замер до правки (ширина 1400): верх колонок 3634.1 у обеих, а верх рамки кода
3634.1 против верха поля ответа 3674.5.

Починка: у левой колонки появилась своя подпись «Программа», и обе колонки
теперь устроены одинаково — подпись, под ней коробка (`.warmcode` рядом с
`.warmans`, подписи набраны одним правилом). После правки верх обеих рамок
совпадает до десятой пикселя на всех трёх задачах.

Высоты коробок остались разными и это нормально: в них разное содержимое, и
подгонять их друг под друга значило бы раздувать поле ответа под длину чужой
программы.

⚠️ Урок: **симметрию ломает не отступ, а разное УСТРОЙСТВО колонок.** Пока у
одной колонки есть подпись, а у другой нет, никакое выравнивание по верху не
поможет — сравнивают рамки, а не контейнеры.

Тест: раздел 11б2 в `tests/full-run.js`. Высоту в jsdom не измерить, поэтому
стережётся сама симметрия: у обеих колонок есть подпись, она первая в колонке,
и набраны они одним правилом CSS. Проверено мутацией: три поломки — три
падения.

**План и факт в группе (1.89.0).** Пункт 3.5 из корзины «репетитор и
родитель». Наставник, открывая группу, спрашивает про ученика не «сколько
пройдено», а «успевает ли», — а группа отвечала только числом сданных уроков.
Двадцать четыре урока за месяц по три занятия в неделю это впереди плана, а за
полгода — далеко позади, и по самому числу их не различить.

Что появилось:

- **`planFact(st)`** — расстояние до рамки, посчитанное по ЛЮБОМУ снимку.
  Точка отсчёта — день, когда рамку сохранили (`frame.setAt`); план — учебные
  дни от неё до сегодня (дни недели минус каникулы) на уроки в занятие (по
  длине, а если замер темпа принят — по замеру этого ребёнка); факт — уроки,
  сданные после той же точки. Обе половины от одного дня: уроки, пройденные
  до рамки, к её плану отношения не имеют.
- **Строка в каждой строке группы** и счёт в сводке «за неделю»: сколько из
  группы идут по рамке или впереди и у скольких рамки нет вовсе.
- **Блок «План и факт» в редакторе рамки** — то есть и в кабинете взрослого
  на устройстве ребёнка, и в карточке ученика у наставника: разметка у них
  общая.

⚠️ Три ограничения, каждое из красной линии «это не табель»:

1. **Допуск — одно занятие.** Разница меньше него это «идёт по плану», а не
   «отстаёт на один урок»: точность там мнимая, а обвинение настоящее.
2. **«Впереди» видно ровно так же, как «отстаёт»** — тем же размером, своим
   цветом. Колонка, у которой заметна только плохая половина, и есть табель.
   По той же причине план — ОТДЕЛЬНАЯ строка, а не пометка в списке «на кого
   посмотреть»: там он читался бы как обвинение.
3. **Без рамки план не выдумывается** по среднему темпу. Экран говорит, что
   рамки нет, и предлагает её поставить. Придуманный план обвинял бы ребёнка
   за нашу догадку.

Плюс потолок: спланировать больше, чем осталось в курсе, нельзя — иначе
дошедший до последнего урока вечно «отстаёт» от плана, который некуда
выполнять.

**⚠️ Попутно найдены и починены две ошибки в карточке ученика у наставника —
место, не покрытое ни одним тестом.** Обе в одной строке `kidRender`:

- **Экран падал.** `frameEditorHTML(f, pace)` звали как `frameEditorHTML(frame())`,
  без второго довода. Пока у выбранного ученика не стояла дата «успеть к»,
  это сходило с рук; стоило её поставить — `pace.none` у неопределённого
  `pace`, и карточка ученика переставала рисоваться. Теперь темп считается
  внутри редактора, и лишнего довода у функции нет вовсе.
- **Обещали чужую дату.** `paceCheck` считал остаток уроков через
  `lessonsLeft()`, а тот смотрел в `S` — то есть в прогресс САМОГО
  НАСТАВНИКА. Наставник, прошедший курс сам, видел бы у любого ученика
  «осталось 0 уроков». Теперь обе функции принимают снимок.

Урок общий: **функция с необязательным доводом, от которого зависит показ,
однажды будет вызвана без него.** Считать надо там, где считается, а не
надеяться, что довод передадут.

Тесты: раздел 10.5 в `tests/full-run.js` — арифметика плана (уроков в занятие,
учебные дни, потолок по остатку курса, каникулы), допуск в одно занятие в обе
стороны, уроки до рамки не в факте, слова строки (говорит «от рамки», не
процентами, без приговора), экран группы. Отдельным куском — обе починенные
ошибки. Проверено мутацией: семь поломок, семь падений.

**Огонёк стал уговором (1.88.0).** Пункт 4.4, и это единственный пункт плана,
который не добавляет, а УБИРАЕТ. Красная линия продукта звучит «страх потерять
серию» — а нарушал её сам продукт, собственной шапкой.

Что убрано у ребёнка:

- **число серии из шапки** — вместо «🔥 12» всегда «🔥 Сегодня»; остались
  честные состояния значка: горит (сегодня занимался) и звенит (сегодня
  условленный день);
- **кнопка «N дней подряд» на Главном** — теперь просто «🔥 Сегодня»;
- **число серии и рекорд на экране «Сегодня»** — вместо них уговор: сколько
  дней в неделю условлено и что с сегодняшним днём;
- **блок «Щиты»** и плашка «🛡️ Щит закрыл пропуск — серия не оборвалась»;
- **строка «🔥 N дней подряд» на победе разминки**;
- ⚠️ и фраза из лида, которая была честной формулировкой красной линии, только
  с той стороны, с которой её быть не должно: «одна маленькая задача в день и
  серия, которую жалко прерывать».

Что осталось: **календарь занятий** (единственная честная вещь во всём блоке —
показывает, как было, и ничего не требует) и **сам механизм**. Серия и щиты
считаются по-прежнему: щит молча закрывает пропуск, бейджи за длинные серии
выдаются. ⚠️ Ребёнку про это не говорят, потому что делать ему с этим нечего:
тратить щит руками нельзя.

⚠️ **Два теста пришлось ПЕРЕВЕРНУТЬ, а не удалить.** Они стерегли прежнее
решение: «на экране «Сегодня» есть блок про щиты» и «в подсказке назван срок
появления щита». Теперь они требуют обратного и вдобавок проверяют, что не
вернулось число серии. Тест, который просто выключили бы, — это дыра.

**Мобильная версия: демо-редактор растягивался на две тысячи пикселей.**
Жалоба фаундера: «на мобильной экран убегает вниз на много слайдов».
Найдено замером: редактор выравнивает номера строк с переносами, проставляя
каждому номеру вычисленную высоту, и вне экрана урока расчёт даёт чушь —
номерам доставались высоты **916 и 1019 пикселей**. Колонка номеров на вывеске
спрятана: шесть строк примера в нумерации не нуждаются. Окно демо 2521 → 663,
страница короче на 1857 пикселей. Стережёт тест `[вывеска]`, проверен мутацией.

**Три находки фаундера на витрине и настоящие значения в карточке (1.87.0).**

1. ⚠️ **«Почему так?» — код начинался ниже вывода, а под ним висела пустота.**
   Причина стыдная и найдена замером, а не глазами: я назвал класс-модификатор
   `wrap`, а `.wrap` в проекте — **класс страницы** (`<div class="wrap" id="app">`)
   с отступами `26px 18px 90px`. Панель кода тихо получила отступы страницы.
   Переименован в `flow`, тест теперь прямо запрещает класс `wrap` на этой
   панели. Второе столкновение имён за день, теперь в CSS: первое было
   `workLink` в JS.
2. ⚠️ **«Меняю цифру — ничего не происходит».** Интерактив работал: замерил,
   `#######...` превращалось в `#######.............`. Но изменившаяся строка
   легко оказывается ниже края панели, и на экране действительно не менялось
   НИЧЕГО. Добавлена вспышка панели вывода при пересчёте — единственный знак,
   который виден независимо от того, где поменялся вывод. Плюс поля с числами
   получили заметный фон.
   ⚠️ Тот же урок, что с «Запустить заново»: работающий код и видимая работа —
   разные вещи, и для человека вторая важнее.
3. **«Нужно выводить реальные значения, а не прятать их за #».** Справедливо:
   в «Карточке героя» родитель видел `Сила #######...` и не знал числа.
   Теперь `Сила       7 #######...` — и число, и полоска. Полоску не убирал:
   она и есть урок про умножение строки на число. Изменены шаги 2–4 проекта
   `project-w1`, их тексты и подсказки; выравнивание числа до двух знаков
   вынесено в отдельную подсказку, иначе у однозначных и двузначных полоски
   разъезжаются.

**Свой проект с именем (1.86.0).** Пункт 2.4. Из песочницы уходило всё, кроме
рисунков: написал программу, ушёл с экрана — и её нет. Рисунок сохранялся, а
считалка или угадайка — нет, потому что черепашка в них не участвовала.

- **Кнопка «🛠 Назвать и сохранить в „Моё“»** в песочнице, для ЛЮБОГО движка
  (в отличие от галереи: та про рисунки). Форма из двух полей — название и
  «что она делает» — прячется до нажатия: два пустых поля под редактором
  выглядели бы обязательными, а называть программу никто не обязан.
- ⚠️ **Описание не для нас**: его читает тот, кому отправят ссылку. Он
  открывает программу, не видя кода, и ему надо понять, что делать.
- **Сохранить сломанное нельзя**: перед формой программа прогоняется, и при
  ошибке сохранение отклоняется. Ссылка на падающую программу — это подарок,
  который не открывается, и стыдно за него будет ребёнку.
- **Раздел «Мои программы» в «Моём»**, перед рисунками: рисунок мы называем
  сами, а тут имя дал ребёнок, и своё имя важнее нашего. Три действия:
  ссылка, «в песочницу», удалить (с подтверждением).
- **Ссылка — `#play=`**, как у игр: друг открывает и ЗАПУСКАЕТ, код за кнопкой
  «Заглянуть в код». Существующий `#work=` тут не подходит: он показывает
  код и не даёт запустить, а названную программу делают, чтобы ею
  пользовались.

⚠️ **Две настоящие ошибки, пойманные по дороге, и обе молчаливые.**

1. **Столкновение имён.** Я объявил `workLink`, не заметив, что такая функция
   уже есть (ссылка «поделиться работой», `#work=`). JavaScript на это не
   ругается: побеждает объявленная позже, и мой код молча начал звать чужую
   функцию — ссылка собиралась, кнопка нажималась, ничего не падало, а
   работало не то. Свои функции переименованы в `myWork*` (рядом с уже
   существующими `myTasks*`), **и добавлена проверка `[имена]`**: в
   `js/app.js` не должно быть двух функций с одним именем.
2. **id из одних миллисекунд.** `"w" + Date.now()` — два сохранения подряд
   попадают в одну миллисекунду, получают один id, и второе затирает первое.
   Увидел глазами: сохранил две программы, в «Моём» осталась одна. Тест этого
   не видел, потому что проверял «не больше предела» — а это выполняется и у
   одной записи. Проверка переписана на точное число.

**Выпускной мира: что ты теперь умеешь (1.85.0).** Пункт 2.2. Новый экран
`screenWorldDone(n)`. Открывается двумя дорогами: кнопкой «🎓 Что я теперь
умею» на победной карточке собранного проекта мира и нажатием на выпущенную
веху в карте пути.

- ⚠️ **Умения написаны ГЛАГОЛАМИ от первого лица**, а не темами программы.
  «Списки, словари, множества» — это оглавление учебника, оно не говорит
  ребёнку ничего про него самого. «Считаю, сколько раз встретилось слово» —
  говорит. Данные — `WORLD_SKILLS` в `js/app.js`, 6–10 умений на мир.
- **У каждого умения стоит id урока, где оно появилось**, и строчка кликабельна:
  список превращается из похвалы в оглавление собственной памяти. Порядок —
  по номерам уроков (`worldSkills` сортирует), чтобы читалось как пройденная
  дорога.
- **«Показать» — вторая половина пункта.** Кнопка «🖨 Показать лист» открывает
  уже существующий сертификат мира: имя, мир, дата, печатается и сохраняется
  в PDF. Ничего нового изобретать не пришлось.
- **Выпускной наступает по тому же правилу, что сертификат** (`certWorldReady`):
  все уроки мира пройдены И проект собран. Раньше времени не поздравляет —
  проверено тестом на трёх состояниях.
- ⚠️ **Тест поймал меня на кириллице.** Первая версия проверки «умение написано
  глаголом» использовала `\b` — а в JS граница слова считает буквой только
  латиницу, и на кириллице молча не срабатывает. Проверка переписана на
  окончание первого слова (-ю/-у), в ней об этом написано.
- Тест `[выпускной]`: у каждого мира ≥5 умений, каждое ведёт в существующий
  урок СВОЕГО мира и начинается с глагола; экран рисует все строчки, строчка
  открывает свой урок, «показать лист» открывает лист, процентов нет.
  Проверен мутацией.

**Витрина проектов: код слева, живые числа, мёртвой кнопки нет (1.84.0, второй заход).**
Разговор с фаундером в тот же вечер, три правки подряд и одна снятая кнопка.

- ⚠️ **«Зачем здесь кнопка „Запустить заново“?»** Правильный вопрос, и ответ —
  низачем. Программа детерминированная, нажатие ничего не меняло; как
  доказательство «это не картинка» она не работала. Сначала я заставил её
  ВЫГЛЯДЕТЬ живой («выполняется…») — то есть отполировал кнопку, которой не
  должно быть. Убрана. Урок на будущее: прежде чем чинить кнопку, спросить,
  зачем она.
- **Колонки переставлены: слева код, справа вывод.** Читают слева направо,
  и причина обязана идти раньше следствия.
- **Числа в коде стали живыми** — идея фаундера. Не весь код редактируемый, а
  именно числа: проект это 65–96 строк, править их целиком на вывеске никто не
  станет, а ткнуть в число и увидеть другой результат — станет. Сломать
  синтаксис так нельзя: куски между числами неизменны. Появляется кнопка
  «↺ Вернуть числа как были» — но только после правки, поэтому мёртвой не
  бывает.
- Числа ищутся ТЕМ ЖЕ выражением, что и подсветка (`HLRE`, группа 3), и по
  сырому коду: иначе цифра внутри строки стала бы полем ввода, хотя в коде это
  текст.
- Тест `[витрина]`: порядок колонок, отсутствие кнопки запуска, живые числа
  меняют вывод, «вернуть» возвращает, перенос строк. Проверен мутацией.

**Пять находок фаундера на живом сайте (1.84.0).** Все пять — с его экрана,
не из головы, и четыре из них оказались настоящими дефектами.

1. **Логотип в кабинете не делал ничего.** Он вёл «домой по роли», а дом
   наставника — тот же кабинет, где тот уже стоял. Выход на общую страницу
   оставался только ссылкой в самом низу («О тренажёре»), до которой мало кто
   долистывает. Теперь логотип зовёт `goLogo`: взрослого — на страницу сайта,
   ребёнка — как раньше, на его главную. Кабинет никуда не делся, он остался
   кнопкой в шапке справа.
2. **Взрослый проваливался в детский тренажёр.** С вывески «Посмотреть первый
   урок» открывало урок, детская навигация подменяла взрослую, и дороги в
   кабинет из урока не было вовсе. Урок ему по-прежнему открыт — наставнику
   полезно видеть ровно то, что видит ребёнок, — но теперь сверху стоит
   полоска «вы смотрите глазами ребёнка» с кнопкой возврата.
3. **Код витрины не влезал в окно.** Строка уезжала вправо под полосу
   прокрутки, которую в macOS не видно, и родитель читал обрубок. Убрана
   колонка номеров (она мешала переносу и всё равно дублировала заголовок
   «96 строк»), длинные строки переносятся.
4. **«Запустить заново» выглядела мёртвой.** Работала честно, но программа
   детерминированная: вывод каждый раз тот же, и на экране не менялось ничего.
   Теперь панель гаснет, показывает «выполняется…» и печатает результат.
   ⚠️ Кнопка, по которой не видно, что она сработала, — сломанная кнопка,
   даже если код за ней исправен.
5. **Идея фаундера, и она принята: код на вывеске стал редактируемым.**
   «Было бы интересно, если бы люди могли поменять что-то в коде и от этого
   менялся вывод». Раньше посетитель смотрел на доказательство, теперь
   проверяет его руками: поменял `звёзд = 5` на `8` — лесенка выросла.
   Редактор взят НАСТОЯЩИЙ (`makeEditor`), тот же, что откроется ребёнку:
   родитель, потрогавший его на вывеске, узнаёт то же окно потом.

⚠️ **Попутно найдена и снята гонка в `landRun`.** Вывод набирается по строке в
такт таймеру, и второй запуск поверх первого не останавливал старый таймер —
тот продолжал дописывать своё поверх нового результата. Раньше ловилось только
двойным нажатием, а с редактируемым кодом запуск идёт после каждой правки, то
есть постоянно.

**Карта пути: сто уроков одной дорогой (1.83.0).** Пункт 2.1. Новый экран
`screenPath`, вход — кнопка «🗺 Показать всю дорогу» в блоке «Уроки» на Главном.

- **Что показывает:** ряд кружков на каждый мир (номер урока внутри мира),
  веха-проект в конце ряда, флажок «ты здесь» на следующем уроке и строка
  «до вехи N уроков, примерно K занятий».
- ⚠️ **Расстояние меряется занятиями, а не процентами и не днями.** Проценты
  ребёнку ничего не говорят, дни мы обещать не вправе. Число считается по его
  собственному темпу (`frame().perLesson`), тем же способом, что `worldCountdown`
  на Главном. Тест запрещает появление процентов на этом экране.
- **Почему отдельным экраном, а не на Главном:** пять карточек миров отвечают
  «что внутри», карта — «где я и сколько до вехи». Сто кружков на Главном
  удлинили бы его вдвое, а правило «страница не должна быть вертикально
  длинной» стоит выше желания всё показать сразу.
- ⚠️ **Карта вскрыла, что легенда врала.** Первый урок КАЖДОГО мира открыт
  сразу (`lessonOpen`: `if (i === 0) return true`) — на карте это стало видно
  пятью светлыми кружками. Первая версия подписи говорила «уроки открываются
  по одному», и это было неправдой. Подпись и подсказка переписаны.
- ⚠️ **Найдено попутно и НЕ исправлено: `behavior:"smooth"` молча не работает**
  во встроенной панели предпросмотра — ни в `scrollIntoView`, ни в
  `window.scrollTo`. Не исключение, не ошибка: просто ничего не происходит
  (`prefers-reduced-motion` при этом выключен). В `js/app.js` от него зависят
  **52 места** — то есть прокрутка наверх при смене экрана. В обычных Chrome и
  Safari smooth работает, поэтому переписывать 52 вызова по одному
  наблюдению я не стал: это решение фаундера. Своя кнопка «Показать, где я»
  подстрахована — если через 400 мс кружок не попал в окно, прокрутка
  доводится без анимации.
- **Тест `[карта]`** в `tests/full-run.js`: сто кружков, пять вех, «ты здесь»
  ровно одно и на том уроке, который откроет «Продолжить», закрытый кружок не
  открывается в обход, веха закрытого мира заперта, процентов нет. Проверен
  мутацией — падает.

**Домашка накрыла весь курс (1.82.0).** Пункт 1.4б. Банк вырос с 33 задач до
44, и перекос снят: было 13 / 6 / 3 / 8 / 3 по мирам, стало **13 / 6 / 8 / 11 / 6**.

⚠️ **Заметка в задачах устарела**: там написано «сейчас банк кроет миры 1–3»,
а на деле у миров 4–5 уже было одиннадцать задач — их добавила соседняя сессия.
Реальной дырой оказался **мир 3**: три задачи на двадцать уроков. То есть
ребёнок, дошедший до классов, файлов и API, получал домашку про сложение.

Добавлено одиннадцать близнецов:

| Мир | Задачи |
|---|---|
| 3 | два ответа сразу (`fn-multi`), не всё в списке число (`try-except`), счётчик классом (`class-methods`), щенок унаследовал (`inherit`), скидка по умолчанию (`fn-default`) |
| 4 | когда каникулы (`datetime`), числа из текста (`regex`), записал и прочитал (`files-write`) |
| 5 | комнаты по кругу (`quest-2`), ответ сервера (`api-2`), столбики без картинки (`data-4`) |

- **Все прогнаны на двенадцати семенах и сверены с настоящим python3** —
  528 прогонов, 132 сверки. У каждой есть `need`: обход «напечатать ответ
  числом» отбивается, проверено живьём на задаче про файлы.
- **Файлы в домашке работают**, хотя `hwRunner` не даёт диска: движок держит
  файловую систему в памяти, поэтому «записать и прочитать обратно» проходит
  в одной программе. Это и проверяется.
- ⚠️ **Поймал у себя кривой русский на числах**: «гавкает 5 раза» и «4 тысяч
  шагов». Числа приходят из семени, поэтому склонение обязано считаться —
  в файле для этого давно лежит `ru(n, one, few, many)`, я его сначала не
  применил. Теперь применён и в условии, и в эталонном коде (там та же фраза).
- ⚠️ **Близнец есть НЕ у каждого урока, и это намеренно.** `git`, `pip и
  окружения`, «Как искать ответы», выпускной и проекты вычислимого ответа не
  имеют — судить там нечего. Слабее всех теперь **мир 2** (6 задач на 20
  уроков): если решим добирать дальше, начинать оттуда.

**Мир «Экзамен» закрыт по типам, а не по счёту (1.81.0).** Пункт 1.1б просил
«до ~40 задач с разборами». Задач к началу работы было уже 44 — то есть счёт
закрыт, — поэтому смотрел не на число, а на то, каких ТИПОВ экзамена нет ни
одной задачи. Нашлось восемь дыр, все закрыты; задач стало 52.

| Тема | Задача | Что за тип |
|---|---|---|
| 📦 Кодирование | Код без путаницы | условие Фано, префиксные коды |
| 📦 Кодирование | Сколько качать | скорость передачи, килобайты против килобит |
| 🤖 Исполнители | Исполнитель Редактор | замена по одному вхождению до упора |
| 🤖 Исполнители | Автомат с проверочным битом | достройка двоичной записи по правилу |
| 🤖 Исполнители | Робот на поле | число путей с препятствиями |
| 🌳 Рекурсия и перебор | Рекурсивная функция | F(n) через F(n−1) |
| 🌳 Рекурсия и перебор | Сколько получится слов | счёт вариантов без перебора |
| 🌳 Рекурсия и перебор | Кто выиграет | выигрышная позиция, счёт с конца |

- **Две новые группы** в `ALGO_GROUPS`: «Исполнители» и «Рекурсия и перебор».
  Всё новое лежит в `js/algo-exam.js` — там же, где кодирование, логика и графы,
  и по той же причине, что записана в шапке файла.
- ⚠️ **Граница обещания на экране обновлена** (`screenAlgo`): теперь она
  называет и новые темы. Что НЕ закрываем — прежнее и единственное:
  **электронные таблицы**, их проверять нечем.
- **Все восемь эталонов сверены с python3** (сверок 928 → 975, наборов
  экзамена 129 → 176) и **прогнаны живьём в браузере**: каждый засчитан судьёй.
  Отдельно проверил обход — на «Кто выиграет» отправил `print("ВТОРОЙ")`
  константой, скрытые наборы её отбили.
- ⚠️ **Тест поймал заготовку «Кода без путаницы»**: она печатала YES, а верный
  ответ на открытом примере тоже YES — то есть задание сдавалось само собой.
  Заготовка теперь печатает NO.
- **Чего сознательно не добавлял:** электронные таблицы (нечем проверять,
  README и экран об этом говорят прямо) и маски подсетей — тема есть на ЕГЭ, но
  она про сети, а не про программу, и наш судья к ней ничего не добавляет.

**Пять игр-проектов: по одной на каждый мир (1.80.0).** Пункт 1.2 плана закрыт
целиком. К «Виселице» добавились четыре игры, и каждая открывается собранным
проектом своего мира — финал мира открывает следующую игру.

| Игра | Мир | Открывает | Чем берёт |
|---|---|---|---|
| ❓ Своя викторина | 2 | «Турнирная таблица» | список словарей, `enumerate`, счёт |
| 🚢 Морской бой | 3 | «Рюкзак героя» | двумерное поле, функция-выстрел с `return` |
| 🐣 Тамагочи | 4 | «Чёрный ящик» | класс с состоянием, время работает против игрока |
| 🧭 Лабиринт | 5 | «Свой сайт» | карта строками, словарь направлений, `continue` |

- **Каркас не трогал**: `kind:"game"`, `needs`, `step.stdin`, финал в игровом
  режиме и `#play=` — всё это уже стояло с 1.62.0 и подошло без правок.
- **Каждая игра — 4 шага, все 20 программ сверены с настоящим python3.**
  Проверка `content-vs-python` выросла с 908 до 928 сверок.
- ⚠️ **Линтер поймал меня на своём же правиле.** Поле морского боя я сначала
  выписал четырьмя одинаковыми строками — и `npm run audit` показал ровно тот
  совет, которым тренажёр ругает ребёнка: «Одна и та же строка 4 раза».
  Переделал на включение из переменной `размер`: поменяешь одно число — поле
  станет больше. Это тот же приём, что `len(секрет)` в «Виселице», и правило
  «не выписывай руками то, что программа посчитает» теперь держится и здесь.
- **Секрет по-прежнему лежит в переменной, без `random`** — сознательно, как и
  в «Виселице»: игра остаётся проверяемой, а перед отправкой другу секрет
  меняют, и это первое, что делают все.
- **Проверено живьём**: игра собрана по шагам в браузере, финал сыгран
  (четыре выстрела, «Все корабли потоплены! Выстрелов: 4»), и все пять игр
  проходят круг «упаковал в ссылку → распаковал» знак в знак. У лабиринта в
  коде 18 решёток — в адресе это значащий знак, поэтому круг проверял именно
  на нём.

**Настоящий Python по кнопке — только в песочнице (1.79.0).** Тяжёлый контур
из правила 1.78.0 построен: дверь, которую слой `Runtime` держал открытой с
самого начала, открыта ровно на ширину песочницы.

- **Что появилось.** Адаптер `pyodide` в `js/runtime.js` (был заглушкой) и
  карточка «движок» в `screenSandbox`. Курс не тронут вовсе: уроки, экзамен,
  разминки и все сверки с `python3` были и остаются на `mini`.
- **Файлы свои**, `vendor/pyodide/` — решение фаундера 06.09.2026 против CDN.
  Версия **314.0.6, то есть CPython 3.14** — та же ветка, что у `python3`,
  которым сверяется курс (3.14.5 на машине фаундера). Замерено: 11,7 МБ на
  диске, около 5 МБ в гите, около 6 МБ по сети (GitHub Pages жмёт даже
  `.wasm` — проверено на живом чужом сайте, не предположено).
  ⚠️ Версию выбирали ОДИН раз и сознательно: гит не забывает, и переложить
  ядро позже стоило бы ещё пяти мегабайт в истории навсегда. Главный довод
  оказался не про Россию, а про офлайн: `sw.js` кэширует свой домен и не
  трогает чужие адреса, поэтому **офлайн после загрузки получился сам собой** —
  проверено на выключенном сервере.
- **Гейт приёмки пройден живьём**, все четыре шага: до нажатия не уходит ни
  одного запроса; по кнопке скачалось и заработало (CPython 3.14.2); сервер
  выключен — по-прежнему работает; урок открыт — он на `mini` (у него есть
  кнопка «Шаг», а её даёт только мини-движок).
- ⚠️ **Инвариант стережёт тест**, а не намерение: `[два контура]` в
  `tests/full-run.js` роняет сборку, если тяжёлый движок попал в `SHELL`
  `sw.js`, в `build.js`, в `<script>` на странице или если один файл перевалил
  за 3 МБ. Проверено мутацией: сломал инвариант — тест упал.
- **Чего в тяжёлом движке нет и это сказано на карточке:** черепашки и
  пошагового разбора. `numpy`/`pandas` не докачаны — скачано ядро.
- **Ошибки переводятся**, а не показываются трейсбеком: `PY_RU` в
  `js/runtime.js` даёт русское объяснение и номер строки в программе ребёнка.
  Бесконечный цикл ловится по времени (10 с) тем же текстом, что у мини-движка,
  иначе `while True:` вешал бы вкладку вместе с написанным кодом.
- ⚠️ **На вывеске про это по-прежнему ни слова.** Правило 3.7б не изменилось:
  продавать «настоящий Python внутри» — значит намекать, что до этого был
  ненастоящий. Кнопка живёт в песочнице и находится тем, кто до неё дорос.

**Два контура и довод про python3 на вывеске (1.78.0).** Правило «лёгкий
офлайн по умолчанию, тяжёлое по требованию» записано в
`docs/golos-i-produkty-2026-09-06.md` §§ 3.7–3.7б. Оно сводит в одно три
решения, которые до этого принимались порознь: Pyodide (02.09), голосовой ввод
(03.09), Whisper (06.09).

- ⚠️ **Курсу тяжёлый контур не нужен и не будет нужен**: все сто уроков и все
  новые задачи экзамена идут на мини-движке. Осмыслен он только в
  **песочнице**, где ребёнок упирается в отсутствие numpy/pandas/matplotlib.
  Там кнопка «включить настоящий Python» узкая, не трогает курс и не удваивает
  проверки.
- ⚠️ **Урок про формулировки.** Первая версия строки для вывески звучала как
  «внутри свой Python, но без numpy и pandas». Фаундер отклонил, и правильно:
  это продажа недостатка, которого покупатель не почувствует — ни в школе, ни
  в ЕГЭ этих библиотек нет вовсе. На вывеске теперь стоит довод, а не оговорка:
  **каждый пример курса при каждой сборке прогоняется через настоящий python3
  и сверяется знак в знак**. Числа сверок в тексте нет намеренно — оно росло бы
  и врало.
- Замер доступности сделан: `cdn.jsdelivr.net` отдаёт Pyodide 0.26.4, ядро
  `pyodide.asm.wasm` 3,8 МБ, частичная загрузка работает. Развилка «CDN или
  своя копия в репозитории (~13 МБ)» решена 06.09.2026 в пользу своей копии —
  см. 1.79.0 выше.

**К-02: экзамен перестал быть только про массивы (1.77.0).** Закрыта та
единственная строка оговорки на вывеске, которая противоречила собственной
вывеске: мы называемся «информатикой», а давали из предмета только
программирование и алгоритмику.

- Новый файл **`js/algo-exam.js`**, девять задач, три темы: **кодирование
  информации** (биты на номер, объём сообщения, объём картинки), **логика**
  (таблица истинности, импликация, перебор параметра), **графы** (обход в
  ширину, кратчайший путь, число путей).
- ⚠️ **Почему отдельный файл, а не продолжение `js/algo.js`.** Содержательно:
  там задачи одного рода («дана последовательность, посчитай подходящие»),
  здесь другого — это разные темы кодификатора. Практически: соседняя сессия
  в этот же день растила `js/algo.js` задачами ОГЭ и ЕГЭ, и две сессии в одном
  файле перемешали бы работу. Файл дописывает в тот же `window.ALGO` и обязан
  подключаться ПОСЛЕ него.
- Регистрировать новый файл содержания надо в ПЯТИ местах: `index.html`,
  `sw.js` (SHELL), `build.js` (scripts), `tests/content-vs-python.js`,
  `tests/lessons.js`. Плюс группы в `ALGO_GROUPS` в `js/app.js`.
- Все девять решений сверены с настоящим python3 (наборов экзамена стало 100
  против 62) и отдельно прогнаны на нашем движке — вывод совпадает.
- ⚠️ **Чего по-прежнему нет и что осталось в оговорке: электронные таблицы.**
  Их нечем проверить — судья умеет запускать Python, а не считать формулы
  Excel. Обещать «закрываем экзамен целиком» нельзя и теперь.
- ⚠️ **README: число сверок выставлено 879** — это правда для ЭТОГО коммита
  (algo.js из HEAD + новый файл). Соседняя сессия в тот же момент правила это
  же число под свой набор задач; когда она закоммитит свои, число снова
  вырастет, и поправить его должна она.

**Первый этаж голоса: «Спросите вслух» в кабинете взрослого (1.76.0).**
Карточка `oralCardHTML`: берётся программа, которую ребёнок написал САМ
(`S.zan[...].progs`, их кладёт `zanKeepProg`), и к ней три вопроса, которые
родитель читает вслух.

- Вопрос 1 «что напечатает эта программа» — ответ считает движок.
- Вопрос 2 «а если поменять 5 на 7» — мутация через `myPredictMake`, ответ
  тоже считает движок. Проверено на живом коде: «очки = 7 / if очки > 5» →
  поменять 5 на 7 → «ещё разок». Это настоящая проверка понимания.
- Вопрос 3 «объясни своими словами» — ⚠️ **нарочно без правильного ответа**:
  машине его проверять нечем, и ставить галочку «верно» было бы враньём.
  Вместо ответа — что слушать: говорит ли ребёнок, ЧТО программа делает, или
  пересказывает, КАК написано.
- Ответы спрятаны в `<details>`: родитель читает вопрос с экрана, и открытый
  рядом ответ он проговорит раньше ребёнка.

⚠️ Ни микрофона, ни распознавания, ни модели: ребёнок отвечает ЖИВОМУ
человеку. Это и есть § 16 форсайта — «репетитор из родителя», а не «тренажёр
вместо родителя».

⚠️ **Решение фаундера 06.09.2026: продукт без обработки данных, пока.** Второй
этаж (Whisper в браузере) записан ДОПОЛНИТЕЛЬНОЙ опцией и не строится. Разбор,
чем этажи отличаются и почему второй слабее, чем кажется, — в
`docs/golos-i-produkty-2026-09-06.md` § 3.3а: второй этаж покупает ровно одну
вещь — независимость от присутствия родителя, то есть работает ПРОТИВ нашей же
ставки «домашний кружок на двоих». Там же записаны три условия, при которых его
можно переоткрывать, и как он должен быть устроен, если день придёт.

**Разбор: голос и следующие продукты (06.09.2026).** Новый файл
`docs/golos-i-produkty-2026-09-06.md`. Кода не трогает, это решение о том, что
строить дальше. Коротко:

- **Из четырёх строк оговорки на вывеске настоящая дыра одна — экзамен.**
  Вывеска называется «информатика», а из предмета закрыто только
  программирование и алгоритмика: нет кодирования информации, логики и таблиц
  истинности, таблиц, файлов, графов. Остальные три строки — выбор, их надо
  переписать, а не закрывать.
- **Голос: продавать надо не «объяснятеля», а экзаменатора.** Наш же замер —
  «как объяснить ребенку программирование» 14 показов против «ребенок не хочет
  учиться» 16 383. Работает не голос, который объясняет, а голос, который
  СПРАШИВАЕТ, — и движок спрашивает лучше репетитора, потому что знает трассу.
- **Устный ответ достраивает ставку А.** Запись авторства показывает, КАК шла
  работа, устный ответ — ПОНЯЛ ли. Перепечатать чужой код руками можно,
  объяснить его вслух без подготовки — нет.
- ⚠️ **Запрет на голосовой ввод от 03.09 устарел наполовину:** он стоял на ПДн
  (Chrome шлёт звук в Google) и на офлайне. К-06 от 05.09 в
  `aichek-platform/docs/proekty-budushchego.md` нашёл обход — Whisper в
  браузере, голос не покидает устройство. Держится запрет теперь на весе
  модели, то есть это вопрос цены, а не принципа.
- **Первый этаж голоса ничего не стоит и офлайн:** движок печатает РОДИТЕЛЮ
  два-три вопроса из трассы вместе с правильным ответом, ребёнок отвечает
  вслух живому человеку. Ни распознавания, ни модели, ни мегабайтов.

**Вывеска: четыре больших блока и озвучка (1.73.0).** «Надо выделить важное
большими блоками, чтобы бросалось в глаза» — указание фаундера. До этого
сильнейшие вещи продукта лежали строчками внутри карточек и вкладок, то есть
их читал только тот, кто и так уже заинтересовался.

- Блок сразу за числами, четыре штуки: **интернет не нужен вообще**;
  **тренажёр не верит на слово** (запись авторства + проверка понимания +
  скрытые проверки); **взрослый видит и настраивает всё** (присутствие,
  рамка, каникулы, потолок); **читает вслух**.
- ⚠️ Отбор жёсткий: сюда идёт только то, чего у конкурентов нет ни у кого.
  Пять «самых важных» вещей — это ноль важных вещей, поэтому их ровно четыре
  и больше быть не должно.
- **Озвучка** на вывеске появилась впервые (была с 1.38.0 и молчала): кнопка
  🔊 на карточке урока, отдельно автоматическое чтение разбора ошибки, звуки
  синтезируются на лету и работают офлайн. Сказано честно, что голос берётся
  у браузера и есть не везде.
- **Витрина проектов — в две колонки**: результат слева, код справа. Было
  «окно большое в горизонталь, а код справа кусочек»: вывод узкий, тридцать
  знаков в строке, и на всю ширину страницы выглядел пустой коробкой. Заодно
  ушла кнопка «показать код» — код виден сразу.

**Добавлено в 1.74.0:** «Спроси» и «Ревью кода» — строками во вкладке «Как
проходит урок», а не пятым и шестым большим блоком (решение фаундера: иначе
большие блоки перестают быть большими).

**Добавлено в 1.75.0:** задача дня и стрик — строками во вкладке «Когда на
урок нет сил».
⚠️ Стрик на вывеске описан **щитом**, а не числом дней подряд, и это надо
сохранить при любых правках. В списке задач висит пункт 4.4 «огонёк → уговор»:
убрать число серии из шапки, потому что «страх потерять серию» записан в
красные линии продукта. Продавать на витрине то, что внутри собираются
смягчать, нельзя — поэтому главное в тексте, что пропуск прощается, а не что
серию страшно оборвать.

⚠️ **Что на вывеске ВСЁ ЕЩЁ не показано** (проверено по README, решать
фаундеру): задача дня и стрик со щитом, поиск по урокам, тёмная тема, панель
символов на телефоне, «?» с подсказкой на каждом экране и полная инструкция
по адресу `#help`. Осознанно: это гигиена, а не довод для родителя.

**Вывеска: «взрослому» наверх, «видно прямо сейчас» и переключатель внутри
вкладки (1.72.0).** Четыре правки по живому разбору фаундера.

- **Блок «Взрослому» переехал третьим**, сразу за «Как это работает». Он стоял
  предпоследним, и фаундер дважды не нашёл вкладки ролей — значит не нашёл бы
  и родитель. Вывеску читает взрослый, и вопрос «а мне-то что» идёт сразу за
  «как это работает». Плюс в шапке появилась прямая дорога туда:
  «Вы родитель, учитель или репетитор? → Что вы получите со своей стороны».
- **У родителя первой строкой — «🟢 Видно прямо сейчас»** (указание фаундера:
  «это очень важно»). И он прав: вопрос, с которым родитель открывает кабинет,
  звучит не «сколько часов за неделю», а «он сейчас занимается или нет».
  Строка честная — всё считается из обычного снимка прогресса
  (`presenceDetailHTML`), ребёнок ничего дополнительного не отправляет.
  ⚠️ Рядом сразу вторая строка: **экран ребёнка — только с его согласия**.
  Без неё первая читается как реклама слежки, а правило продукта обратное:
  ребёнок всегда видит, когда его видят.
- **Мелкий ряд вкладок стал переключателем.** Два одинаковых ряда пилюль
  подряд читались как один перенесённый, и было непонятно, что чему
  подчинено. Теперь это обойма с подписью («Разминка:», «Что рисуем:»):
  другой вид, другой вес.
- ✈️ вместо 📶 у «Работает без интернета»: 📶 — это шкала уровня сигнала, и
  рядом с обещанием «без интернета» она говорила ровно обратное.

**Вывеска: вкладки вместо простыни, и всё живое (1.71.0).**
Разбор фаундера сразу после 1.70.0. Три правила, теперь обязательные:
**страница не должна быть вертикально длинной**, **текст не идёт узкой
колонкой посреди пустоты**, **много одинаковых карточек подряд никто не
читает**. Плюс просьба: примеры должны быть живыми, «чтобы люди нажимали и
что-то оживало».

- Замер: было 6 730 px, стало **4 709 px** — при заметно большем содержании.
- Разделы сложены во вкладки (`landTabsHTML` / `wireLandTabs`): «Как это
  работает» (4 вкладки), «Кроме уроков» (2), «Взрослому» (3 роли),
  витрина проектов (5 миров). «Честно про границы» и «С чего начать» слиты
  в один блок в две колонки — страница больше не заканчивается дважды.
- Шапка раздела — две колонки (`secHead`): заголовок слева, лид справа.
- Живое, всё настоящим движком: **черепашка рисует** (`animateTurtle`),
  **«угадай вывод»** с показом расхождения, **витрина пяти проектов**
  (программа выполняется на месте), **задача-близнец** у репетитора
  (`HW.build`, кнопка «🎲 Другой ученик»).
- ⚠️ У родителя живого блока НЕТ сознательно: рисовать отчёт про
  несуществующего ребёнка нельзя — внутри продукта имён детей на сервере
  нет вовсе, и врать на витрине об этом же тем более.
- Цена перестала молчать: фишка «🆓 Сейчас бесплатно» на первом экране и
  прямая строка у кнопки входа (оплаты в продукте нет вовсе).
- ⚠️ **Грабли: имена классов.** `.pane`, `.shcode`, `.shout`, `.world` уже
  заняты другими экранами, и блоки вывески молча получали чужое оформление.
  Landing-классы теперь `.ldpane`, `.ldcode`, `.ldout`, `.ltabs.wld`. Новый
  класс вывески — сначала проверить на столкновение в `css/style.css`.
- ⚠️ **Красный тест не мой:** `tests/content-vs-python.js` требует поправить
  в README «841 сверок» на 844. Число выросло от правок соседней сессии в
  `js/algo.js` и `js/homework.js` (они не закоммичены). Чинить должен тот,
  кто их делает.

**Вывеска стала красивой: живое окно вместо стоковой фотографии (1.70.0).**
Задача фаундера 06.09.2026: «посмотри на сайты конкурентов и на наш — у них
красивые сайты, у нас нет». Посмотрели вживую: Алгоритмика, Айтигенио,
Пиксель, «Код будущего» и Codédex. Разбор — `docs/vyveska.md` § 7, это первый
разбор конкурентов **по картинке**, а не по словам.

- **Находка:** у всех российских школ на первом экране фотография ребёнка за
  ноутбуком. Она есть у всех и потому не отличает никого; снимать нам некого,
  а главное — у конкурентов **нечего показать**, обучение живёт за логином.
- **Ответ:** на первом экране стоит **работающее окно редактора**. Не скриншот:
  код выполняется настоящим движком (`Runtime.get("mini")`), вывод печатается
  построчно, а кнопка «💥 А если ошибиться» убирает двоеточие и показывает ту
  же плашку `errHTML`, что увидит ребёнок в уроке. Два обещания вывески
  («код запускается сразу», «ошибки объясняются словами») больше не
  обещаются, а показываются.
- Программа курса — **дорога из пяти цветных миров** (рельс, номер призраком,
  темы фишками, хвост «босс мира, сертификат»); четыре шага урока — дорожка с
  узлами; числа — одна полоса на четыре колонки; границы обещания — один
  документ вместо четырёх жёлтых полос; значки карточек — на подложке;
  под кнопкой четыре фишки-возражения; у разделов надпись-указатель и
  появление при прокрутке (`.rv`).
- ⚠️ **Ни одной внешней картинки и ни одного видео.** Всё оформление —
  CSS-градиенты и системные эмодзи, то есть килобайты: сайт обязан работать
  без сети и собираться в один файл. Скриншота продукта не будет никогда —
  картинка через полгода начинает врать, живое окно сломается только вместе
  с движком.
- Правки: `screenAbout` и помощники `landDemoHTML` / `wireLandDemo` /
  `landRun` / `wireReveal` / `landWorldVars` в `js/app.js`; блок «вывеска» в
  `css/style.css` (переменные `--w1…--w5`, `--lg1…--lg3` живут там же и больше
  нигде). Все проверки зелёные: `npm test`, `npm run audit`.

**Вывеска: у сайта появилась главная страница (1.69.0).**
Жалоба фаундера 06.09.2026: «не вижу, где главная — открывается выбор из трёх
ролей». Так и было: незакреплённое устройство встречал `screenRoles()` — три
двери и ни слова о том, куда они ведут. Родитель по ссылке не мог узнать ни
чему тут учат, ни сколько это уроков, ни как выглядит урок. Описание продукта
жило только в `<title>`/`meta description` и в `docs/vyveska.md` как план.

- Новый экран `screenAbout()` (место `about`, адрес `#about`) — первый для
  того, кто ещё ничей. Слова заголовка — вариант А из `docs/vyveska.md`
  («Информатика без репетитора»), тот же, по которому уже стояли title и
  meta description.
- Разделы: числа продукта, «Как проходит урок», программа пяти миров,
  «Кроме уроков», «Что видит взрослый», «Честно про границы», вход.
- ⚠️ **Числа и программа не написаны руками**, а считаются при отрисовке
  (`aboutCounts()` + `CURRICULUM` + `trainCards()`): вывеска не может
  разойтись с продуктом, когда список разминок или задач изменится.
- «👀 Посмотреть первый урок» открывает настоящий урок **без регистрации** —
  самый короткий путь от вывески к продукту.
- «Честно про границы» стоит ДО кнопки входа: язык один (Python), экзамены
  закрываем частично, проверяет движок, прогресс лежит в браузере.
- `screenRoles()` стал тем, чем задуман, — входом: с него есть дорога назад
  «О тренажёре», пока устройство ничьё.
- `screenRoles()` стал тем, чем задуман, — входом: с него есть дорога назад
  «О тренажёре», пока устройство ничьё.
- **Дом гостя.** `goHome()` теперь знает четвёртую роль: пока устройство ничьё,
  «домой» ведёт на вывеску. Раньше логотип уводил гостя в пустую карту миров —
  та же ошибка, от которой в 1.65.0 лечили родителя, только с другого конца.
- **Направления вынесены из общей сетки** (по замечанию фаундера «мы же сделали
  и другие направления, и ЕГЭ»): раздел «Не только язык» — алгоритмы и экзамены
  (10 ОГЭ + 10 ЕГЭ, счёт шагов), «Ты и ИИ» (19 + 5 на приёмку), проекты (7).
  Раньше всё это стояло одной карточкой между разминками и играми.
- **Раздел «Что остаётся на руках»** (по замечанию «не пропусти часть нашей
  работы»): портфолио и 8 сертификатов, галерея и файлы .py, своё задание
  ссылкой, своя игра по ссылке, мастерская, «Повторить» с бестиарием.
- **Кабинет назван для всех троих** — родителя, учителя, репетитора: добавлены
  «Занятие, а не поток уроков» (20/30/45, перерыв, сжатие плана) и «Рабочее
  место наставника» (группа, ссылки, живое занятие). Рабочее место было
  сделано в 1.56–1.64 и на вывеске не упомянуто вовсе.
- **Карточка ссылки (og:image) и мета-теги.** Раньше ссылка в мессенджер
  приходила голой — ни картинки, ни заголовка, а мессенджер сегодня наш
  единственный канал (домена нет). Картинка НЕ нарисована руками: её собирает
  `node og/make.js` из настоящих чисел курса, чтобы обещание в превью не
  устарело молча. ⚠️ Поменялся курс — пересобрать и закоммитить.
- `npm test` и `npm run audit` — зелёные, ошибок JS ноль.

**Понятные подсказки: тренировочные разделы, проход завершён (1.68.0).**
Третья и последняя часть прохода по отзыву ребёнка. Теперь ВСЕ подсказки
продукта соответствуют стандарту: уроки (1.66–1.67) + тренировки (1.68).

- Переписаны подсказки: разминки — 21 блок (осторожно: для predict-разминок
  подсказка НЕ должна выдавать сам ответ, ответ и есть вывод), «Ты и ИИ» —
  19, проекты — 28 шагов (включая обе игры), алгоритмы/ОГЭ — 27,
  спецификации — 5, домашка — 22 (там подсказки — короткие строки-опоры,
  формат сохранён).
- **Тестовая защита распространена на все разделы**: в tests/lessons.js
  проверка подсказок вынесена в `checkHints()` (текст есть, обратные
  кавычки парные, пример `code` запускается движком) и зовётся для уроков,
  разминок, «Ты и ИИ», проектов, а также для SPECS и ALGO (их файлы теперь
  грузятся в тест). В tests/homework.js — та же проверка своей копией.
- Она сразу и сработала: поймала сырые теги ссылки в подсказке проекта
  project-w5 (разметка-проверка) — переформулировано без тегов.
- `npm test` (163/163) и `npm run audit` (0 находок) — зелёные.
- Отзыв ребёнка от 05.09 закрыт полностью: жаргон вычищен, каждая
  подсказка в продукте объясняет явно, код показывается кодом.

**Понятные подсказки, миры 2–5 (1.67.0).** Продолжение прохода 1.66.0 по
слову фаундера «делай дальше»: переписаны ВСЕ подсказки уроков в мирах
2, 3, 4 и 5 — 240 подсказок в 80 уроках.

- Тот же стандарт, что в мире 1: подсказка объясняет явно, а не намекает;
  код в \`обратных кавычках\` рендерится чипом; у ~25 ключевых подсказок —
  блок-пример на других числах/именах, чем решение (лесенка «объяснил
  приём → показал место → почти собрал» сохранена, аудит слива первой
  подсказки прошёл: одну находку — урок 73 context — он поймал, поправлено).
- Мир 5 и до прохода был развёрнутым — там в основном код-чипы, точечные
  примеры и уточнения формулировок; миры 2–4 переписаны глубже.
- Проверено: `npm test` (163/163) и `npm run audit` (0 находок) — примеры
  из подсказок движок реально запускает (tests/lessons.js).
- ⚠️ Осталось на следующий проход: подсказки в разминках (21), «Ты и ИИ»
  (19), проектах (28 шагов), алгоритмах (27), спецификациях (5) и домашке
  (22) — пока обычные строки. Механика их уже понимает, нужен только
  контент. См. «ЧТО МОЖНО ДЕЛАТЬ ДАЛЬШЕ».
- Попутно починен плавающий тест: проверка «лёгкий урок попал в трудные»
  в tests/full-run.js читала весь остаток недельной карточки и падала в те
  даты, когда «Вопрос за ужином» (меняется раз в день) выпадал из урока
  «Первая команда». Теперь она смотрит только сам список «Тяжело далось».
  Падение было НЕ от контента: воспроизводилось и на чистом 1.66.0.

**Понятные подсказки: объяснение + наглядный пример (1.66.0).** Отзыв ребёнка
с занятия: не все подсказки понятны, споткнулся о «хвост после запятой» в
описании. Решение фаундера: цель — не скорость, а понятность; подсказка
должна объяснять явно, текстом И визуально, с очень понятными примерами.

- **Механика.** Подсказка теперь строка ИЛИ объект `{ t, code }`: `t` — текст,
  куски кода в нём пишутся в \`обратных кавычках\` и рендерятся кодом-чипом
  (как в теории), `code` — маленький пример-программа отдельным блоком с
  подписью «Пример (не ответ, а образец)». Рендер — `hintHTML` в app.js,
  стили — `.hintout .step code`, `.hintex`, `.hintcode` в style.css. Работает
  во всех разделах через общий `wireHint` (уроки, разминки, «Ты и ИИ»,
  проекты, алгоритмы, спецификации, домашка).
- **Правило примеров:** пример пишется на ДРУГИХ числах и именах, чем решение
  задачи, — он объясняет приём, а не выдаёт ответ. За первой подсказкой
  по-прежнему следит tests/audit.js (для объектов считает и t, и code).
- **Контент.** Переписаны все 60 подсказок мира 1: явные объяснения вместо
  намёков, код в обратных кавычках, у 11 ключевых подсказок — блок-пример.
  Остальные миры ЖДУТ такого же прохода (см. «ЧТО МОЖНО ДЕЛАТЬ ДАЛЬШЕ»).
- **Жаргон «хвост» вычищен** из текстов для ребёнка: world1 (теория деления —
  теперь «цифр после точки бывает очень много»), world3 (super().card()),
  projects.js (super().описание()), cheatsheet.js (print sep/end). Переменные
  `хвост` в решениях мира 5 не тронуты — это код, а не объяснение.
- **Тесты.** lessons.js проверяет каждую подсказку: текст есть, обратные
  кавычки парные, пример `code` запускается движком без ошибок; проверка
  разметки читает у объектов поле t. audit.js «первая подсказка выдаёт
  решение» понимает оба формата. Вся сотня уроков + audit — зелёные.

**Облако развёрнуто, и живое занятие починено по факту боя (1.65.1).**
Функция обновлена через `yc` (версия d4em89sf8hcjiggppu5f, папка
b1g2lrffd7accfi53l3c, бакет kodokvest-progress-7f3a91) — метрики и трансляция
работают на бою. По дороге вылезли два настоящих дефекта, которых не было
видно в тестах с обычной папкой:

- **`unlink` в смонтированном бакете молча не работает.** Выключение
  трансляции отвечало «ок», а кадр продолжал читаться. Теперь кладётся
  надгробие `{off:true}`; тест подделывает неудаляемый файл и проверяет
  второй путь.
- **Кэш смонтированного бакета СВОЙ В КАЖДОМ экземпляре функции.** Соседние
  запросы отвечают разным больше десяти секунд. Сервер теперь отдаёт
  надгробие как есть, а решение принимает клиент: `liveWatcher`/`liveAccept`
  помнят максимальное serverAt и не откатываются назад. Замер на боевой
  функции: мигания нет, гаснет за 3 с после «Прекратить».
- ⚠️ **Вывод на будущее:** смонтированный бакет годится для медленных данных
  (прогресс), но не для быстрого обмена. Любую новую «живую» механику
  проверять НА БОЮ, а не только тестом с папкой.
- ⚠️ ADMIN_KEY засветился в выводе команды деплоя (он у фаундера в
  транскрипте сессии). Смена — одной командой, см. ответ ассистента.


**Три находки фаундера с боя (1.65.0).**

- **Планшет: клавиатура съезжала экран.** `interactive-widget=resizes-content`
  в мете вьюпорта (Android перестал накрывать страницу); класс `kb` по сжатию
  visualViewport (`kbApply`) снимает липкую колонку урока, прячет `.taskpin`
  и плавающие кнопки; `scroll-padding` + доцентрирование редактора через
  450 мс после фокуса (только touch). ⚠️ Тест проверяет мету, порог класса и
  наличие CSS-правил — настоящего планшета в CI нет.
- **Время: «не видно, сколько занимался».** `fmtDur` вместо округления до
  минут: «40 сек», «3 мин 20 сек», «1 ч 12 мин». Старый формат печатал «—»
  на всём короче минуты. Плюс `daySec/dayMs` — время за СЕГОДНЯ из карты
  часов: плитка в statsGrid, строка в сводке момента, карточка на «Сегодня»
  ребёнку. fmtMins оставлен псевдонимом.
- **Кнопка «Кодоквест» уводила взрослого в детский тренажёр.** Причина: у
  взрослого детская навигация спрятана (adminui), а логотип вёл в
  screenWorlds. Введён `goHome()` — дом по роли; на него переведены ВСЕ 26
  кнопок «домой», логотип и вкладка «Главное». Тест падает, если появится
  новая кнопка с прямым `onclick = screenWorlds`.
- ⚠️ У фаундера на родительском маке остался старый детский профиль
  («Роман-11») — это локальные данные того устройства, не утечка. goHome
  делает его недостижимым по кнопке; при желании чистится «Выйти» в профиле.


**Сводка момента и починка словаря мест (1.64.1).** По взгляду фаундера на
бой в первый же час: статус показывал «в тренажёре: в тренажёре» — половина
ключей PLACE_RU была выдумана и не совпадала с настоящими именами экранов.
Починено + тест теперь снимает список мест С САМОГО app.js и роняет сборку
на выдуманном ключе. По просьбе фаундера статус стал сводкой момента
(`presenceDetailHTML`): что делает сейчас, что сдано сегодня (уроки по
именам, домашка, задача дня) и как идёт занятие («сделано 1 из 3 шагов»).
Считается целиком из обычного снимка — ребёнок ничего нового не шлёт;
опрос карточки обновляет и живые поля сводки. ⚠️ Подробная сводка — в
карточке ученика и у родителя; в группе осталась лёгкая точка.

**Присутствие и живое занятие (1.64.0).** Решение фаундера 05.09 по вопросу
«видеть, что делает ребёнок онлайн»: построены уровни 1 и 2, уровень 3
(скрытое наблюдение) отвергнут и закрыт правилом продукта **«ребёнок всегда
видит, когда его видят»** (записано в README, сказано ребёнку в профиле,
держится тестом).

- **Уровень 1 — статус**: `S.now` пишется активным тиком, уезжает обычным
  пушем (25 с) — НОЛЬ новых запросов. `presenceInfo/presenceHTML`: зелёная
  строка в карточке ученика (обновляется опросом `kidWatch` — только чтения),
  у родителя, зелёная точка в группе. «Офлайн» не показывается намеренно.
- **Уровень 2 — трансляция**: ребёнок жмёт «📺 Показать экран наставнику»
  (экран занятия + профиль) → кадр (код+вывод) раз в 7 с через `op=live_set`
  в `<код>.live.json`; неизменившийся кадр не шлётся. Плашка «🔴 Наставник
  видит твой код» живёт на body — сменой экрана не прячется; тест падает,
  если трансляция без плашки. Гаснет: кнопкой, концом занятия, через 45 мин.
  Взрослый: `screenLiveView` (только чтение, свежесть по СЕРВЕРНЫМ часам),
  вход из карточки ученика при живом кадре («🔴 Ребёнок показывает экран»).
- Сервер: `live_set/live_get` в cloud/index.js; `isStudentFile` — файлы
  трансляций не попадают в list/stats/ping. ⚠️ Функцию в облаке обновить
  руками.
- Тесты: серверные (кадр/выключение/лимиты/не-ученик) + раздел 10г full-run
  (свежесть, слияние S.now, тик пишет статус, кадр доезжает, повторный кадр
  не шлётся, плашка обязательна и переживает навигацию, конец занятия гасит,
  профиль говорит ребёнку).
- ⚠️ Кнопка показа есть на экране занятия и в профиле; на обычном уроке вне
  занятия — только через профиль (сознательно: не перегружать урок).


**Экзамен: первый срез К-02 (1.63.0).** Пункт 5 топ-5 плана
`docs/razvitie-2026-09-05.md`. Раздел `#algo` («Алгоритмы, ОГЭ и ЕГЭ»)
вырос с 10 до 27 задач:

- **+7 задач формата ОГЭ** (каркас ФИПИ, всего в группе 10): минимум чётных,
  среднее по кратным, диапазон, последняя цифра, пары соседей, минимум по
  пятеричной, сумма двух наибольших чётных. Везде «NO» и ограничения числами.
- **Новая группа «Типы ЕГЭ»** (10 задач, свои формулировки, только «напиши
  программу»): делители (ровно 6), ближайшее подходящее, пары с чётным
  произведением, наибольшая сумма соседей, самая длинная серия в строке,
  вхождения «ab» (ban .count), основание системы, подъёмы, произведение цифр,
  «наибольшая сумма, кратная трём» (задача на идею — босс группы).
- **Скрытые наборы `sets`** в runAlgoCheck: программа и эталон гоняются на
  наборах, которых нет на экране, — `print(3)` с ответом из примера больше
  не сдаёт задачу. Набор, где разошлось, ПОКАЗЫВАЕТСЯ целиком (мы учим, а не
  экзаменуем). Сеты добавлены и трём старым задачам ОГЭ. Правило теста:
  у задач групп oge/ege не меньше двух сетов (учебных без ввода не касается).
- **Сверка с python3**: раздел «алгоритмы» в content-vs-python — все 62
  набора каждый прогон (итого сверок 841, README поправлен).
- Тесты: правила сетов + целевой обход константой в 13.3а full-run.
- ⚠️ Отложено из плана К-02 сознательно: генерация задач от кода ученика —
  сначала замер письмами репетиторам (нужен ли экзамен раньше домашек);
  полный мир на 40 задач с разборами — после замера.


**Игра по ссылке и первая игра-проект «Виселица» (1.62.0).**
Пункт 4 топ-5 плана. Что появилось:

- **`#play=`** — игра уезжает ссылкой целиком (как #task=/#work=, без сервера
  и ПДн). Друг открывает и СРАЗУ играет: редактор спрятан классом `plonly`,
  «🔧 Заглянуть в код» открывает изнанку, «Забрать в песочницу» уносит код.
  ⚠️ Сознательное исключение из правила «#work= не запускает чужой код» —
  причина записана в комментарии у playPack и в README.
- **«🔗 Отправить игру другу»** на экране каждой игры (уезжает ТЕКУЩИЙ код,
  с правками ребёнка) и на финале игры-проекта.
- **Игры-проекты**: kind:"game" + needs:"project-w1" в PROJECTS. Раздел
  «Собери свою игру» на экране игр; заперта до сборки проекта мира 1
  (финал мира открывает следующую игру, п. 4.5 плана). Финальный экран
  игры-проекта — в игровом режиме play:true (иначе input() падал бы).
- **«Виселица»** (`project-game-hangman`): 4 шага — прятки строкой, функция
  показать() c циклом, ход через input(), игровой while. Без random намеренно:
  секрет в переменной, перед отправкой другу его меняют. Все шаги сверены с
  python3 (779 сверок).
- **Проекты научились шагам с input()**: step.stdin — записанная партия;
  runProjectCheck гоняет ребёнка и эталон с одними ответами; lessons.js
  требует stdin при input(); content-vs-python передаёт stdin (пятым
  аргументом compare!); витрина запускает игру записанной партией.
- Тесты: раздел «игра по ссылке» в full-run (упаковка/мусор/границы, экран
  прячет код, партия играется, «Заглянуть в код», песочница, замок
  игры-проекта, финал в игровом режиме).
- ✅ Остальные четыре игры-проекта собраны 07.09.2026 (1.80.0) — см. верх файла.


**Возвращаемость: метрики и три крючка (1.61.0).** Пункт 3 топ-5 из
`docs/razvitie-2026-09-05.md`. Закрывает измерителем главный вопрос
«доходит ли чужой ребёнок до 20-го урока».

- **`op=stats` в облачной функции** (`cloud/index.js`): начавшие, доля
  вернувшихся в первую неделю (окно закрыто через 8 дней), дошедшие до
  20/40/60/80/100 уроков (курс последовательный — счётчик и есть позиция),
  активные за 4 недели, медиана уроков. Ключ — тот же ADMIN_KEY. Клиент —
  `Cloud.stats(key)`, карточка «📈 Возвращаемость» на `#group`; при <5
  начавших экран сам говорит «это случаи, а не замер». ⚠️ Функцию в облаке
  надо обновить руками (cloud/README.md).
- **«В следующий раз»** в финале занятия: следующий урок и день следующего
  занятия по расписанию. Не «ещё чуть-чуть»: продолжить нельзя, названо
  «когда завтра».
- **«До конца мира — примерно N занятий»** на Главном и на экране мира;
  темп ребёнка из frame.perLesson, если принят.
- **Возвращение после паузы** (4+ дня): карточка «С возвращением» на Главном —
  всё цело, где остановился, вход с разминки. Без состояния: исчезает после
  первого занятия. Тест ищет слова вины.
- Тесты: раздел «метрики» в `tests/cloud-function.js` (ветки: вернулась /
  не вернулся / окно не закрыто / пустой код), раздел 10в в `full-run.js`
  (счётчик, карточки, «без расписания день не называется», стата через
  настоящую функцию). ⚠️ Раздел 10в прячет и возвращает живое состояние
  теста: до него все сто уроков уже решены.
- ⚠️ НЕ построено из пункта 4.3 плана: «первое занятие после паузы короче» —
  это правка zanPlan, отложена сознательно.


**Домашка от наставника и банк задач-близнецов (1.59.0).** Первый пункт
топ-5 из `docs/razvitie-2026-09-05.md` (план развития по четырём корзинам,
собран в этой же сессии; там же — разбор «супер-платформы не только Python»
по вопросу фаундера). Что появилось:

- **`js/homework.js`** — 22 задачи-близнеца по трём мирам: то же умение, что
  в уроке, но другие условие и числа. Программу-эталон пишет шаблон, ответ
  считает движок (как в `js/parent.js`). Числа из СЕМЕНИ (`hwSeed`: код
  ученика + id + день выдачи) — у ребёнка и наставника условие одно, у разных
  детей разное. У задач есть `need` («напиши функцию» не сдать тремя print)
  и `ban` (рекурсия без циклов).
- **Выдача из кабинета**: в карточке ученика (`kidRender`) секция «📮 Домашка» —
  до 3 задач (`HW_MAX`), срок, «что уже задано». Список строится ИЗ ПРОГРЕССА
  ребёнка (`hwAvailableFor`): задать непройденное нельзя. Едет через поле `hw`
  тем же путём, что расписание (`kidSaveHW` перечитывает запись перед
  сохранением). Родитель из своего кабинета тоже может задавать.
- **У ребёнка**: кнопка «📮 Домашка · N» на Главном, экран `#hw`, решение в
  обычном studio с подсказками. Звёзд НЕ даёт (правило задания взрослого).
  Просрочка ничего не сжигает: «срок был вчера» — факт без укора.
- **В группе**: пометка «домашка: сдано X из Y» (приглашение поговорить,
  не табель).
- **Слияние** (`mergeProgress`): условие (семя, срок) — от того, кто задал;
  «сдано» и попытки — от того, кто делал.
- **Тесты**: `tests/homework.js` (264 прогона по 12 семенам + сверка эталонов
  с настоящим python3, в `npm test`), секция 4а в `tests/full-run.js`
  (выдача → решение → «сдано», обход need не проходит, слияние, просрочка).
  Тест поймал по дороге: homework.js не был в кэше sw.js — офлайн бы сломался.
- **Домашка всей группе одной кнопкой** (та же сессия): карточка на экране
  группы, у каждой задачи счётчик «получат: N из M» (только пройденные уроки),
  семя у каждого ученика своё. Запись по одному с перечитыванием; итог
  называет, что НЕ уехало. В строке ученика — «домашка 1/3». Тест 10.4 в
  `full-run.js` гоняет выдачу через настоящую серверную функцию: кому уехало,
  кому нет, повтор не задваивает, семена у соседей разные.
- **Отчёт родителю текстом + вопросы к занятию + напоминание** (та же
  сессия, пункт 2 топ-5 плана). `weekFacts(st)` — расчёт недели, один на три
  потребителя. `parentReportText` — 6 строк для мессенджера БЕЗ имени ребёнка,
  с плохими новостями и без приговоров (тест ищет слова-приговоры); карточка
  с «Скопировать» в карточке ученика и в кабинете родителя (слова свои у
  каждого). `askCardHTML` — три темы для разговора: трудный урок, команда из
  пройденного, частая ошибка из бестиария. Порог тишины 5 → 3 дня
  (`GROUP_QUIET_DAYS`), у замолчавшего в группе кнопка «Текст напоминания» —
  приглашение без укора, и только у того, кто занимался и замолчал.
  Тест — раздел 10б в `full-run.js`.
- ⚠️ Чего НЕТ намеренно: писем и рассылки (адрес взрослого — ПДн), печати
  отчёта (канал — мессенджер), наград за домашку, «сгорания» просрочки.


**Раздельные кабинеты: взрослый и дети (1.56.0).** Роли разведены по
УСТРОЙСТВАМ (настоящей авторизации у статического сайта нет). Мак взрослого
объявляется кабинетом: адрес `#admin` → «Сделать это устройство кабинетом» →
пароль (хранится только в браузере как свёртка, `adminHash`). Дальше это
устройство всегда открывается в кабинет (`screenKids`), детская навигация в
шапке спрятана (класс `.adminui`). В кабинете: «Мои ученики» — список с
кодами и ссылками-приглашениями (`?kid=…`), карточка ученика с расписанием и
отчётом. Расписание правится УДАЛЁННО: `kidTarget` подменяет `frame()`/
`frameSet()` на рамку выбранного ребёнка, `kidSave` пишет её на сервер по коду
(`Cloud.save(data, code)`), рамка догоняет ребёнка при следующем обмене
(слияние «свежее по setAt побеждает»). Ребёнок открывает ссылку — устройство
привязывается к своему коду и снимает с себя админ-статус (на случай «открыл
на мамином ноутбуке»). Нового на сервере не потребовалось. Старая полная
панель наставника (группа, просмотр по коду) жива под `#panel`, ссылка на неё
внизу кабинета. Общий редактор рамки вынесен в `frameEditorHTML` +
`bindFrameEditor`, чтобы кабинет ребёнка (`#adult`) и кабинет взрослого делили
одни органы управления. ⚠️ Код доступа `kodokvest-2026` в старую панель
по-прежнему открытым текстом — это отдельный незакрытый долг.

**Пачка правок по «взгляду ребёнка и родителя» (1.56.0, та же сессия).**
Шесть тупиков (окно победы без прокрутки резало кнопку «Дальше»; номера строк
съезжали с кодом; поля <16px вызывали автозум на iPhone; «undefined минут» в
кабинете; ложное «Отправлено» без кода; вход по коду стирал прогресс до
запроса), жёсткий потолок дня, который не запирал уроки/игры/разминки в обход
занятия; стирание рамки взрослого детским «Выйти»; кабинет, выбрасывавший
взрослого на главный экран из-за отрисовки в `then` у `doRegister` и обхода
`claimScreen` четырьмя экранами взрослого. Подробности — в истории git.

**Перерыв и потолок дня (1.43.0).** Варианты F и E из § 16
`docs/zanyatie-i-vzroslyj.md`; на этом все шесть вариантов финала занятия
разобраны — пять построены, B отвергнут осознанно.

- **Перерыв**: кнопка между шагами (внутри урока её нет), пять минут, отдельный
  экран, который больше ничего не предлагает. Время перерыва идёт в ПАУЗУ, а не
  в работу и не в карту часов. На 45-минутном занятии после половины тренажёр
  предлагает перерыв сам.
- **Потолок дня**: 30/45/60/90 минут или без потолка, считается ВСЁ время в
  тренажёре — родитель мерит экранное время, а не учебное. ⚠️ По умолчанию
  мягкий (говорит, но не запирает); жёсткий включается отдельной галочкой и
  всё равно не обрывает начатое: кнопки «открыть шаг» нет, а закрыть занятие
  можно. Наказывать за увлечённость продукт не будет.

**Тренажёр сам меряет длину занятия (1.42.0).** Вариант G из § 16
`docs/zanyatie-i-vzroslyj.md`. Число уроков в занятии было посчитано из длины
текста урока — из замысла, а не с ребёнка. Теперь `zanStats` меряет по закрытым
занятиям, сколько активных минут уходит на один урок у ЭТОГО ребёнка, и кабинет
предлагает взрослому поправку: «занятие идёт 33 минуты при заявленных 30, один
урок занимает 11 минут — дольше средних 7; помещается 2 урока, а ставится 3».

- Четыре честности зашиты в замер: только закрытые занятия с хотя бы одним
  уроком, только активные минуты, медиана вместо среднего, молчание, пока
  занятий меньше трёх («одно занятие — случай, а не замер»).
- ⚠️ **Замер ничего не меняет сам.** План перестраивается только после кнопки
  взрослого; принятое значение живёт в `frame.perLesson` и идёт и в состав
  занятия (`zanSlotsFor`), и в план от даты (`paceCheck`) — обещать дату по
  среднему темпу, зная темп ребёнка, значило бы врать вежливо.
- Это единственное место, где продукт правит собственное обещание по факту.
  ⚠️ И оно же упирается в то, что данных пока нет: код готов, живых занятий
  чужих детей — ноль.
- Из § 16 остались непостроенными E (потолок дня) и F (перерыв внутри занятия).

**Конец занятия: выбор из трёх и сжатие плана (1.41.0).** Продолжение той же
сессии. Фаундер выбрал из шести вариантов § 16 `docs/zanyatie-i-vzroslyj.md`
три — A, C и D.

- **Карточка «30 минут прошло» с тремя кнопками** на границе шага: закончить /
  ещё один урок / только проверку и всё. Средняя — это согласованный объём, а
  не открытая дверь: `rec.ask` помнит обещание, и вопрос вернётся, когда
  обещанный урок СДЕЛАН. ⚠️ Продолжение не хвалится ни словом — «молодец, что
  не остановился» превращает занятие в гонку; тест ищет эти слова и падает.
- **Сжатие плана** (`zanSqueeze`): половина времени прошла, сделано меньше
  половины — последний урок откладывается, чтобы ребёнок всё равно дошёл до
  финала. ⚠️ Видимо: пунктирная пометка «перенесли на следующий раз» в плане,
  полоска на экране, отдельная строка в отчёте (разная для сжатия по времени и
  для выбора ребёнка). Проверка понимания не режется никогда.
- ⚠️ **Найденная по дороге дыра:** окно победы уносило кнопкой «Дальше →» прямо
  в следующий урок, мимо занятия, — значит разговор «время вышло» не случился бы
  вовсе. Пока идёт занятие, главная кнопка теперь «← К занятию».
- Занятие закрыто, когда в остатке пусто: каждый шаг либо сделан, либо
  перенесён (`zanRemaining`, а не сравнение длин).
- ⚠️ **Не построено из § 16:** E (потолок дня), F (перерыв внутри занятия),
  G (система сама измеряет длину занятия). G самый ценный — он закрывает
  данными вопрос «сколько уроков влезает в полчаса живого ребёнка».

**Занятие, кабинет взрослого и честное время (1.40.0).** Самая большая правка
оболочки за всё время: продукт перестал быть только детским тренажёром и
получил второй контур — взрослого. Стратегическая часть разобрана в
`docs/zanyatie-i-vzroslyj.md`, там же причины каждого решения; здесь коротко,
что появилось в коде.

- ⚠️ **Сначала пришлось починить время, и это был долг, а не улучшение.**
  `startTimer` прибавлял десять секунд каждые десять секунд, пока открыт экран
  урока: ребёнок, ушедший ужинать с открытой вкладкой, «занимался» всё это
  время. Пока цифра жила в панели наставника, это была неточность. С того дня,
  как время уезжает взрослому в отчёт, она стала бы ложью, обесценивающей весь
  отчёт. Теперь секунда считается рабочей, только если вкладка видима, окно в
  фокусе и страницу касались не позже шести минут назад; остальное —
  **пауза**, и она хранится отдельно (`pauseMs`) и показывается честно.
  Счётчик стал один на приложение (`actStart`/`tickOnce`), `startTimer` лишь
  говорит ему, какой урок открыт.
- **Занятие как единица** (`S.zan`, экран `#zan`): 20/30/45 минут, план из
  разминки, 2–4 уроков, повторения и проверки понимания. Пять правил держатся
  кодом: урок не режется посередине, занятие закрывается даже если не
  получилось, план детерминирован по дате, перевыполнение не поощряется,
  пропуск не сгорает. ⚠️ Это НЕ блиц на таймере (его мы по-прежнему не делаем):
  здесь время ограничивает сеанс и разрешает закончить, а не торопит думать.
- **Отчёт из четырёх строк** (`zanReport`): что было, за что похвалить
  конкретно, понял или прошёл (результат `predict`), что спросить (вопрос за
  ужином). ⚠️ Отчёт обязан иногда содержать плохие новости — подсказки и
  показанные решения не прячутся, иначе через три недели его перестанут читать.
- **Карта активности день × час** (`S.hours`, `heatHTML`): отвечает не на
  «сколько», а на «когда». Слияние — максимум по ячейке, не сумма.
- **Рамка взрослого** (`S.frame`): дни, длина, соотношение «новое/повторение»,
  дата-цель с обратным отсчётом, каникулы, галочка отчётов. ⚠️ Порядок уроков
  взрослому не отдан намеренно: его держит проверка «конструкция объяснена
  раньше, чем понадобилась». Есть гейт разумности: «курс за месяц» честно
  пересчитывается вслух и не даётся.
- **Кабинет взрослого** (`#adult`, вход тем же кодом, что и панель наставника).
- **Задание от взрослого**: три жанра (задача с его числами по шаблону из
  `js/parent.js`, назначение урока, вопрос на объяснение). Задача уезжает тем же
  `#task=`, что и «Своё задание» — программу пишет шаблон, ответ считает движок,
  взрослому программировать не нужно. Не даёт звёзд, не чаще одного в неделю.
- ⚠️ **Имя ребёнка больше не уходит на сервер** (`CLOUD_SKIP`). Это не мелочь, а
  условие, при котором фраза «о ребёнке мы не храним ничего» остаётся правдой:
  имя рядом с адресом взрослого превратило бы обработку в категорию
  «несовершеннолетние». Появился второй снимок — `localSnapshot()` (с именем,
  для слияния у себя) против `cloudSnapshot()` (без имени, для отправки); без
  этого разделения каждый обмен затирал бы местное имя пустым. Следствие: в
  списке учеников у наставника имени нет, вместо него — подпись, которую он
  заводит сам (`S.admin.labels`, только на его устройстве).
- **Ни alert, ни prompt.** Оба блокируют страницу целиком и не проверяются
  тестом; сообщения рисуются в карточке, подпись вводится прямо в строке списка.
- **Тесты дописаны вместе с механикой**, как требует правило проекта: четыре
  новых раздела в `tests/full-run.js` — честное время (активный тик, простой,
  спрятанная вкладка, слияние часов, обрезка), занятие (детерминированность
  плана, закрытие руками, отчёт, счёт предсказаний), рамка (гейт, каникулы,
  слияние «свежее побеждает»), задание от взрослого (ссылка туда-обратно, ноль
  звёзд, недельный лимит, прогон ВСЕХ шаблонов движком).
- ⚠️ **Чего сознательно нет: автоматической отправки отчёта.** Она требует
  адреса взрослого, то есть домена, почты, политики ПДн и дежурства. Пока отчёт
  показывается в кабинете — это ноль новых персональных данных. Галочка
  «получать отчёты» уже стоит и сохраняется: когда появится отправка, управлять
  ей будет она.
- ⚠️ **Что по-прежнему не измерено и важнее всего построенного:** сколько уроков
  реально влезает в полчаса живого ребёнка. Число уроков в занятии посчитано из
  длины текста урока (5–8 минут), а не снято с ребёнка. Первое же настоящее
  занятие может потребовать переверстать состав.

**Навигация урока, шапка и хвосты (1.37.0).** Сессия началась с трёх хвостов и
закончилась переделкой экрана урока по замечанию фаундера: «нажимаю в редактор —
страница уезжает вниз, ребёнок не поймёт, как вернуться».

- ⚠️ **Раскладку правил ДВАЖДЫ за сессию, второй раз по замечанию фаундера.**
  Сначала справа стояли задание + редактор. Фаундер: «оставь задачу в конце как
  и было, а редактор пусть справа ставится напротив каждой карточки». Стало:
  слева объяснение и задание в конце, справа только верстак. Тут же вылезло
  следствие — «непонятно, как решить задачу справа»: у верстака не осталось
  ни слова о задаче. Ответ — шапка «🎯 Здесь решаешь задачу» с текстом задачи
  одной строкой. И третье, найденное по дороге: кнопка «→ В редактор» затирала
  код задания МОЛЧА; с верстаком напротив каждой карточки её будут нажимать
  постоянно, поэтому теперь она пишет, что подменила код, и даёт «↩ Вернуть
  мою задачу».
- **Экран урока в две колонки.** Замер до правки (урок «Текст и числа»,
  1280×720): страница 2341px, редактор начинался на 1698-м — на 2,4 экрана ниже.
  Щёлкнув в редактор, ребёнок оказывался там, где не видно ни объяснения, ни
  текста задания. Теперь от 1040px слева объяснение, справа задание + редактор +
  подсказки, правая колонка липкая; редактор начинается на 606-м. Уроки с
  рисованием (их четыре) идут одной колонкой — рядом с редактором холст, и на
  540px эта пара не помещается. Подробности — README, «Экран урока».
- **Липкая полоска задания** для узкого экрана: появляется ровно тогда, когда
  карточка задания ушла выше края, показывает текст задания, разворачивается
  тапом и даёт кнопку «↑ Объяснение». ⚠️ Отступ сверху НЕ константа: шапка на
  телефоне двухстрочная (61px против 103px замерено), зашитое число спрятало бы
  полоску под шапкой ровно на том устройстве, ради которого она делалась.
- **Шапка перестала переноситься.** Она влезала в одну строку только пока цифры
  маленькие: с рангом «Гуру» и «★ 300 · 100/100» ей нужно 1061px из 1080, и «?»
  уезжал на вторую строку — у того ребёнка, который дошёл до конца. Перенос
  запрещён, лишнее убирается ступенями, посчитанными замером, проверено на
  1280/1100/1000/900/800/700.
- **Черновики на шагах проектов.** Код шага уезжал в прогресс ТОЛЬКО на победе, а
  до неё жил в разметке: уход за подсказкой стирал написанное. Ключ
  `proj-<id>-<шаг>`, кнопка «Начать шаг заново», сданный шаг черновика за собой
  не оставляет.
- **Сертификаты за разделы вне сотни** — «Разминка» (все упражнения) и «Ты и ИИ»
  (все задания И проект «Напарник»). Сертификатов стало восемь. ⚠️ У разминок и
  «Ты и ИИ» не было метки времени вовсе — они отмечались единицей. Поэтому дата
  выдачи запоминается один раз в `S.certAt`, а при слиянии двух устройств
  берётся ранняя: обычный `mergeSet` схлопнул бы её в единицу, то есть в
  1 января 1970 года на распечатанном листе.
- **Имя ученика в списке наставника** (решение фаундера). Раньше были одни коды.
  ⚠️ Требует ПЕРЕВЫКЛАДКИ Cloud Function — правка в `cloud/index.js`.
- **Контент:** +4 упражнения «Ты и ИИ» (граница «не ниже», «починил одно —
  переписал другое», пустой список, int() вместо округления) и +9 записей
  шпаргалки (115). Мир 5 в шпаргалке по-прежнему почти пуст — причина
  документирована и не изменилась.

- **Разбор примера под кодом, Миры 1 и 2 целиком — 153 штуки.** Замечание
  фаундера «в каждом примере надо более подробное описание» подтвердилось
  замером, и картина оказалась обратной нужной: в Мире 1 у 55 карточек из 72
  объяснение короче 150 знаков, в Мире 2 — у 69 из 81, а в Мире 5 всего у 11
  из 79. Чем МЛАДШЕ ребёнок в курсе, тем меньше ему объясняли. Новое поле
  `note` у карточки: `p` говорит зачем, `note` — что делает вот эта программа,
  с настоящими числами. ⚠️ Держат два теста: `<code>` в разборе обязан
  буквально быть в примере или в его выводе (поймал 7 случаев за сессию), и
  карточка в мире из `NOTES_REQUIRED` не может остаться без разбора.

**Три находки в самих проверках, каждая дороже своей правки:**

- **Программа, падающая НАРОЧНО, вообще не сверялась с python3.** Текст ошибки у
  python3 английский и подробный, у движка свой, поэтому любое падение читалось
  как расхождение — и содержание вроде «ИИ выдумал функцию» оставалось
  непроверенным. Теперь при падении сравниваются три вещи: что напечаталось ДО
  падения, какое это исключение (имена классов у движка совпадают с
  питоновскими — проверено на шести) и на какой строке.
- **Регулярка, которая перестаёт работать ровно когда число меняется.** Проверка
  числа сверок в README искала «сейчас (\d+) сверк» — и на «765 сверок» молча
  перестала находить строку. Теперь «сверо?к».
- **Числа в текстах разъезжались молча**, в том числе «106 команд» в подсказке,
  которую читает ребёнок. Заведена проверка `[числа]` в `tests/lessons.js`:
  берёт число из самих файлов содержания и сверяет с README и с текстом в
  интерфейсе. Файл, где числа нет вовсе, — ошибка тоже: значит проверять нечего.

**Решение фаундера: Pyodide НЕ подключаем.** Спор двух документов закрыт. Цифры,
на которых решение принято: ни один из 100 уроков не просит настоящего Python
(все миры на `engine: "mini"`); мини-движок сверяется с python3 на 774 кусках
содержания при каждом прогоне, то есть «разойдётся незаметно» — не про нас; ядро
Pyodide это 6 МБ по сети и 13 МБ на диске против 1,5 МБ всего сайта (pandas
добавил бы ещё 22 МБ); Flask и сетевые запросы в браузере он всё равно не даёт.
Условие пересмотра: появится тема, которую своим движком не сделать (таблицы,
графики). Заглушка и слой `Runtime` остаются — дверь открыта.

**Редакторский проход по всему, что вне ста уроков (1.36.10).** Прочитал
глазами 21 разминку, 15 упражнений «Ты и ИИ», 5 игр и все 106 записей
шпаргалки. Материал сильный, но нашлось семь вещей — и одна из них
настоящее противоречие внутри курса.

**Главное: курс сам себе противоречил про `//`.**
Урок 4 учил «`//` отбрасывает остаток и оставляет целое». Разминка 2,
открытая ЭТИМ ЖЕ уроком, спрашивает `-7 // 2` — и в подсказке говорит правду:
«`//` округляет ВНИЗ; для отрицательных это не то же самое, что отбросить
дробь». Правильный ответ −4, а по правилу из урока выходит −3. Ребёнок,
выучивший урок, отвечал бы неверно и не понимал почему. Шпаргалка, кстати,
всё это время была права: там так и написано — «округляет ВНИЗ», и пример
с `-7 // 2` на месте.

Починил урок 4 (добавил в пример `-7 // 2` и правильную формулировку), урок 3
(туда я сам перенёс ту же неправду на прошлом проходе) и разбор в
`ai-confident`, где повторялась та же фраза.

**Остальные шесть.**
* «Ты и ИИ», `ai-verdict-ok`: «с пустой строкой пришлось бы держать особый
  случай» — на деле наоборот, особый случай нужен как раз при `слова[0]`
  на пустом списке. Переписано на утверждение, которое верно.
* Игра «Угадай число»: «Ты угадал за 1 попыток» на первой же удаче.
  Заменено на «Попыток: 1» — согласование не нужно вовсе.
* Игра «Пещера дракона»: переменная `идём = True`, которую никто не читает, —
  ровно то, что курс учит не писать и за что ругает его же линтер. Убрана.
* Шпаргалка, `random.randint`: «Каждый запуск разное». В тренажёре как раз
  ОДИНАКОВОЕ — проверил, два отдельных запуска дают одни и те же числа, — и
  урок 37 честно об этом говорит. Формулировка исправлена.
* Шпаргалка, `строка[2:5]`: описание «с 2-го знака по 5-й» спорит с принятым
  в курсе счётом с нуля, а самого `[2:5]` в примере не было. Переписано,
  пример дополнен.
* Шпаргалка, `.startswith() и .endswith()` и `int(), str(), float()`:
  заголовок обещал две-три команды, а пример показывал одну-две. Ребёнок
  искал `startswith` и `float` и не находил.

**Новая проверка (аудит, раздел 1):** заголовок записи шпаргалки обязан быть
показан примером. Кириллические имена в заголовке пропускаются — по
соглашению справочника это заглушки (`def имя(параметры)`,
`class Наследник(Родитель)`), а не настоящие названия. Нашла обе последние
находки, проверена мутацией.

**Грабли:** курс может противоречить сам себе в двух разных файлах, и ни один
тест этого не увидит — оба места по отдельности зелёные. Правило, повторённое
в уроке, разминке, шпаргалке и разборе «Ты и ИИ», надо сверять руками во всех
четырёх местах.

**Проверки доведены до всего содержания, а не только до ста уроков (1.36.9).**
На вопрос «теперь максимально чисто» честный ответ был «нет»: весь редакторский
проход и все новые проверки касались CURRICULUM, а разминки, «Ты и ИИ», игры,
проекты и шпаргалка оставались вне. Прогнал их — и нашёл ровно тот же дефект.

**Найдено и починено: сырой HTML в проекте пятого мира.** Четыре требования
(`<h1>`, `<p>`, `<li><a href>`, `<html>/<head>/<footer>`) браузер ИСПОЛНЯЛ,
а не показывал. Ребёнок читал «Функция главная() возвращает заголовок и абзац»
вместо «возвращает `<h1>…</h1>` и `<p>…</p>»` — то есть условие без того
самого, что от него требуется. Ровно как в уроках 90, 93 и 95, только моя
проверка туда не смотрела.

**Проверка разметки теперь смотрит ВСЁ**: уроки, разминки, «Ты и ИИ», проекты,
игры, шпаргалку. Поля с кодом (`solution`, `demo`, `files`, `data`…) при этом
пропускаются: там теги — данные для python3, а не разметка страницы. Проверено
мутацией.

**Сверка обещанного вывода доведена до шагов проектов.** Там она пока молчит
по делу: требования проектов написаны ОПИСАТЕЛЬНО («напечатай место, команду
и очки»), а не литералами в `<code>`, и это осознанный стиль — литеральные
строки связали бы проект по рукам. Чтобы проверка не оказалась мёртвой
незаметно, в неё встроен сторож: не загрузились проекты — она об этом скажет.
Раньше `audit.js` вообще не подключал `js/projects.js`, и ветка молча
проверяла пустой список.

**Разово сверил литералы в «ёлочках» по всем шагам проектов** (16 штук):
два расхождения, оба намеренные — «GET /путь → код» это шаблон с заглушками,
а «1) уроки: 60» это нарочно НЕПРАВИЛЬНЫЙ формат, который ребёнок должен
заметить. Постоянной проверкой не делал: правило по «ёлочкам» требует словаря
слов-заглушек и начнёт врать.

**Грабли:** проверка, которая молча получила пустой список данных, выглядит
зелёной и не проверяет ничего. Это второй такой случай за две сессии (первый —
`offsetParent` в jsdom). Пишешь проверку — убедись, что данные до неё доехали.

**«Всё чисто?» — посчитано, а не сказано (1.36.8).** Единственное место, где
оставалась неопределённость, — ревью кода: аудит показывал «19 программ
с находками», и по этому числу нельзя было понять, споткнётся ребёнок или нет.

**Почему число было бесполезным.** `audit:lint` прогоняет правила без гейтов
(`all: true`) — в списке оказывались и советы, закрытые по прогрессу, которых
ребёнок в этом уроке не увидит физически. Теперь аудит печатает второй раздел —
**«Что ребёнок увидит на эталоне урока»**: те же находки, но с учётом гейта
(`lintKnows`) и конфликтов с `needCode`, то есть ровно то, что покажется на
экране. И отдельно — сколько из них на РЕШЕНИИ, то есть на коде, который
ребёнок пишет сам.

**Итог: видимых советов 14, на своём коде ребёнка — 1.** Тринадцать из
четырнадцати — на примерах теории: они попадут в редактор, только если ребёнок
нажал «→ В редактор», и советуют ему тогда про чужой код. Единственная находка
на решении — урок 18: эталон пишет `count = count * 2`, ревью предлагает
`count *= 2`. Это не противоречие: подсказка ТОГО ЖЕ урока дословно говорит
«Удвоение: count = count * 2 (или короче count *= 2)». Оставлено намеренно —
менять эталон ради обнуления счётчика было бы подгонкой.

**Правило на будущее:** добавил урок — смотри не на «программ с находками»,
а на строку «из них на СВОЁМ коде ребёнка». Ноль там — то, к чему надо
стремиться; всё остальное в выводе справочное.

**Перепроверка всего и правила на будущее (1.36.7).** Фаундер спросил прямо:
всё ли перепроверено, записаны ли правила, не споткнётся ли ребёнок, всё ли
логично. Проверил каждый вопрос по отдельности — и нашёл ещё четыре вещи.

**1. Ревью кода врало на решениях самого курса.** Правило «три одинаковые
строки» считало, сколько раз строка встретилась в файле ВООБЩЕ. В уроке 21
три `print(queue)` стоят между тремя РАЗНЫМИ действиями со списком, в уроке 55
три `return self.coins` лежат в разных методах — циклом это не сворачивается.
Ребёнок писал правильный ответ, жал «🧹 Ревью кода» и получал замечание за
верный код. Правило переписано: ищет повторяющийся КУСОК из 1–4 строк ПОДРЯД.
Настоящий случай (`forward(120)` / `right(120)` три раза в уроке 9) ловится,
ложные — нет. Программ с находками стало 19 вместо 30. Проверено живьём:
урок 21, эталонный ответ, «Ревью кода» → «Чисто».

**2. Урок 51 предлагал править то, что просил не трогать.** Ревью советовало
`right += 1` на строке из заготовки, а условие говорит «исправь ОДНУ строку,
остальное не переписывай». Заготовка и решение теперь сразу с `+=`.

**3. Проверка «первая подсказка выдаёт решение» была слепа на многофайловых
уроках** — она смотрела в `task.solution` (это `main.py`, который ребёнок и не
трогает), а работа лежит в `task.files[].solution`. Заодно доля считалась от
ВСЕГО решения вместе с выданной обвязкой, из-за чего порог пришлось когда-то
занизить до трети. Теперь знаменатель — только то, что ребёнок правда пишет
(решение минус заготовка), а порог стоит на половине, как и написано словами
в самом правиле. И одна строка больше не считается дважды, когда короткая
целиком лежит внутри длинной.

**4. Три числа в README разошлись с делом.** Корпус движка вырос до 151, сверок
с python3 — 752, корпус линтера — 521, а в тексте стояли 149, 730 и 522.
Исправлено — и теперь этого больше не случится молча: каждое из трёх чисел
сверяет тот тест, который его и считает. Поправил корпус — `npm test`
покраснеет и назовёт число.

**Заодно записаны в «грабли» одиннадцать правил** (пункты 50–60): теги
в тексте урока, пробелы в `<code>` как обещание, литеральное обещание
в `<code>`, работа многофайлового задания в `task.files[].solution`,
«что выдано в заготовке — ребёнок не пишет», однобуквенные кириллические
имена, урок 81 как место ввода русских имён, числа README под тестом,
жёсткий цвет ломает светлую тему, `offsetParent` в jsdom, цвет доски из
`--board`.

**Чего проверки по-прежнему не видят** (честно, чтобы не создавать ложного
покоя): интересно ли ребёнку, верна ли кривая сложности, не скучен ли урок.
И урок 1 по-прежнему проходится печатью ответа — но он про `print`, там это
и есть задание.

**Курс вычитан целиком: миры 3, 4, 5 плюс всё отложенное (1.36.6).**
Все сто уроков прочитаны глазами. Отложенное из прошлых проходов сделано.

**Отложенное — сделано.**
1. **Уроки 9 и 10.** Урок 10 повторял урок 9 на три четверти. Из урока 9
   убраны блоки «Цвет и толщина» и «Поднять карандаш» (они дословно есть
   в уроке 10), вместо них — «Куда смотрит черепашка» (стартовое направление
   и повороты) и короткий «Цвет и толщина ставятся сверху» с явной отсылкой:
   подробности в следующем уроке. Задача урока 9 не пострадала: `color`
   и `width` там и так стоят готовыми в заготовке.
2. **Урок 6.** Блок «Что умеют строки» удалён: он был анонсом уроков 7 и 8,
   его текст дословно повторял лид урока 7, а `word[0]` появлялся там без
   объяснения (объясняется только в уроке 8).
3. **Урок 11.** В требование добавлено, что «x» в формате — латинская:
   русская «х» выглядит так же, и в разборе расхождения две строки были бы
   неотличимы глазом.
4. **Урок 26.** `needCode:["{"]` был беззубым — скобка уже стоит в заготовке
   `hero = {}`. Теперь `["hero[", ".get("]`: словарь нужно правда наполнить.
5. **Уроки 32 и 33.** Кириллические `с` и `о` в примерах заменены на `w`
   и `m`: они неотличимы от латинских `c` и `o`, и ребёнок, перепечатывающий
   пример, получал NameError без понятной причины.
6. **Урок 35.** Третье требование просило `lambda` там, где хватает
   `key=len`. Теперь так и написано — и это отдельная мысль: не всякий
   признак стоит заворачивать в lambda.

**Мир 3 «Свой код» — сильный, одна находка.**
**Урок 48.** Теория возила `tools.py` с готовой `clean()`, а задание просило
написать ровно её. Примеры 1 и 2 переписаны на `greet`/`frame`.
Заодно обещание вывода в уроке 42 переведено в `<code>` — теперь его сверяет
аудит.

**Мир 4 «Настоящий Python» — отличный, одна находка.**
**Урок 69.** Два требования говорили одно и то же разными словами. Сведены
в одно.

**Мир 5 «Профессия» — две находки.**
1. **Уроки 90, 93, 95: сырой HTML в требованиях.** `<p>`, `<h1>`, `<html>`
   в условии — это КОД, который ребёнок обязан вернуть из функции. Но текст
   урока попадает на страницу как HTML, и браузер эти теги ИСПОЛНЯЛ: ребёнок
   видел условие без того самого, что от него требовалось. Экранировано.
2. **Урок 81: русские имена переменных.** С этого мира весь код написан
   кириллицей (`комната`, `предмет`, `ПРЕДМЕТЫ`) — и нигде за 80 уроков не
   было сказано, что Python так умеет. Добавлено во вступление урока 81,
   с честной оговоркой: в настоящей работе код принято писать по-английски,
   но это привычка людей, а не правило языка.

**Замечание, которое я не трогал.** Урок 52 в эталоне пишет
`assert strong(...) == True`. Свой же линтер курса считает сравнение с `True`
лишним — правда, внутри `assert` оно у него не срабатывает, так что ребёнок
противоречия не увидит. Пара `== True` / `== False` симметрична и читается
понятнее, чем `assert not ...`, поэтому оставил как есть.

**Проверок стало больше.**
* Проверка 6 в аудите переписана до конца. Теперь она смотрит и **файлы
  модуля** рядом с примером (`t.files`), и файлы задания, **пропускает то,
  что выдано в заготовке** (класс `Fighter` в уроке 59 повторять в теории
  нужно), и снижает порог до двух строк, если условие ПРЯМО называет функцию.
  На чистом курсе — ноль находок, на мутации ловит.
* Новая проверка в `lessons.js`: в тексте урока разрешены только теги
  оформления (`<b>`, `<code>`, `<br>`…). Всё остальное — литерал, и его надо
  писать через `&lt;`. Это жёсткая проверка, а не аудит: ребёнок видит не тот
  текст, и заметить это нельзя.

**Редакторский проход по Миру 2 «Данные» (1.36.5).** Прочитал все двадцать
уроков (21–40) глазами, как Мир 1.

**Мир сильный.** Порядок тем правильный: список → срез → сортировка → сетка →
проект, потом словарь → перебор → счётчик → множества → кортежи → вложенность,
потом включения тремя шагами, lambda, три проекта и босс. Ловушка
`[["."] * 3] * 3` в уроке 24 разобрана образцово: сначала показано, как
ломается, потом почему, потом как правильно. Урок 25 даёт каркас поля, но
НЕ даёт условие для стен — ребёнок дописывает сам. Так и надо.

**Починено — и оба раза причина оказалась одна.**

1. **Урок 37 «Случайность».** Условие обещало `Всего: 37`, а тренажёр всегда
   печатает `Всего: 44` (случайность тут детерминированная — об этом прямо
   сказано в блоке 4 того же урока). Заодно условие говорило «числа будут
   свои», что блоку 4 противоречит. Конкретное число убрано.
2. **Урок 40, босс.** Условие показывало `Вика␣␣␣␣9`, а печатается
   `Вика␣␣␣␣␣9`: имя дополняется до ширины 8, и между именем и числом ещё
   один пробел.
3. **Причина, общая с Миром 1: `<code>` схлопывал пробелы.** HTML сжимает
   любую цепочку пробелов в один, поэтому показать выравнивание в условии
   было НЕЛЬЗЯ в принципе. И это же било по уроку 7 первого мира: условие
   обещало `raw = "␣иван␣петров␣"`, а в редакторе лежит
   `raw = "␣␣иван␣␣␣петров␣␣"` — при том что весь смысл задания как раз
   в лишних пробелах. Починено стилями: `white-space: pre-wrap` у `<code>`
   в условиях. Проверено на живой странице: урок 7 теперь показывает ровно
   то, что в редакторе.

**Не трогал — на усмотрение фаундера.**
* Уроки 32 и 33 пишут переменные кириллицей (`сырое`, `с`, `оценки`, `о`)
  посреди мира, где всё остальное по-английски. Хуже того, `с` и `о`
  визуально неотличимы от латинских `c` и `o`: ребёнок, перепечатывающий
  пример, получит NameError и не поймёт почему.
* Урок 35 требует `key=lambda name: len(name)` там, где хватает `key=len`
  из урока 23. Ради практики lambda — но выглядит как антипаттерн, и свой же
  линтер курса такие вещи ловит.
* Урок 26: `needCode: ["{"]` беззубый — фигурная скобка уже стоит в заготовке
  (`hero = {}`), так что условие «построй словарь» ничем не подкреплено.

**Проверок стало больше.** Аудит теперь сверяет обещанные пробелы дословно
и отдельно ругается, когда строка та же, а пробелы другие. А тест следит,
чтобы стили не начали схлопывать пробелы снова, — иначе выравнивание
в условиях опять станет ложью, и заметить это будет нельзя. Обе проверены
мутациями.

**Редакторский проход по Миру 1: прочитал все 20 уроков глазами (1.36.4).**
Это то, что не выражается порогом: понятно ли условие, к месту ли пример,
не повторяет ли урок предыдущий. Мир 1 выбран потому, что с него начинают все
и цена непонятного урока там выше всего.

**Общее впечатление: мир написан хорошо.** Структура ровная (3–4 блока теории,
3 требования, 3 подсказки на урок), тон правильный, разбор ошибок в уроке 5
на месте, ловушка FizzBuzz в уроке 15 подстроена нарочно и обезврежена
комментарием в заготовке. Нашлось три дефекта и одна структурная претензия.

**Починено.**

1. **Урок 3.** Блок назывался «Пять действий с числами», а в примере их
   шесть: там же стоит `7 // 3`. Целое деление не названо ни словом и
   объясняется только в уроке 4 — ребёнок запускал пример, видел `2` и не
   понимал, откуда оно. Заголовок и текст исправлены.
2. **Урок 16.** `True` и `False` печатаются в трёх примерах из четырёх и
   лежат в переменных (`ticket = True`, `rain = False`), а что это такое, не
   сказано НИГДЕ — ни здесь, ни раньше. Текст говорит «верно» и «ложь»
   по-русски, экран показывает `True` по-английски, и связать это ребёнок
   должен был сам. Добавлена строка во вступление.
3. **Урок 20, босс мира.** Задание просит написать функцию `square(size)` —
   а она уже лежала в теории того же урока слово в слово. Босс проходился
   копированием сверху вниз. Пример в теории заменён на `triangle(size)`:
   идея та же, переносить её теперь надо руками.

**Не починено — это решение фаундера.**

**Урок 10 «Цвет и перо» на три четверти повторяет урок 9 «Черепашка».**
В уроке 9 блоки 2 и 3 — это палитра, толщина и `penup`/`pendown` с полным
списком цветов и диапазоном 1–20. Ровно то же самое, с той же глубиной,
составляет блоки 1–3 урока 10. Нового в уроке 10 только `goto`/`home`,
`brown`/`black` и русские названия цветов. Ребёнок читает одно и то же дважды.
Предложение: оставить в уроке 9 только движение (`forward`, `right`, `left`),
а цвет, толщину и перо целиком отдать уроку 10. Задача урока 9 от этого не
страдает: `color` и `width` там и так стоят готовыми в заготовке. Но это
перекройка двух уроков, и делать её без слова фаундера я не стал.

**Мелочи, которые я НЕ трогал (на его усмотрение).**
* Урок 6, блок 4 «Что умеют строки» — это анонс уроков 7 и 8 целиком, причём
  `word[0]` (обращение по номеру) появляется там без объяснения, а объясняется
  только в уроке 8.
* Урок 11 требует формат `7 x 1 = 7` с латинской «x». Кириллическая «х»
  выглядит точно так же, и в разборе расхождения две строки будут
  неотличимы глазом. Риск невелик (f-строка и так набирается в английской
  раскладке), но он есть.

**Заодно усилена проверка 6 в `tests/audit.js`.** Она ловила только «решение
почти равно примеру» и не видела случая, когда пример лежит ВНУТРИ решения —
как раз босса мира 1. Пробовал два признака и оба выбросил: «N строк подряд»
дал три ложные находки на переиспользуемых идиомах, «совпало имя функции» —
шестьдесят (`__init__`, `main`, `inner` повторяются в каждом втором уроке про
классы). Работает третий: совпало И имя, И тело целиком. После правки урока 20
проверка показывает 10 находок в мирах 3–5 — их надо смотреть глазами
отдельным проходом, это не дефекты по определению.

**Добрал четыре хвоста и нашёл ошибку в уроке 39 (1.36.3).** После 1.36.2
оставалось четыре непроверенные вещи. Проверил все.

**1. Офлайн — проверен по-настоящему.** Раньше я только смотрел, что файлы
лежат в кэше. Теперь: открыл сайт, ОСТАНОВИЛ сервер, перезагрузил страницу —
урок открылся, код выполнился, вывод верный. Работает.

**2. Клавиатура и экранный диктор в окне помощи — не проверено, а починено.**
Фокус в окно не уходил вовсе. Теперь при открытии он встаёт на само окно
(не на «✕»: иначе диктор первым делом читает «Закрыть»), Tab ходит по кругу
внутри, Esc закрывает и ВОЗВРАЩАЕТ фокус на «?». Есть `aria-expanded`.
Отдельная проверка держит всё это, поймала обе мутации.
Грабли, на которые наступил: фильтр по `offsetParent` молча выключал ловушку
фокуса в jsdom — там нет раскладки, и `offsetParent` всегда null. Проверка
была зелёной и не проверяла ничего. Фильтр убран.

**3. Печать.** Убедился, что на бумагу уходит только сертификат: вытащил блок
`@media print` из живой таблицы стилей, применил его к экрану и посмотрел —
шапки, кнопок, окна помощи нет, лист чистый. Заодно убрал своё же дублирующее
правило: `body>*{display:none}` и так закрывает всё, что лежит в body. Это
теперь проверка: правило обязано остаться ОДНИМ, а не превратиться в список.

**4. Содержание уроков — дописал два аудита, и один нашёл настоящую ошибку.**

Урок 39 «Проект: разбор песни» полгода обещал в условии `Слов: 12`,
`Разных: 10` и `Самая длинная строка: счастливые друзья`. Эталон печатает
**11, 9 и «мы едем едем едем»**. Три обещания из пяти — неправда. Тесты этого
не видели и не могли: они сверяют вывод ребёнка с ЭТАЛОНОМ, а не с текстом
условия. А ребёнок видел: написал правильную программу, получил другое число
и решил, что ошибся он.

Вдобавок вскрылась ловушка: самых длинных строк в песне ДВЕ, обе по 17 букв.
`max` берёт первую, `sorted(...)[-1]` — последнюю. Ребёнок с правильным кодом
мог не сойтись с эталоном. Числа исправлены, а про две одинаковые строки
теперь сказано прямо в условии — это и есть, чему тут стоит научиться.

Проверка 5 в `tests/audit.js` сверяет только то, что урок обещает ДОСЛОВНО
(куски в `<code>`). Первая версия сверяла все числа подряд и выдала 54 пустых
находки на нормальных уроках — «напечатай `строк:` и число строк» это
описание, а не обещание. Граница между ними и есть вся проверка.
Проверка 6 (задание списано с примера теории) чиста; сначала она ругалась на
урок 28, но там задание «починить» — у него эталон и обязан быть похож на
правильный пример. Исключение прописано.

**Подсказки врали в семи местах — вычитано и закрыто проверкой (1.36.2).**
Проверка 1.36.1 показала, что КОД работает. Но тексты подсказок писал я, и их
никто не сверял с тем, что на экране на самом деле. Сверил — нашлось семь
расхождений, и каждое ребёнок поймал бы раньше меня:

1. **Визуализатор** объяснялся через кнопки «⏭» и «⏮», которых там нет.
   Настоящие — «⏮ в начало», «◀ Назад», «▶ Играть», «Вперёд ▶». И главное:
   перематывать нечего, пока не нажал «▶ Показать по шагам», — про это в
   тексте не было ни слова.
2. **Игра.** Было «пиши код и жми ▶ Запустить — ход засчитается». На деле
   ходы пишутся в поле под игрой и подтверждаются Enter, а кнопка называется
   «▶ Новая игра».
3. **Сроки повтора.** Было «через день, потом через три, потом через неделю».
   В коде `REVIEW_STEPS = [2, 7, 21]` — через два дня, через неделю, через три.
4. **Одна звезда.** Было «★ — сдан». Неправда: одна звезда даётся только за
   ПОКАЗАННОЕ решение, а сколько бы попыток ни ушло, ниже двух не опускается.
   В прежней формулировке ребёнок боялся лишней попытки зря.
5. **Ранги.** Было «Новичок → … → Мастер». Мастер шестой из восьми, последний —
   Гуру.
6. **Панель наставника.** В куске для родителя стояло «в профиле есть панель
   наставника». В профиле её нет: она открывается адресом с `#admin` и просит
   код. Родитель искал бы кнопку, которой не существует.
7. **Задание от друга.** Было «на прогресс не влияет». Влияет: за решённое
   чужое задание идёт опыт и бейдж, не начисляется только за своё же.

Плюс уточнено: щит появляется за каждые пять дней (было «за несколько»), у
своего задания есть ещё поле «Название», подсказка в разминке бесплатная,
и названа кнопка «🔍 Разобрать» — она есть и на уроке, и в песочнице.

**Что из этого важно на будущее.** Такую ошибку не ловит ни один тест на
работу кода: код исправен, врёт текст. Поэтому появилась отдельная проверка:
из каждой подсказки берётся всё, что взято в «кавычки» и похоже на кнопку
(есть значок ▶ ✓ 💡 …), рисуется тот самый экран, и кнопка обязана на нём
быть. Плюс числа: сроки повтора, правило щита и последний ранг сверяются с
константами кода. Проверка проверена мутациями — все три подмены поймала.

**Правило:** правишь текст подсказки — назови кнопку ровно так, как она
подписана на экране. Меняешь константу (`REVIEW_STEPS`, `SHIELD_EVERY`,
`RANKS`) — ищи её словами в `HELP` и в `screenGuide`.

**Проверка после выпуска светлой темы — три находки (1.36.1).** Прогнал всё:
тесты, обход двадцати одного экрана в обеих темах, сквозное прохождение урока,
телефонная раскладка, service worker, живой сайт. Ошибок JavaScript ноль,
все экраны рисуются, помощь на каждом своя. Нашлось три вещи, и все три
починены.

1. **Холст черепашки спорил сам с собой.** В CSS доска была светлой (по теме),
   а `drawTurtle` заливал её `#070a16` числом в коде: до первого запуска доска
   светлая, после — тёмная. Теперь цвет один и живёт в стилях (`--board`),
   а рисование берёт его оттуда. Доска остаётся ТЁМНОЙ в обеих темах, и это
   нарочно: палитра курса под неё и подобрана, `color("white")` на белом
   просто исчез бы.
2. **Приглушение закрытых уроков не пережило смену темы.** `opacity:.42` на
   тёмном съедает свечение, на светлом — контраст: закрытый урок стал еле
   видимой полоской. Вынес в `--dim` / `--dim2`, на светлой теме приглушаем
   слабее. Касается и бейджей, и бестиария, и закрытых игр, и выдачи поиска.
3. **Контраст светлой темы посчитал цифрами.** Номера строк в редакторе давали
   1.99:1 — почти не читались; комментарии в коде 2.93:1. Подвинул `--gutink`,
   `--c`, `--faint`, `--gold`, `--warn`: теперь ничего ниже ≈3.4:1, основное
   выше 4.4:1.

**Светлая тема, помощь «?» и поиск по урокам (1.36.0).** Фаундер сказал: фон
очень тёмный, и вообще «добавь удобства — подсказки, инструкции, как этим
пользоваться; хорошо подходят вопросики в правом верхнем углу с раскрывающимся
окном». Сделано четыре вещи.

**1. Две темы, светлая по умолчанию.** Это была не пятиминутная правка:
половина цветов стояла ПРЯМО В ПРАВИЛАХ (`background:#0f1428`), а жёсткий цвет
не переопределить ничем. Всё, что меняется между темами, вынесено в переменные
(около шестидесяти имён), и обе темы описаны одним набором: `:root` — светлая,
`:root[data-theme="dark"]` — тёмная. Тема ставится маленьким скриптом в `<head>`
ДО первой отрисовки, иначе у выбравшего тёмную мелькает светлый фон. Выбор
живёт в `localStorage` и НЕ синхронизируется с сервером — это настройка
устройства, как и снятые замки. Черепашку `themeSet` перерисовывает руками:
холст стилями не перекрасить.
**Грабли:** новый жёсткий цвет в правиле ломает светлую тему незаметно. Тест
ищет вбитые тёмные цвета в собранном файле и падает на них.

**2. Кнопка «?» — пятый инструмент в панели.** До этого объяснение «что тут
происходит» было ровно одно — блок «Как устроен урок» на Главном, и тот
исчезал навсегда после первого пройденного урока. Теперь «?» в правом верхнем
углу открывает всплывающее окно, и текст в нём МЕНЯЕТСЯ вместе с экраном
(два десятка мест: урок, мир, визуализатор, «Сегодня», «Повторить», «Своё
задание» и так далее). Тексты лежат в `HELP` в `js/app.js`. Где мы находимся —
отдельное поле `curPlace`, которое ставит `enterScreen(tab, place)`: вкладок
три, а экранов под ними два десятка, и `curTab` для этого не годится.
У заголовков «Уроки», «Тренировки», «Моё», «Достижения» стоят кружки «?» —
тот же механизм, но сразу на нужной теме.
**Важно:** в панели теперь ПЯТЬ инструментов, и тест разрешает ровно пять.
Шестого быть не должно — это был единственный случай, на который хватило
причины. Новый экран без своей подсказки — забытая работа, тест ругается.

**3. Экран «Как пользоваться» (`screenGuide`, адрес `#help`).** Полная
инструкция: что это такое, первый час по шагам, что делают все кнопки наверху,
звёзды и ранги, что делать, когда не получается, кнопки клавиатуры,
оформление, сохраняется ли прогресс и кусок ДЛЯ РОДИТЕЛЯ. Всплывающее «?»
отвечает «что тут прямо сейчас», инструкция — «как всё устроено вообще».
Ссылки на неё: из помощи, из профиля, из блока для новичка на Главном.

**4. Поиск по урокам на Главном.** Сто уроков в пяти мирах, «где было про
словари» искалось глазами по пяти экранам. Ищет по названию, подзаголовку,
теме мира и номеру, Enter открывает первый найденный. Закрытые уроки из выдачи
НЕ прячутся (спрятать — соврать «такого нет»), но и открыть их оттуда нельзя
(пустить — сломать порядок, на котором держится курс): строка видна и помечена
замком. На это есть проверка.

Плюс мелочь: кнопка «↑ наверх», которая появляется после экрана прокрутки.

Всё проверено: `npm test` зелёный, `npm run audit` без новых находок,
и живьём — светлая и тёмная тема, урок с редактором, помощь на четырёх
экранах, телефонная раскладка.

**Пустой экран после обновления — найдено и закрыто (1.35.0).** Сразу после
выпуска 1.34.0 сайт у фаундера открылся пустым: шапка есть, под ней ничего.

**Причина.** Рассинхрон файлов при обновлении. GitHub Pages отдаёт статику с
`max-age=600`, браузер взял из своего кэша СТАРЫЙ `js/app.js` к уже НОВОЙ
странице. В старом коде запуск начинался строкой
`document.getElementById("btn-map").onclick = screenWorlds;` — а кнопки
`btn-map` в новой шапке нет. Падение на первой же строке, и до отрисовки дело
не дошло. Воспроизвёл в jsdom: новая разметка + старый скрипт = шапка есть,
экран пуст. Ровно то, что было на скриншоте.

**Что сделано.**

1. `allWorldsContent().then(bootRender, bootRender)` — отрисовка больше не
   висит на одном обещании без `catch`.
2. `bootFallback` — если экран остался пустым, ребёнок видит слова и кнопку
   «Обновить» (чистит кэши и перезагружает). Зовётся из `try/catch` вокруг
   отрисовки И из `window.onerror`, то есть на ЛЮБУЮ ошибку старта.
3. `sw.js` просит у сети свежее (`cache: "no-cache"`) вместо ответа из
   HTTP-кэша браузера.
4. Имя кэша теперь с версией выпуска (`kodokvest-1.35.0`), старые чистятся при
   активации. Тест сверяет версию в `sw.js` с `package.json` — разъехались,
   проверка падает.

**Правило на будущее (в грабли):** всё, что в коде запуска трогает разметку,
пишется с проверкой `if (b)`. Одна переименованная кнопка иначе роняет старт
целиком — и не у того, кто правит, а у того, у кого в кэше вчерашний файл.

**Что сказать фаундеру, если пустой экран увидит кто-то ещё:** Cmd+Shift+R
(жёсткая перезагрузка) или подождать десять минут — кэш протухнет сам.
С 1.35.0 вместо пустоты будет сообщение с кнопкой.

**Пересобрано устройство сайта: три вкладки вместо одиннадцати кнопок (1.34.0).**
Фаундер сказал прямо: «пока не понятно что зачем и почему, просто набор
каких-то продуктов». Так и было — полгода разделы добавлялись по одному, и
верхняя панель стала плоским списком из одиннадцати равных кнопок, где урок
и игра, шпаргалка и профиль весили одинаково.

**Что сделано.** Наверху осталось только «куда идти»: 🏠 Главное · 🎯 Тренировки ·
🎒 Моё, а справа четыре инструмента (огонёк дня, шпаргалка, профиль, фокус).
Главное отвечает на четыре вопроса по порядку — что делать сейчас, как устроен
урок, где уроки, что тут ещё есть и зачем. Появился экран «Тренировки», где у
каждого из пяти разделов написано «зачем это» и «когда сюда заходить».
«Моё» — это портфолио, куда добавлены свои задания (были только на своём
экране). Подробно — в `README.md`, раздел «Как устроен сайт».

**Что важно знать перед следующей правкой.**

1. **Вкладка светится по экрану, а не по клику.** Экран сообщает свой раздел
   сам: `enterScreen("train")`, `enterScreen("mine")`, `enterScreen(null)` для
   профиля и регистрации. Урок и проект заходят через `claimScreen`, поэтому
   там `curTab` ставится вручную. Заводишь новый экран — реши, чей он.
2. **Панель обязана влезать в ОДНУ строку.** Она ограничена 1080 пикселями и
   шире не становится, так что «на большом мониторе поместится» — неправда:
   не поместится никогда. Каждый новый элемент придётся чем-то оплатить.
   Уже оплачено: число опыта убрано из панели (осталось подсказкой на ранге
   и в профиле), имя убрано с кнопки 👤. Проверка в тестах не даёт довести
   инструменты больше чем до четырёх.
3. **Объяснения показываются один раз в жизни.** Блок «Как устроен урок» на
   Главном и полоска «что дальше» на уроке живут, пока не пройден ни один
   урок. Условие именно такое, а не «это первый урок»: ребёнок может начать
   не с начала, если наставник снял замки.
4. **Ни один экран не удалён и не переписан.** Изменилось только то, как до них
   добираются: все адреса (`#games`, `#viz`, `#folio`…) работают, добавился
   `#train`. Старые кнопки панели убраны, их обработчики — тоже, и на это есть
   отдельная проверка.

**Мутации проверены:** вкладка перестаёт следовать за экраном — тест падает
шестью строками с именами разделов; объяснение новичку показывается всегда —
падает. `npm test` и `npm run audit` зелёные.

**Что стоит посмотреть глазами ребёнка, когда он сядет заниматься:** понятно ли
из «Сейчас», что делать; читает ли он вообще «Как устроен урок» или жмёт
кнопку сразу; ходит ли в «Тренировки» или они так и останутся вкладкой,
в которую заходят один раз. Это те вопросы, на которые ответит только
наблюдение, а не ещё один экран.

**Шесть последних идей из списка — закрыт весь список (1.33.0).** Пересказ
программы словами, галерея рисунков, файл `.py`, панель символов на телефоне,
установка на домашний экран (PWA) и «вопрос за ужином». Подробности каждой —
в `README.md`, здесь только то, что стоит знать перед правкой.

**Появились новые файлы в корне:** `manifest.webmanifest`, `icon.svg`, `sw.js`
и `tests/lint-check.js` (последний — из 1.32.0). При коммите не забудь
`git add`: их ещё не было в индексе.

**1. Пересказ программы (в визуализаторе).** Структура берётся из дерева, факты
(сколько раз, что напечатано, чему стало равно) — из шагов. Один проход по
шагам теперь кормит и пересказ, и приписки значений в редакторе: это `stepFacts`.
Раньше петля жила внутри `watchCompute`, и вторая петля разошлась бы с первой
при первой же правке.
*Грабля, на которую я наступил:* `print(площадь(3, 4))` печатает не на своей
строке — вывод в шагах достаётся строке ВНУТРИ функции. Поэтому там сказано
«напечатал результат вызова», а «пустую строку» — только когда в печати правда
нет вызовов. И строка, где прогон оборвался, называется прямо, иначе про
`print(10 / 0)` вышло бы «напечатал пустую строку».

**2. Галерея рисунков.** Храним ПРОГРАММУ, не картинку: 154 байта против 26 КБ
в base64 (замерено), а прогресс уезжает на сервер целиком. Плюс правило
проекта: результат вычисляется, а не хранится. PNG делается из холста в момент,
когда его просят. Название — первый комментарий программы (заодно повод их
писать). `S.gallery` лежит в `KEEP_ON_RESET` рядом с `games` и `mytasks`.

**3. Файл `.py`.** Рисующей программе дописываются `from turtle import *`
и `done()` — без них у ребёнка на компьютере она не пойдёт. Рисует ли
программа, решает дерево (`pyIsDraw`), а не поиск слова: `forward` в строке
ничего не рисует.
*Мутация показала дефект в самом тесте:* проверка искала «from turtle import *»
в тексте файла, а в шапке есть объясняющий КОММЕНТАРИЙ с этой же фразой — и
успокаивалась, даже когда самой строки кода не было. Теперь ищется строка.

**4. Панель символов на телефоне.** Пары `()` `""` `[]` ставят курсор внутрь,
«⇥ отступ» — четыре пробела. В атрибут кнопки пишется НОМЕР ключа: среди знаков
есть кавычка, а `esc()` кавычки не экранирует — разметка порвалась бы (ту же
грабку уже находили на форме «своего задания»; тест её и поймал).

**5. PWA.** Стратегия кэша — **«сначала сеть, кэш запасной»**, осознанно
наоборот к обычному «сначала кэш»: тренажёр правится почти каждый день, и
иначе ребёнок неделями сидел бы на старой версии. Офлайн проверен по-настоящему:
сервер выключен, страница перезагружена — игра поднялась целиком, урок открылся,
код выполнился.
**Что нельзя забыть:** добавляешь файл, который грузит `index.html`, — допиши
его в список `SHELL` в `sw.js`. Тест проверяет это в обе стороны и падает
с именем файла. Ссылки на манифест и иконку `build.js` из dist вырезает: тот
файл обещает «ничего не грузится извне».
Иконка одна, SVG: Android и десктоп берут её, iOS рисует свою. Пушей для веба
на iOS нет — напоминания живут только внутри сайта, как и раньше.

**6. Вопрос за ужином.** Берётся из шпаргалки (`sig` + `what` уже сверены с
python3), только из пройденного, детерминирован по дате. Сто вопросов руками
писать не понадобилось.

**Тесты:** шесть новых разделов в `tests/full-run.js`. Мутации проверены на
PWA-инварианте (убрал файл из кэша — падает с именем) и на файле `.py`.
`npm test` и `npm run audit` зелёные.

**Дальше — не придумывать, а смотреть.** Список идей из 1.30.0 закрыт целиком,
и ни одна из новых механик не видела живого ученика. Прежде чем строить
следующие, стоит дать ребёнку позаниматься: часть из этих десяти может
оказаться ненужной, а нужное окажется тем, чего в списке не было.

**Ревью кода: свой линтер по дереву (1.32.0).** Последняя большая идея из
списка и единственная тема курса, которой не было вовсе, — рефакторинг.
Кнопка «🧹 Ревью кода» рядом с подсказками на уроке (и в панели песочницы),
одиннадцать правил, разбор по AST через `window.MiniPy.parse`.

**Главное, что стоит знать, если будешь править правила.**

1. **Инструмент важнее самих правил.** `npm run audit:lint`
   (`tests/lint-check.js`) прогоняет ревью по ВСЕМ 522 эталонным программам
   автора и печатает каждую находку. Первый прогон: **75 сработавших правил,
   и почти всё — ложь.** Шесть ложных правил найдены только так:
   имя внутри f-строки (парсер оставляет `{имя}` текстом и разбирает лишь при
   выполнении — правило считало переменную лишней), имя внутри ФОРМАТА
   f-строки (`f"{полоска:<{ширина}}"`), поле класса (`hp = 10` читают через
   `self.hp`), числа внутри списка данных, `assert x == True` (это запись
   ожидания, а не условие), распаковка кортежа. Добавляешь правило — прогони
   инструмент ДО того, как показывать ребёнку.
2. **Гейты по прогрессу обязательны.** У каждой находки есть `after` — урок,
   после которого совет имеет право появиться. Совет «возьми sum()» на пятом
   уроке это ровно та дыра, которую в курсе закрывали дважды. Тест проверяет,
   что каждый гейт — настоящий id урока.
3. **Совет не спорит с требованием урока.** В разбор уезжает `check.needCode`;
   находка с меткой `conflict` не показывается, если требование её запрещает
   («возьми sum()» на уроке, который ТРЕБУЕТ цикл, — подстава).
4. **Многофайловому уроку ревью не даётся.** Константа из модуля читается
   другим файлом, разбор видит один — и посчитал бы её лишней. Кнопки там нет,
   а инструмент такие файлы пропускает и пишет об этом.
5. **Показываем не больше трёх советов** (`LINT_MAX`) и никогда не отнимаем
   звёзд. В победной карточке — только приглашение с числом находок: карточка
   и без него из шести блоков.

**Осталось законных находок:** 13 правил на 522 программах — три одинаковые
строки у черепашки (до урока про циклы совет закрыт гейтом), накопление в цикле
в примерах, которые этому и учат, `26` в проекте про шифр Цезаря. Это не мусор,
а честные наблюдения; трогать содержание из-за них не надо.

**Мутации проверены:** снимаешь гейт по прогрессу — тест падает («sum()
советуется до урока про списки»); перестают разбираться f-строки — падает
(«ложная находка на "имя только в f-строке"»). `npm test` и `npm run audit`
зелёные.

**Что осталось из списка идей** (в «Планах» `README.md`): «объясни мою программу
словами», галерея черепашки, PWA, панель символов на телефоне, экспорт `.py`,
«вопрос за ужином». Крупных тем в списке больше нет — всё, что было про
«показать то, что тренажёр уже знает», сделано.

**Ещё две фишки: значения в редакторе и бестиарий ошибок (1.31.0).**
Продолжение списка идей из 1.30.0 — взяты следующие две по отношению
«польза ÷ цена». Обе про одно и то же: сделать видимым то, что тренажёр уже
знает, но показывает мельком.

**1. Значения прямо в редакторе.** После запуска рядом со строкой стоит то,
что эта строка сделала: «итог = 0», «×5  i = 4», «→ сумма 6». Данные брались
вторым проходом по шагам — и вот два места, где легко ошибиться:

- **Второй проход честен только потому, что ГПСЧ движка детерминирован**
  (`Interp.prototype.random` всегда стартует с 12345, если зерно не задано).
  Иначе приписка у строки с `randint` показывала бы одно, а консоль — другое.
  В тестах на это стоит прямая проверка: приписка обязана совпасть с выводом.
  Если когда-нибудь захочется сделать random по-настоящему случайным —
  сначала прочитай эту проверку, она упадёт.
- **Главная трудность оказалась не в данных, а в раскладке.** Подсветка
  (`pre.hl`) лежит ПОВЕРХ текстового поля, и раскладка у них общая: если
  приписка отожмёт перенос строки в подсветке, а в поле переноса не будет,
  видимый код съедет относительно курсора. Поэтому ширина знака измеряется
  настоящим шрифтом (`colsFit` в `makeEditor`), приписка ставится только если
  влезает, и при `resize` всё пересчитывается. Проверено в браузере на 1280px
  и на 375px: высота подсветки совпадает с высотой поля в обоих случаях, а на
  узком экране лишние приписки исчезают сами.
- Код с тройными кавычками приписок не получает вовсе: без них подсветка
  красит текст одним куском (и многострочная строка красится целиком),
  а с приписками приходится идти построчно.

**2. Бестиарий ошибок.** Тринадцать типов ошибок карточками на экране
«Повторить», три состояния: не встречался → встретился → побеждён. «Побеждён»
даётся за ПОЧИНКУ (программа после правки пошла), а не за встречу, и
**показанное решение победы не даёт** — чинил тогда не ребёнок. Шесть разных
побеждённых — бейдж «🐉 Укротитель». Хранится в `S.errs`, при слиянии по
каждому типу берётся большее, а не сумма (сложение удваивало бы встречи при
каждом обмене — та же грабля, что с попытками в журнале).

**Проверка бестиария нашла старую дыру, к фишке не относящуюся.** Она требует
русское имя для каждого зверя — и оказалось, что пяти типов в `KIND_RU` не было
вовсе: `FileNotFoundError`, `UnboundLocalError`, `OSError`, `JSONDecodeError`,
`EOFError`. Движок их выбрасывает, а ребёнок видел английское имя вместо
человеческого. Дописаны. Мораль прежняя: инвариант («у каждого X обязано быть
Y») ловит то, чего не ловит проверка отдельных случаев.

**Мутации проверены обе:** перестают гаситься приписки при правке — тест падает
(«они начали врать про изменённый код»); засчитывается победа после показанного
решения — падает. `npm test` и `npm run audit` зелёные.

**Что осталось из списка идей** (в «Планах» `README.md`): ревьюер кода (свой
линтер по AST — самая большая незакрытая тема, рефакторинг), «объясни мою
программу словами», галерея черепашки, PWA, панель символов на телефоне,
экспорт `.py`, «вопрос за ужином».

**Три фишки: разбор своего кода, цель по шагам, свои задания (1.30.0).**
Просили придумать, что ещё можно сделать, и сделать выбранное. Из десятка идей
взяты три с лучшим отношением «польза ÷ цена»; остальные записаны в «Планы»
в `README.md`, чтобы не придумывать их заново.

**1. Кнопка «🔍 Разобрать» на уроке и в песочнице.** Богатый визуализатор
(коробки, стрелки, перемотка) жил только на своих примерах, а на уроке ребёнок
видел бедный список переменных. Блокером были не силы, а потеря кода: уйти
с урока было нельзя. Черновики (1.25.0) блокер сняли — уход сохраняет код,
возврат его ставит на место, и это проверено тестом.

Две мелочи, которые легко забыть при следующей правке:

- `vizRecord(code, opts)` теперь получает окружение прогона (ответы для
  `input()`, файлы, модули). Без него урок с `input()` падал бы в разборе там,
  где обычный запуск работает. В тесте есть обратная проверка: без ответов
  такой урок ОБЯЗАН падать, иначе первая проверка ничего не значит.
- Кнопки нет на многофайловом уроке (в визуализаторе один редактор, подсветка
  уехала бы в чужой файл) и нет в проектах — **вот почему:** код шага проекта
  сохраняется только на победе, `draftFlush` проекты не покрывает. Захочешь
  добавить разбор в проект — сначала заведи черновики шагам проекта.

**2. Цель по шагам: «уложись не дороже решения автора».** Цена программы
считалась с 1.23.0, но говорилась фактом — факт ничего не просит. Теперь есть
цель, перышко у урока в списке, +10 XP один раз на урок и бейдж «Лёгкая рука»
за пять уроков.

Решения, которые важнее цифры: цель «не дороже», а не «короче» (иначе учим
писать нечитаемые однострочники — против собственного урока про PEP 8);
показанное решение цель НЕ засчитывает (код автора уложился бы в цену автора,
и медаль была бы обманом); промах не ругает, а приглашает вернуться.
`leanAward` зовётся ДО сборки победной карточки — иначе опыт в полоске и в
карточке разошлись бы на 10 XP.

**3. Раздел «Своё задание» (`#mine`).** Смена роли: сто уроков ребёнок решал
задачи, здесь он их составляет. Пишет программу и условие словами; правильный
ответ ВЫЧИСЛЯЕТ движок (то же правило, что у разминок и вердиктов, — «автор
ошибся в ожидаемом выводе» невозможно); задание пакуется в ссылку `#task=…`
и уезжает другу. Сервера не потребовалось.

- **Программы автора в ссылке НЕТ** — только условие и ожидаемый вывод. Друг
  пишет свою программу, сойтись должен ответ. Код остаётся у автора
  (`S.mytasks`).
- **Запрещены случайность и `input()`** — не из вредности: у друга случайное
  выпало бы другое, а ответов для `input()` в ссылке нет, и задание оказалось
  бы непроходимым.
- **Разбор ссылки идёт ДО `toLowerCase()`** в `routeHash`. base64 различает `A`
  и `a` — общий нижний регистр превратил бы рабочую ссылку в «не прочиталось».
  Это первое место в проекте, где регистр хэша значим.
- **Битая ссылка объясняется экраном** («скорее всего, обрезали в мессенджере»),
  а не молчаливым уходом на карту миров.
- **Опыт за чужое задание — один раз** по ключу-хэшу от условия и ответа; за
  своё же, открытое «глазами друга», опыта нет вовсе.
- `mytasks` стоит рядом с `games` в `KEEP_ON_RESET`: это работа ребёнка, сброс
  прогресса её не стирает, смена ученика стирает. Новые поля прогресса —
  `mytasks`, `friendTasks`, `mytaskDraft`; инвариантная проверка формы прогресса
  подхватила их сама, дописывать её не пришлось.

**Проверки.** Три новых раздела в `tests/full-run.js` (цель по шагам, разбор
своей программы, свои задания). Обе мутации проверены: ломаешь `out.hit` или
разбор ссылки — тест ПАДАЕТ и называет, что именно. По дороге тест пришлось
чинить: сломанная фича обрывала прогон исключением вместо аккуратной проблемы
в отчёте (`solveTask` теперь сообщает и идёт дальше). `npm test` и
`npm run audit` зелёные.

**Мелочь, найденная в браузере, а не в тестах.** На экране автора был лишний
`<div class="msg" id="tmsg">`, в который никто не пишет: сообщения показывает
сама студия через `showMsg`. Убрал. И имя автора в заголовке чужого задания
сначала выводилось как «задание от Аня» — теперь «автор — Аня»: русские имена
в коде не склоняем, а победная карточка написана без родовых окончаний (как и
текст сертификата).

**Инструменты аудита и главная дыра курса (1.29.0).** Просили сделать
инструменты для вычисления ошибок и прогнать их. Сделано три, запуск —
`npm run audit` (в `npm test` НЕ входят: часть находок законна).
*В 1.32.0 к ним добавился четвёртый — `tests/lint-check.js`, ревью кода против
всех решений автора.*

**`tests/bypass-check.js` — самый результативный.** Подсовывает каждому уроку
программу «просто напечатай ожидаемый вывод построчно» и смотрит, засчитает ли
урок. Первый прогон: **28 уроков из 77 проходились печатью ответа.** И это не
теория: при неверной попытке тренажёр САМ показывает ожидаемый вывод
(`diffBlock`), скопировать его может кто угодно. После разбора остался один —
урок 1, где напечатать три строки и есть задание.

Закрыто расстановкой `needCode` в 27 уроках. Принцип выбора токена:

- где урок ЕСТЬ конструкция — требуем конструкцию (`def `, `class `, `for`,
  `sorted`, `append`, `set`, `randint`, `choice`, `//` и `%`);
- где решений может быть несколько — требуем не способ, а РАБОТУ С ДАННЫМИ:
  урок 17 получил `needCode:["marks"]`, и считать список можно хоть готовыми
  командами, хоть своим циклом;
- два требования пришлось ослабить сразу: FizzBuzz законно решается через
  `while` (оставил только `if`), конфеты — сложением вместо умножения
  (заменил `*` на `boxes`). Проверил обе альтернативы отдельно — проходят.

**По дороге нашлась ошибка, из-за которой требование молча ничего не делало.**
`needCode` / `noCode` / `needText` / `needMsg` читаются ТОЛЬКО из `task.check`.
Я добавил `needCode` уроку 6 на уровень `task` — урок выглядел защищённым, а
защиты не было, и ни один тест не пикнул. Заметил только сторонним инструментом.
Теперь в `tests/lessons.js` стоит жёсткая проверка на это (мутацию проверил,
роняет), а в грабли добавлен пункт 48.

**`tests/audit.js` — четыре статические проверки,** каждая из настоящего
дефекта. Нашли и закрыли пять штук: три записи шпаргалки указывали не на тот
урок (`len` → `fstrings`, `join` и `startswith` → `str-methods`), а `abs` и
`type` шпаргалка обещала за уроками 4 и 2, где их не показывали ни разу —
дописал в оба урока, они там и просились. Плюс урок 20: первая подсказка
вставляла готовый код вместо толчка.

**Инструмент врал про самого себя.** Проверка №2 в `audit.js` читала `needCode`
с уровня `task` — той же ошибкой, которую я чинил в контенте, — и продолжала
ругаться на уже защищённый урок 6. Если инструмент показывает находку, сначала
проверь сам инструмент.

**Выверка курса: четыре дефекта «требуем необъяснённое» плюс три списанных
задания (1.28.0).** Просили выверить ошибки — вот что нашлось и чем закрыто.

**Класс 1: решение требует того, чего не объясняли.** Тесты этого не ловили
вообще, и дефект попадал в курс дважды (`isinstance` нашёлся в прошлой сессии).
Прогнал по всем ста урокам: конструкция стоит в решении → показана ли она в
примере этого или более раннего урока? Нашлось три новых:

- **`.title()` — урок 7.** Задание требует «Иван Петров» из двух слов. В теории
  показаны `.upper()` и `.capitalize()`, а `.capitalize()` для двух слов даёт
  «Иван петров» — то есть неверно. Единственное место, где `.title()` назван, —
  ТРЕТЬЯ ПОДСКАЗКА, за которую снимается звезда.
- **`.lower()` — не показан НИГДЕ во всём курсе,** а нужен в решениях шести
  уроков (48, 60, 81–84). Шпаргалка при этом утверждала, что он из урока 7.
- **`round()` — урок 17.** Задание говорит «округли до 1 знака», но `round`
  не показан ни в уроке 4 «Арифметика», ни где-либо ещё; назван только в
  третьей подсказке.

Все три закрыты в тех уроках, где им место: `.lower()` и `.title()` дописаны в
карточку «Через точку и со скобками» урока 7 (заодно там теперь виден контраст
`capitalize` против `title`, а он и есть ловушка задания), `round` — в карточку
«Два разных деления» урока 4, где деление как раз даёт длинный хвост. Проекты
и «Ты и ИИ» прогнал тем же способом отдельно — там чисто.

**Проверка стала постоянной** (`tests/lessons.js`, «порядок объяснений»).
**И у неё нашлась своя дыра, найденная мутацией.** Сначала я засчитал подсказки
за объяснение — и тест честно прошёл на `.title()`, то есть ровно на том, ради
чего писался. Подсказка стоит звезду, значит она не бесплатна и объяснением
считаться не может. После ужесточения тест сразу нашёл `round`, которого я до
этого не видел.

**Класс 2: задание списано с теории того же урока** (грабля 13). Три случая:

- **`range-step` (12):** решение — это дословно пример карточки «Назад — шаг с
  минусом», вместе со словом «Пуск!». Пример переписан на другой счёт (минуты).
- **`comp-basic` (32):** две из трёх строк решения — дословно карточка «Внутри
  можно что угодно». Карточка переписана на другую форму данных (чистка строк
  через `.strip()`), лекарство ровно то, что записано в грабле 13.
- **`proj-field` (25):** решение — карточка «Стены по краям» целиком плюс две
  строки из соседней. Карточка переписана: каркас тот же, но условие внутри
  шахматное, а границу ребёнок выводит сам. Каркас в списке совпадений остался
  (73%) — и это правильно: его урок и обязан дать готовым.

**`tests/echo-check.js`** — служебный инструмент под этот класс (в `npm test`
НЕ входит, как `try-one.js`). Автоматической проверкой он не стал сознательно:
порога нет. У «починить» совпадение законно, у уроков с каркасом — тоже, а три
настоящие находки выглядели в списке ровно как ложные. Отличить удалось только
чтением уроков целиком.

**Пробелы программы и замок разминок (1.27.0).**

**Сначала поправка к записи 1.26.0.** Там сказано, что `any`/`all` не
объясняются нигде. Это было неверно: я искал по подстроке `all(` и получил
`apply_all`, `sum_all` и `findall`. Как есть на самом деле:

- **`any` и `all` объясняются** — в уроке `api-4`, то есть на 92-м из ста.
  Это не дыра, а поздний ввод, ровно как было с `zip` (объяснялся на 80-м).
  Поступил так же, как решили тогда с `zip`: **карточка в `comp-if`** (урок 33,
  Мир 2 — там как раз строят списки из условий, и `any`/`all` читаются как
  «свернуть такой список в один ответ»), а карточка в `api-4` **переписана из
  «новое» в «вспомним»**, чтобы не стать повтором: она теперь про то, что
  изменилось — вместо списка внутри скобок стоит выражение-генератор, список
  целиком не строится и `all` останавливается на первом же неподходящем.
- **`isinstance` — настоящая дыра, и она была хуже, чем я думал.** Он стоит в
  примерах уроков `inherit` (57) и `dataclass` (58) и — главное — **в решении
  задания урока `docs` (99)**, то есть ребёнка просят написать то, чего ему не
  объясняли. Починено там, где он впервые показан: текст карточки в `inherit`
  теперь объясняет, что `isinstance` отвечает «того ли ты рода», что для
  потомка ответ True и про свой класс, и про родительский, и чем это
  отличается от `type()`.

**Замок разминок по прогрессу — это нашлось попутно и оказалось важнее.**
Разминки были открыты ВСЕ сразу, а задача дня берётся из того же пула. Значит
ребёнку из Мира 1 могло выпасть упражнение про `zip` или про включения — читать
их ему нечем. Теперь:

- у разминки есть поле `lesson` (проставлено всем 19 старым и 2 новым);
  `warmupOpen` / `warmupsOpen` в `app.js`;
- закрытая разминка **показана замком**, а не спрятана: видно, что впереди есть
  ещё, и написано, какой урок её откроет. Спрятанное просто не существует;
- `dailyPick` выбирает только из открытых, а когда открытых нет вовсе (первые
  дни), возвращает `null` — и экран «Сегодня» честно пишет, что задача появится
  после первых уроков. Раньше я сделал тут откат «берём из всех», и это была
  ошибка: он возвращал ровно ту нечитаемую задачу, от которой замок и защищает;
- «Открыть все уроки» в панели наставника снимает и этот замок.

**Плюс контент:** две разминки (`predict-anyall` — с ловушкой пустого списка,
где `any` даёт False, а `all` True; `predict-isinstance` — наследование работает
в одну сторону) и две записи шпаргалки (`any-all`, `isinstance`), 104 → 106.

**Проверки:** новый блок в `full-run.js` («замок разминок по прогрессу») и три
мутации, каждая роняет тест: задача дня из всех вместо открытых, замок снят
совсем, кнопка закрытой карточки не выключена.

**Шпаргалка: 86 → 104 записи (1.26.0).** Закрыт документированный пробел.
Оказалось, что дыра была не там, где записано: не в Мире 5, а в Мире 4.

- **Было 7 уроков Мира 4 из 20 со своей записью, стало 15.** Добавлены: даты
  (`date`, разность и `timedelta`, `strftime`), `itertools` (`combinations`,
  `product`), `defaultdict`, `pathlib`, свой `with` (`__enter__`/`__exit__`),
  подсказки типов, файлы (`open` на чтение и запись, `csv.DictReader`).
- **Плюс шесть базовых дыр:** `f"{x:.2f}"`, `.sort()` против `sorted()`,
  `[::-1]`, `.add()`/`.discard()`, `finally`, словарь функций.
- **Новое поле `data`** — файлы рядом с примером. До него правило шпаргалки
  прямо запрещало файлы, потому что `sheetRun` звал движок без них. Теперь
  файлы уходят и в тренажёр, и в python3 при сверке. Подключено в ТРЁХ местах:
  `sheetRun`, `tests/lessons.js`, `tests/content-vs-python.js`.
- **Проверка в `full-run.js` усилена:** раньше она брала вывод ПЕРВОЙ записи,
  теперь прогоняет каждую через `sheetRun` и требует непустой вывод без ошибки.
  Это отдельный путь от двух других тестов, и запись с файлами падает именно
  здесь — со старой проверкой это прошло бы мимо (мутацию проверил, роняет).

**Про Мир 5 в шпаргалке: его там почти нет и не будет.** Это не дыра, а
следствие правила «пример обязан запускаться и совпадать с python3». pandas,
requests, Flask и git внутри вкладки не работают, а выдумывать для них вывод —
враньё. То же про `pip`, `pep8`, `read-code`, `perf`, `boss-package`: это не
забываемые команды, а привычки. Если фаундер захочет «шпаргалку по Миру 5» —
это будет другой жанр (памятка без запуска), и решать это ему.

**Найден пробел ПРОГРАММЫ, не справочника: `any()` и `all()` нигде не
объясняются.** ⚠️ **Закрыто позже, проверено 02.09.2026:** `any`/`all`
объясняются в `comp-if`, `isinstance` — в `inherit`, и записи шпаргалки на них
ссылаются честно. Абзац ниже оставлен как разбор метода, а не как открытый долг. `all()` используется в четырёх уроках (`fn-as-value`, `regex`,
`typing`, `api-4`), `any()` — в `api-4`, но ни одного объяснения нет. Записи в
шпаргалку я поэтому не делал: она ссылается на урок, «где это объясняли», и
ссылка была бы враньём. То же с `isinstance` (встречается в `inherit`,
`dataclass`, `docs` — объяснения нет). Чинится карточкой в уроке — как чинили
`enumerate` и `zip`. Кому это отдать, решает фаундер.

**Черновики кода на уроке (1.25.0).** Закрыта грабля 34 — единственная в
продукте потеря работы ребёнка: уход с урока стирал написанное.

- **Где хранится:** `S.drafts[id] = { files:[{name, code}], at }`. Списком файлов,
  потому что так же выглядит многофайловый урок, а обычный — это список из
  одного файла. При возвращении файлы раскладываются по ИМЕНАМ, а не по
  порядку: содержание урока могли поправить.
- **Где сохраняется:** в `claimScreen`. Это оказалось единственное место, через
  которое проходит ЛЮБАЯ смена экрана (`enterScreen` зовёт его, `openLesson` и
  `openProject` зовут его напрямую) — в том числе переход с урока сразу на
  другой урок, который `enterScreen` не задевает вовсе. Плюс отложенное
  сохранение после набора (крючок `editor.onEdit`) и `beforeunload` — на случай
  просто закрытой вкладки.
- **Черновик, равный заготовке, не хранится.** Иначе сто уроков превратились бы
  в сто программ, уезжающих на сервер при каждой синхронизации. Плюс предел
  `DRAFT_MAX = 60`, самые старые выбрасываются.
- **Подпись над редактором намеренно не говорит «твой код».** Если ребёнок
  открыл решение и ушёл — вернётся решение, и врать об этом не надо. Рядом
  кнопка «Вернуть заготовку»: у обычного урока «↩ Вернуть как было» нет, она
  только у `fix`, и без новой кнопки ребёнок остался бы заперт со своей кашей.
  Назвал именно так, чтобы не путалась с «↺ Сброс» в редакторе (та чистит
  вывод, а код не трогает).
- **Заодно нашлась та же потеря в песочнице.** Её код сохранялся только по
  «Запустить» и по нижней кнопке «Ко всем мирам»; уход кнопкой ВЕРХНЕЙ панели
  стирал написанное. Теперь песочница помечает свою сессию `sandbox:true`, и
  тот же `draftFlush` её сохраняет.
- **Проверки** в `full-run.js` плюс пять мутаций, каждая роняет тест: уход не
  сохраняет, черновик пишется даже нетронутым, возвращается только главный файл,
  слияние берёт старую копию, песочница не сохраняется.

**Портфолио и сертификаты (1.24.0).** Закрыт последний незакрытый пункт из
анализа конкурентов. Проекты были готовы — не хватало страницы, которая
собирает их вместе и позволяет ПОКАЗАТЬ.

- **Раздел «Портфолио»** (`screenFolio`, адрес `#folio`): сводка цифрами, все
  шесть проектов с кодом ребёнка целиком (кнопки «Открыть и запустить» и
  «Скопировать код») и шесть сертификатов. Входов три: карточка на карте миров
  под достижениями, кнопка в профиле, адрес. В верхнюю панель кнопку НЕ добавлял
  — там уже одиннадцать штук, а портфолио открывают редко и осознанно.
- **Условие выдачи сертификата выбрано так:** все уроки мира ПЛЮС собранный
  проект мира. Не «прошёл уроки», а «прошёл и сделал вещь». Сертификатов шесть:
  пять по мирам и один за весь курс. Пока не выдан — карточка говорит словами,
  чего не хватает, а не показывает проценты.
- **Сертификат — оверлей, а не экран.** Причина другая, чем у шпаргалки (там
  дело было в черновиках): его ПЕЧАТАЮТ, а печать берёт документ целиком.
  Разметка `#cert` в `index.html`, в `@media print` скрыто всё, кроме листа.
  Печать — штатный `window.print()`, он же даёт «Сохранить в PDF».
- **Лист выглядит бумагой** (светлый, золотая рамка) поверх тёмного сайта, а
  текст безличный («Мир 1 пройден полностью»): угадывать род по имени нельзя.
- **Дата на сертификате не должна плыть** — поэтому появилось единственное новое
  поле прогресса за сессию: `S.projects[id].doneAt`, дата сборки проекта.
  Ставится один раз, при слиянии берётся РАННЯЯ из двух (проект собран тогда,
  когда собран впервые). Дата сертификата — самое позднее из `solvedAt`
  последнего урока мира и `doneAt` проекта.
- **Проверки** в `full-run.js` и все четыре мутации, которыми они проверены на
  честность: сертификат без проекта, незаписанная `doneAt`, слияние по поздней
  дате, убранное правило печати — каждая роняет тест.
- **Заодно починен `copyText`** (им пользуются и профиль, и портфолио). Отказ
  современного способа копирования — а браузер отказывает легко: нет разрешения,
  страница во фрейме, нет жеста — приводил к полной тишине: кнопка нажата, не
  изменилось ничего. Теперь отказ падает на старый способ через невидимое поле.
  Нашлось живой проверкой в браузере, тесты этого не ловили и не поймали бы:
  в jsdom буфера обмена нет вовсе.

**Четыре механики, которых нет ни у кого (1.23.0).** Фаундер попросил придумать
новое — и отбор шёл по одному признаку: что можно сделать ТОЛЬКО потому, что
интерпретатор свой. Стрики и бейджи есть у всех; пошаговый прогон, снимок кучи и
счётчик операций — ни у кого, потому что все сидят на Pyodide и внутрь него не лезут.

**1. «Поймай ИИ на ошибке» (`type: "catch"`, 4 задания).** Переворот `review`:
вердикт не выбирается кнопкой, а ДОКАЗЫВАЕТСЯ. Ребёнку дают код от ИИ, верный на
примере автора, и просят написать свою проверку, разводящую его с правильной
версией. **У задания нет единственного правильного ответа** — годных проверок
бесконечно много, а судит движок: `run(code+probe) !== run(truth+probe)`.

- Код ИИ и проверка живут в ОДНОМ редакторе. Иначе «Запустить» ничего бы не
  запустило: проверка без функции — не программа. Целость кода ИИ стережёт
  `catchProbe`: первые строки сверяются с оригиналом построчно.
- Отклоняются три вида проверок с РАЗНЫМИ сообщениями: пустая; падающая на
  ПРАВИЛЬНОЙ версии (значит дело в проверке, а не в коде ИИ); ничего не поймавшая.
- **Случайность победы не даёт**, но не потому, что я её ловлю: генератор у нас
  засевается одинаково на каждый запуск, поэтому обе версии получают одни и те же
  числа. Двойной прогон в `runAICatch` оставлен ради `time.time()` — вот он между
  двумя запусками может перевалить через миллисекунду.
- Правило для содержания, которое проверяет тест: `run(code) === run(truth)` —
  баг ОБЯЗАН быть спрятан за примером автора. Иначе искать нечего, и это review.

**2. Разминка «Предскажи память» (`type: "memory"`, 5 штук).** Все просят угадать
ВЫВОД. Эта просит угадать ПАМЯТЬ: программа замерла перед подсвеченной строкой,
что лежит в переменных? Правильный ответ считает `heapSnapshot` — тот же, что
рисует визуализатор, поэтому вопрос и картинка разойтись не могут. После верного
ответа показывается настоящая память со стрелками.

- `stop` — строка обязана выполняться РОВНО ОДИН раз, иначе «здесь» значит разное.
- Сверка с python3 хитрая и важная: перед строкой `stop` вставляется
  `print(repr(...))`, и результат сравнивается с нашим снимком. Раз строка
  выполняется один раз, вставка ей строго равносильна. Без этого «правильный
  ответ» зависел бы от того, как наш движок печатает списки.
- При ошибке говорим, В КАКОЙ переменной, но НЕ говорим правильное значение —
  иначе задание решается со второй попытки без единой мысли.
- Пять штук закрывают классику: алиасинг, копия срезом, неизменяемость строк,
  что осталось после `break`, своя память у вызова функции.

**3. Цена программы в шагах.** Оказалось, считать ничего не надо: интерпретатор
уже считает операции ради защиты от вечного цикла, и `run()` отдаёт `steps`
наружу. Сумма 1..100 циклом — 411 шагов, через `sum(range(...))` — 9.

- В победной карточке: своя цена, цена решения автора, личный рекорд.
  Соревнование со вчерашним собой, а не с другими детьми.
- **Ругать за длинную программу нельзя** — ребёнок только что её дописал и она
  работает. Поэтому фраза факта, а «можно короче» появляется лишь при разнице
  больше чем вдвое.
- Не показываем у черепашки (число зависит от длины линий) и у случайности.
- `bestSteps` — ЕДИНСТВЕННОЕ поле журнала, где меньше значит лучше. В слиянии
  для него отдельная функция `minPos`: ноль значит «рекорда нет» и не должен
  победить настоящий.

**4. Проект «Напарник» — программу пишет ИИ.** Шестой проект и единственный ВНЕ
миров (`world: 0`): живёт в разделе «Ты и ИИ», открывается по заданиям раздела.
Ребёнок не пишет код, а командует: напарник трижды отдаёт «почти готовое» —
понял буквально, добавил новое и сломал старое, ошибся в мелочи (`max` у кортежей
сравнивает по названию, а не по минутам).

- Понадобилась одна правка механики: **у шага может быть свой `starter`**.
  Раньше он был только у первого. Теперь шаг с `starter` начинается с редакции
  напарника — и подставляется она ОДИН раз (`st.aiAt`), иначе уход на карту и
  возврат затирали бы правки ребёнка. `aiAt` есть и в слиянии.
- Требования к такой заготовке те же, что к первой: запускается, проверку не
  проходит, и вывод отличается от прошлого шага — иначе «он переписал программу»
  было бы неправдой.

Всё четыре покрыты тестами и проверены мутациями (11 мутаций, каждая роняет тест).
Две из мутаций сначала НЕ ловились, и обе поучительные — см. грабли 37 и 38.

**Две дыры в движке закрыты (1.22.0).** Обе нашлись, когда я писал проекты
миров 4 и 5, и обе про то, что ребёнок вставит в песочницу чужой код и получит
непонятную ошибку на ровном месте.

- **`re.compile` и `re.escape`.** Были только функции модуля. Теперь
  скомпилированный шаблон — объект со своим типом `re.Pattern`: `search`,
  `match`, `fullmatch`, `findall`, `sub`, `split`, свойство `.pattern`,
  печать как `re.compile('\\d+')`. Негодный шаблон падает в момент `compile`,
  а не при первом применении — как в Python. Тела функций модуля вынесены
  в `reFindall` / `reSub` / `reSplit`, чтобы модуль и объект звали один и тот же
  код, а не разошлись при следующей правке.
- **Неявная склейка соседних строковых литералов.** `"а" "б"` теперь `"аб"`,
  куски в скобках можно разносить по строкам, f-строка склеивается с обычной
  (результат — f-строка, части кусков выстраиваются в один список). Сделано
  в `parseAtom`: пока следующий токен STRING или FSTRING — забираем.
  **Важно, что НЕ сломалось:** два строковых литерала на разных строках без
  скобок остаются двумя выражениями — между ними лексер выдаёт NEWLINE, и
  второй до `parseAtom` не доходит. Документные строки и словари тоже целы,
  на всё это есть программы в корпусе.

Обе добавлены в `tests/programs.txt` (`re_compile`, `str_concat_implicit`) —
корпус вырос со 149 программ до 151.

**Четыре вещи за одну сессию (1.21.0).** Фаундер спросил «чем ещё улучшить»,
получил список и выбрал сразу четыре пункта. Все четыре сделаны и покрыты тестами
с проверкой мутациями.

**1. Проекты миров 2–5.** Механика была готова с 1.17.0, а проект существовал
ровно один. То есть в четырёх мирах из пяти ребёнок доходил до конца и упирался
в карточку «скоро». Теперь их пять:

- Мир 2 «🏆 Турнирная таблица» — словари, `sorted` с `lambda`, множества, включения.
  Данные подобраны так, чтобы **сортировка была видна**: у Тигров и Ежей поровну
  очков, и порядок меняется только когда на шаге 3 появляется разница мячей.
  Без этого шаг 3 не отличался бы от шага 2 ничем, кроме кода.
- Мир 3 «🎒 Рюкзак героя» — классы, наследование, `raise`/`except`, `assert`.
  Наковальня не влезает, а яблоко после неё влезает: программа не останавливается
  на первой беде, и это видно по выводу.
- Мир 4 «📜 Чёрный ящик» — регулярки, `Counter`, генератор, декоратор, JSON.
  Шаг 4 не выбрасывает человеческий отчёт ради JSON, а строит оба вида из ОДНОГО
  словаря: иначе последний шаг выглядел бы как потеря сделанного.
- Мир 5 «🌐 Свой сайт» — маршруты, страницы, 404, шаблон. Это единственная часть
  курса, которую иначе не показать: уроки про Flask (`web-1`…`web-4`) в браузере
  не запускаются, и веб оставался разговором. Здесь ребёнок пишет то, что Flask
  делает внутри, и оно работает.

Все 20 шагов сверены с настоящим python3. **Сквозной тест раньше проходил только
`PROJECTS[0]`** — теперь он проходит все пять по шагам эталонами (проверено
мутацией: поломка в проекте Мира 2 тест роняет).

**2. Раздел «Повторить» (`#again`).** Данные для интервального повтора копились
с самого начала — `S.log` знает `attempts`, `hints`, `shown`, `solvedAt` по каждому
уроку, — но никуда не шли. Теперь:

- в список попадает не «нерешённое», а **пройденное, которое стоило дорого**:
  показанное решение, подсказки, три+ попытки, неполные звёзды. Причина названа
  словами на карточке;
- промежутки `REVIEW_STEPS = [2, 7, 21]` дня. Три чистых повтора подряд — урок
  «закреплён» и уходит навсегда. Сбился — счётчик в ноль;
- **повторяется то же самое задание**, новых задач не писалось: смысл в том, чтобы
  вспомнить, как это делается. Звёзды при повторе только растут — `winLesson`
  никогда не понижает уже полученные, так что хуже не станет;
- новое поле `S.review = { id: { n, at } }` добавлено в `PROGRESS_MAPS` и в слияние
  (берётся больший `n`). Бейдж «🔁 Закрепил» за пять закреплённых.

**3. Шпаргалка (`js/cheatsheet.js`, 106 команд).** Главное решение — **это оверлей,
а не экран**. Черновики кода нигде не сохраняются, поэтому уход со страницы урока
стёр бы написанное, а справочник нужен ровно посреди урока. На это есть тест, и он
проверяет не «объект жив», а что `#studio` остался В ДОКУМЕНТЕ: сессия держит
ссылку на редактор и после того, как экран стёрт, — по ссылке поломку не увидеть
(первая версия теста мутацию пропустила именно поэтому).

**Вывод примеров нигде не хранится** — считается движком в момент показа
(`sheetRun`, с кэшем на сессию: 86 программ на каждую букву в поиске это перебор).
Значит, справочник физически не может разойтись с тем, что ребёнок увидит у себя.
Показывается только пройденное; галочка «показать всё» открывает остальное.

**4. Отчёт «Что было за неделю»** в панели наставника. Там было много верных чисел
и ни одной фразы. Теперь: занятия и уроки за 7 дней, полоса дней (сколько уроков
в каждом, 🛡 для дня под щитом), три самых трудных урока с причинами, что дальше.
**Посуточного учёта времени в прогрессе нет** — есть только время по каждому уроку.
Поэтому в дне показано время тех уроков, которые в этот день пройдены, и подписано
это именно так. Придумывать точность, которой в данных нет, нельзя.

**Две дыры в движке, найденные по дороге** — **закрыты в 1.22.0**, см. ниже.

**Визуализатор: что изменилось на шаге и стек вызовов (1.20.0).** Последняя
незакрытая вещь из «идей на будущее» — и, как выяснилось, там была не только
недостающая возможность, но и вранье.

- **Стек вызовов. Раньше визуализатор внутри функции ВРАЛ.** `heapSnapshot`
  сваливал все области видимости в один список и дедуплицировал имена: ребёнок
  видел местную `ш` и никак не мог узнать, что внешняя `ш` с другим значением
  всё ещё существует. Это ровно то место, где новички путаются, — и инструмент,
  который должен был его объяснять, показывал одну переменную вместо двух.
- **Что для этого понадобилось в движке.** `Interp` теперь ведёт `this.stack` —
  `[{name, env}]` снизу вверх. Счётчика `this.depth` не хватало: он знает
  глубину, но не имена кадров и не их местные переменные. Push/pop вокруг вызова
  функции, на выполнение не влияет. `stepper().next()` отдаёт копию стека,
  `heapSnapshot(env, idMap, skip, stack)` добавляет к снимку `scopes` — по списку
  переменных на КАЖДЫЙ кадр. Куча одна на снимок, как в настоящем Python.
  149 программ движка и 598 сверок с python3 остались зелёными.
- **Кадр функции рисуется отдельной коробкой** поверх коробки главной программы.
  Пока в функции не заходили — кадр один и рисуется как раньше, без рамки и
  заголовка: чрома не должно быть больше, чем данных.
- **Имя функции в заголовке кадра не в верхнем регистре** (в отличие от подписи
  «главная программа»). Сначала сделал через общий `text-transform:uppercase` и
  получил «ПЛОЩАДЬ( )» — а в Python имена регистрозависимы, и `ПЛОЩАДЬ` это
  другое имя. Подпись не должна врать про код.
- **Что изменилось на шаге.** `vizDiff(prev, cur)` сравнивает соседние кадры,
  говорит фразой («вызвана функция `площадь(ш = 3, в = 4)`», «`всего`: 0 → 12»,
  «в словарь добавился ключ `'м'`», «напечатано: 22») и помечает изменившееся:
  золотая рамка у нового, бирюзовая у изменившегося. Метка на самой ячейке, а не
  на коробке: в словаре из восьми ключей важно, какой именно поменялся.
- **Изменение считается от предыдущего кадра**, а не от того места, откуда
  прыгнули ползунком — иначе «на этом шаге» значило бы разное в зависимости от
  того, куда ребёнок ткнул на шкале.
- **Вход в функцию называется вместе с аргументом.** Сначала было просто
  «вызвана функция факториал» — и на рекурсии все шаги читались одинаково, хотя
  вся суть в том, с каким числом позвали на этот раз.
- **Обратный путь рекурсии честно назван недоступным по одному шагу.** Шагомер
  выдаёт шаг на СТРОКУ, а возвраты из вложенных вызовов случаются внутри одного
  выражения `n * факториал(n - 1)`. Стек сворачивается сразу на несколько кадров,
  и разбор говорит сколько, а не называет один вызов. Делать шаг на каждое
  подвыражение — это менять гранулярность интерпретатора, отдельная большая
  работа, и без запроса я в неё не лез.
- **Два новых примера:** «Функция: свои переменные» и «Рекурсия: стек вызовов».
  Раньше среди примеров визуализатора не было ни одного с `def` — стек вызовов
  показать было буквально не на чем. На это теперь есть тест.
- **Грабля из этой сессии:** `.vizwhat` — flex-контейнер, и `<b>` внутри фразы
  становился flex-элементом, поэтому «всего: 0 → 12» рисовалось как
  «всего : 0 → 12» с зазором перед двоеточием. Текст фразы обязан лежать в одном
  `.vwt`. В CSS про это написано прямо над правилом.
- Проверено шестью мутациями: движок не ведёт стек, кадры снова сваливаются в
  один список, разбор всегда молчит, подсветка не попадает в разметку, вход в
  функцию без аргумента, убраны примеры с функциями. Каждая ломает тесты.

**Живой разбор расхождения (1.19.0).** Пункт 4 списка конкурентов. Формулировка
там была «живые подсказки по конкретной ошибке ученика», и первое, что я сделал —
проверил, есть ли дыра вообще: прогнал через движок 18 типовых ошибок ребёнка
(нет двоеточия, нет отступа, опечатка в имени, незакрытая скобка, `=` вместо `==`,
строка плюс число, индекс за краем, русская запятая, смешение табов…). **Движок
объясняет падения отлично** — везде человеческий текст с номером строки. То есть
дыра не там, где казалось.

Настоящая дыра: когда код **не падает, а просто отвечает не то**. Ребёнок получал
две колонки «должно быть / получилось» и номер строки вывода — что не так, ни
слова. А в самом злом случае (лишний пробел на конце) колонки выглядели
**одинаково**.

- **`whyDiffer(exp, got, emptyWhy)`** называет разницу словами. Одиннадцать
  случаев: только пробелы, только регистр, тот же набор в другом порядке, строк
  меньше/больше (с частой причиной — печать внутри или вне цикла), `2` против
  `2.0` (то есть `/` вместо `//`), сдвиг на единицу (нумерация с нуля или range),
  напечатан сам список вместо значений, значение в кавычках, запятая вместо
  точки, пустой ответ.
- **Невидимое не только называется, но и ПОКАЗЫВАЕТСЯ:** когда разница только в
  пробелах, колонки рисуются со значками — пробел `·`, таб `→`. Без этого
  объяснение звучало бы издёвкой: «у тебя лишний пробел» над двумя одинаковыми
  текстами.
- **Разбор один на всё приложение.** Его звинчивают и `diffBlock` (уроки, «Ты и
  ИИ», проекты), и `predictDiff` (разминки), поэтому новый случай появляется
  сразу везде. Заодно из обоих ушла копипаста: колонки рисует общий `cmpBlock`.
- **Правило: молчать, если не уверены.** Пустой ответ разбора — норма, тогда
  остаётся прежняя механическая подсказка. Ложное объяснение хуже отсутствующего:
  оно уводит от настоящей причины. Поэтому тест проверяет ОБЕ стороны — и что
  причина названа верно, и что на непохожих ответах разбор ничего не выдумывает.
- Текст про пустую сторону задаёт вызывающий: у урока «программа ничего не
  напечатала», у разминки «ты ничего не написал». Спутать их нельзя — в разминке
  пустая сторона это ответ ребёнка, а не вывод программы.

**Бейджи за длинный стрик (1.19.0).** Пункт из «идей на будущее»: щит серию
держал, а награды за неё не было — ребёнок видел растущее число и всё. Теперь
`week` (7 дней подряд) и `month` (30 дней), список `STREAK_BADGES` в `app.js`.
Порогов два: один достижимый сразу, второй остаётся целью. Проверяются в двух
местах — в `markActiveToday` и в `applyProgress`: если серия доросла на планшете,
бейдж должен быть на ноутбуке сразу, а не ждать следующего занятия.

Обе вещи проверены мутациями (9 штук): разбор всегда молчит, разбор придумывает
причину, пробелы не показываются значками, у пустого предсказания текст про
программу, бейдж без описания в BADGES, порог сдвинут, бейдж не выдаётся при
слиянии, не выдаётся при занятии сегодня. Каждая ломает тесты.

**Закрыли всё, что ждало решения фаундера (1.18.0).** Фаундер сказал «давай
сделаем то, что ждёт решения» — четыре висевших вопроса заданы прямо, и по трём
из них появился код.

- **`zip` перенесён в Мир 2.** Решение фаундера: перенести И переписать карточку
  в Мире 5, чтобы она не стала повтором. Новая карточка «Два списка рядом — zip()»
  стоит в уроке `tuples` — место выбрано не случайно: `zip` как раз выдаёт кортежи,
  а распаковку `x, y = point` этот урок объясняет прямо перед ней. Плюс две
  разминки, `predict-zip` и `blocks-zip`, обе про **молчаливое обрезание по
  короткому списку** (в первой Вика остаётся без зелья, и программа об этом не
  говорит). Карточка в `web-2` теперь «Вспомним zip — и зачем тут сверять длины»:
  там из той же молчаливости выводится, почему код ниже сверяет длины ДО цикла.
  Так перенос не отобрал у Мира 5 смысл, а дал ему новый.
- **Щит остаётся «за 5 дней».** Вопрос задан, ответ — не менять. Причина в самом
  вопросе: живого наблюдения за ребёнком ещё нет, а `SHIELD_EVERY` — одна цифра,
  поправить можно за минуту, когда наблюдение появится. Кода в этой сессии не
  потребовалось.
- **Полные аккаунты — по-прежнему нет.** Подтверждено. См. блок про аккаунты выше.
- **Появился тип `review` в «Ты и ИИ»** — большая часть сессии, подробно ниже.

**Тип `review`: ребёнок сам выносит вердикт (1.18.0).** Дыра, которую он
закрывает, была смысловая. Раздел учит «проверять результат ИИ», но все три
прежних типа сообщали ответ до начала: тип `fix` так и называется — «почини»,
значит код точно сломан. Ребёнок ни разу не решал, сломан ли он вообще.

- **Три вердикта, а не два.** «Работает верно» / «Работает, но не всегда» /
  «Врёт сразу». Средний — главный: код от ИИ почти всегда сходится на том
  примере, который показан вместе с ним, и разъезжается на любом другом.
  Два варианта дали бы монетку, три — уже выбор.
- **После верного вердикта «врёт» надо ткнуть в строку** с поломкой. Строки
  берутся из исходного `x.code`, а не из редактора: ребёнок вправе там всё
  переписать, пока проверяет, и номера бы разъехались.
- **Студия осталась редактируемой и без кнопки «Проверить»** — намеренно.
  Проверка это и есть вердикт, а редактор нужен как лаборатория: запусти,
  допиши свою строку с вызовом, подставь свои числа. Ровно этому и учит
  вторая подсказка второго упражнения.
- **Правильный ответ не записан, а вычисляется.** У задания есть `truth` —
  правильная версия с тем же интерфейсом и тем же примером, — и `probe`,
  несколько своих проверок, которые дописываются и к `code`, и к `truth`.
  Дальше `reviewTruth()` запускает четыре программы: разошлись выводы на
  примере автора → «врёт сразу»; совпали, а с `probe` разошлись → «не всегда»;
  совпали везде → «работает верно». Игра судит по вычисленному, а `verdict`
  в содержании — страховка, которую тест сверяет с запуском. **Соврать в таком
  задании нельзя, и это главное решение в его устройстве.**
- **`badLine` тоже не берут на слово.** Сначала я закрыл это неверно: и
  содержание, и игра сверялись с одним и тем же числом, так что неверный номер
  строки прошёл бы незамеченным. Теперь тест выводит его из разницы между `code`
  и `truth` и требует, чтобы расходилась ровно одна строка — иначе у ребёнка
  несколько правильных ответов, а игра примет один.
- **После победы карточка показывает доказательство** (`reviewProof`): на каких
  именно данных код разошёлся с обещанием. Для «работает верно» его нет — там и
  не разошёлся.
- Три упражнения, по одному на каждый вердикт, стоят между «Прочитай чужой код»
  и первым `fix`. Порядок теперь честный: прочитать → решить, сломано ли →
  починить. Прогресс по id, так что вставка в середину ничего не сбила.
- Проверено шестью мутациями: соврать в `verdict`, соврать в `badLine`, выдать
  победу сразу после вердикта, принять любую строку, принять любой вердикт,
  убрать доказательство. Каждая ломает тесты.

**Мелочи, найденные по дороге (1.18.0).**

- **У Мира 5 не было поля `engine`.** Уроки получали `engine: undefined` и падали
  в `mini` через фолбэк `Runtime.get()` — работало верно, но неявно, и именно в
  таком мире не заметили бы, что движок выбран не тем, кем думали. Теперь поле
  стоит, а тест требует его от каждого мира и сверяет со списком адаптеров,
  который читает из самого `runtime.js`.
- **Отчёт `content-vs-python.js` врал про покрытие.** Строка читалась как
  «пропущено: 63 (в т.ч. разминок: 12)», хотя `warmups`/`ailab`/`projsteps` —
  это сколько ПРОВЕРЕНО, а не пропущено. Выходило, будто разминки с python3 не
  сверяются вовсе. Формулировка исправлена, а счётчики теперь считают только то,
  что реально ушло на сверку.

**Проход по коду: ошибки, лаги, дубли (1.17.1).** Новых возможностей нет — только
починка того, что уже было. Каждая правка закрыта тестом, и каждый тест проверен
мутацией: ломаю починку — тест падает.

- **Визуализатор тикал в фоне после ухода с экрана.** Кнопка «Играть» ставила
  `setInterval` на 800 мс, а экраны меняются простой заменой `app.innerHTML` —
  таймер оставался жив и продолжал перерисовывать плеер, уже выброшенный из
  документа. Каждый следующий заход в визуализатор добавлял ещё один такой
  таймер. Теперь ручка проигрывания одна на всю страницу (`vizPlay`), гасится
  из `enterScreen()`, из нового прогона и сама, если плеера нет в документе.
- **Появился `enterScreen()`** вместо пары `stopTimer(); clearAdminHash();`,
  выписанной в пятнадцати местах. Именно из-за этой копипасты таймер
  визуализатора и не погасили: дописывать пришлось бы в пятнадцать мест.
  ⚠️ У `screenAdmin` по-прежнему свой вход без `clearAdminHash` — этот экран
  как раз открывается по `#admin`.
- **`S.games` и `S.gamesPlayed` были вне слияния и вне очистки.** Поля добавили
  вместе с разделом «Игры» и забыли в двух местах из четырёх: своя версия игры
  не доезжала на второе устройство, а при смене ученика доставалась следующему
  ребёнку вместе с чужим прогрессом (ровно то, от чего страхует грабля 27).
- **Форма прогресса теперь описана ОДИН раз** — списками `PROGRESS_MAPS`,
  `PROGRESS_NUMS`, `KEEP_ON_RESET` и функциями `blankProgress` / `ensureShape` /
  `clearResults` / `clearAll`. До этого она была переписана руками в четырёх
  местах (загрузка, смена ученика, сброс в панели, загрузка файла), и все четыре
  расходились. Тест проверяет не отдельные поля, а инвариант: **каждое** поле
  прогресса обязано сливаться при обмене с сервером и обнуляться при смене
  ученика. Эта проверка ловит будущие пропуски сама.
- **«Сбросить весь прогресс» в панели не сбрасывал разминки, «Ты и ИИ» и
  проекты** — расходился с очисткой при смене ученика. Теперь сброс стирает все
  результаты, но **оставляет** имя, расписание, песочницу и свои версии игр:
  это не результат, а работа ребёнка. Текст подтверждения переписан честно.
- **Загрузка прогресса файлом** сначала удаляла из `S` все ключи, а потом
  восстанавливала не все — `warmups`, `ailab`, `games`, `gamesPlayed` оставались
  `undefined`. Не падало только потому, что все записи в них случайно шли через
  `S.x = S.x || {}`. Теперь форму дополняет `ensureShape`.
- **«Перемешать заново» в разминке «собери из блоков» могло выдать ответ.**
  Стартовое перемешивание избегало правильного порядка, а кнопка тасовала без
  этой проверки. Замер по мутации: на упражнении из трёх блоков ответ выпадал
  в 32 нажатиях из 200. Перемешивание теперь одно (`shuffleOrder`).
- **Игра помечалась «твоя версия» после простой партии.** `onRun` сохранял код
  при каждом запуске, даже нетронутый. Теперь пометка ставится только если код
  отличается от исходного, а «Вернуть оригинал» её снимает.
- **Кнопка подсказки была скопирована четырежды**, и копии разошлись: в уроке
  не было проверки «а подсказки есть», поэтому урок без `hints` уронил бы
  страницу при первом нажатии (в `lessons.js` требование `hints` стояло только
  для разминок, «Ты и ИИ» и проектов). Теперь один `wireHint`, а требование
  `hints` — и для всех ста уроков.
- **Уход с экрана во время загрузки мира.** Урок, мир и проект дорисовываются
  после подгрузки `content/worldN.js`; если за это время ребёнок нажал другой
  раздел, запоздавшая отрисовка затирала уже показанный экран и запускала
  счётчик времени по уроку, который никто не открывал. Теперь каждый заход
  берёт номер (`claimScreen`), а отрисовка сверяет его (`screenStale`).
- **Мелочи:** шесть одинаковых циклов объединения множеств в `mergeProgress`
  свёрнуты в `mergeSet`; `weekStripHTML` больше не держит свою копию названий
  дней недели (есть `WD_SHORT`); в `statsGridHTML` локальная `solved` затеняла
  функцию `solved()` — переименована; `routeHash` считал `location.hash` семь
  раз — стал таблицей `HASH_SCREENS`; убрано мёртвое `CURRICULUM.flat`, которое
  вдобавок затеняло встроенный `Array.prototype.flat`.
- **Что проверено и НЕ оказалось проблемой:** движок быстрый (цикл на 100 000
  шагов — 114 мс, худший случай визуализатора в 800 кадров — 109 мс), в CSS
  ни одного дублирующегося селектора и ни одного неиспользуемого класса кроме
  `.pout` (класс без стилей, вреда нет), дублей ключей в объектах нет, id
  уроков, игр, разминок, заданий «Ты и ИИ» и проектов не пересекаются,
  повторяющихся блоков кода длиннее пяти строк в проекте больше не осталось.

**Проект в конце мира — механика готова, первый проект собран (1.17.0).**
Пункт №1 бэклога из анализа конкурентов. Решение фаундера из трёх предложенных:
**отдельный раздел вне сотни, но карточка показана прямо на карте мира**; и делаем
**один проект (Мир 1), а не все пять сразу** — если форма не зайдёт, переделывать
один.

- **Чем проект отличается от урока — не размером, а тем, что код ПЕРЕЕЗЖАЕТ между
  шагами.** Стартовый код шага N+1 — это то, что ребёнок написал на шаге N
  (`projectStartCode`); нет своего — берётся эталон прошлого шага. Поэтому в конце
  получается одна цельная вещь, а не четыре отдельные задачки. Это и есть весь
  смысл: момент «я собрал целую штуку», которого в курсе не было.
- **Первый проект — «Карточка героя»** (`js/projects.js`), 4 шага: рамка и имя →
  функция полоски → все характеристики циклом → всё в функцию и две разные
  карточки. Использует ровно инструменты Мира 1 (строки, умножение строк,
  f-строки, списки, `for`, `if/elif/else`, функции) — ничего из будущих миров.
- **Вне сотни:** звёзд и XP не даёт, в счётчик «N из 100» не входит. Прогресс —
  `S.projects[id] = { step, code, done }`. Бейдж «🏗 Строитель» за первый собранный.
- **Но на карте мира карточка есть** (`.projcard` в `screenWorld`, сразу за 20-м
  уроком) — иначе раздел вне сотни просто не заметят. Открывается, когда все
  готовые уроки мира пройдены; «Открыть все уроки» в панели наставника тоже
  открывает (это законно и покрыто тестом).
- **Финал** (`screenProjectDone`): вся программа целиком, запускается, кнопка
  «Забрать в песочницу» кладёт её в `S.sandbox`. Проверено в браузере — карточка
  героя рисуется.
- **Слияние устройств:** `step` и `done` берём дальние (max), а КОД — из более
  свежего сохранения, как песочницу. Складывать две версии программы нельзя,
  получится каша.
- **Покрыто всеми тремя тестами** (как «Ты и ИИ»): схема и «шаг обязан менять
  вывод» в `lessons.js`; все 5 программ проекта против python3 в
  `content-vs-python.js`; сквозной прогон в `full-run.js` — замок до прохождения
  мира, прохождение всех шагов эталонами, проверка что стартовый код шага НЕ
  проходит проверку, переезд кода, бейдж, «забрать в песочницу», слияние.
  Тест проверен мутацией: ломаю перенос кода — падает тремя сообщениями.
- **Что осталось было:** проекты для миров 2–5. **Написаны в 1.21.0** — см.
  «ЧТО СДЕЛАНО» вверху. Механику при этом трогать не пришлось вовсе, как
  и планировалось: только контент плюс расширение сквозного теста на все пять.

**Пробел с `enumerate` закрыт (1.16.0).** `enumerate` не объяснялся в сотне уроков
НИ РАЗУ — при этом ребёнок видел его в готовом коде игр (`js/games.js`, три игры),
а Миры 4 и 5 шесть раз пишут `for i in range(len(...))` — ту самую громоздкую форму,
которую `enumerate` и заменяет. То есть курс учил худшему варианту примером и не
показывал лучший.

- **Куда поставили.** Карточка теории в уроке `dict-loop` (Мир 2, «Перебор
  словаря»), пятой, сразу после карточки про `items()`. Логика места: `enumerate`
  — это списочный БЛИЗНЕЦ `items()`, обе штуки про «две переменные в цикле,
  распаковка пары». Задание урока не тронуто (оно про словарь и требует `items`),
  так что грабля 13 «задание списано с теории» не срабатывает.
- **Почему не отдельным уроком.** Сотня жёстко устроена как 5 миров по 20, `id`
  уроков менять нельзя (по ним хранится прогресс). Новый урок в Мир 2 не влезает
  без перетряски нумерации.
- **Плюс две разминки** в `js/warmups.js` — иначе «прочитал и забыл»:
  `predict-enumerate` (ловушка: `start=1` меняет только счётчик, а индексы второго
  списка остаются с нуля — отсюда `готово[н - 1]`; это самая частая ошибка
  на сдвиг единицы) и `blocks-enumerate` (пронумерованное меню; порядок держится
  на двух разных причинах — зависимость `len(меню)` от списка и порядок печати).
- **Единственность порядка у `blocks` проверена перебором**, а не на глаз: все 120
  перестановок пяти строк прогнаны через настоящий python3, тот же вывод даёт ровно
  одна. Это грабля 23, и тесты её НЕ ловят — проверка идёт по выводу, поэтому второй
  верный порядок прошёл бы незамеченным.
- Разминок стало 12, сверок с python3 — 578. Все пять наборов зелёные.
- **`zip` НЕ трогали, и это осознанно.** Он в курсе есть, но поздно — карточкой в
  `web-2` (Мир 5). Дыра тут меньше, чем была у `enumerate`, а перенос в Мир 2
  сделает карточку в Мире 5 повтором. Это решение фаундера, не моё.

**Щит для стрика (1.15.0).** Один пропущенный день больше не обнуляет серию —
это была главная причина бросить. Пункт 3 бэклога из анализа конкурентов
(у Mimo это «streak shield»). Сервер не трогали, новых файлов нет.

- **Щиты НЕ храним счётчиком.** В `S.shields` лежит множество ДАТ, на которые щит
  потрачен; сколько щитов заработано — вычисляется из числа дней занятий
  (`SHIELD_EVERY` = 5 дней на щит, запас `SHIELD_MAX` = 2). Причина та же, по
  которой стрик держится на `S.days`: счётчик разъехался бы при слиянии двух
  устройств, а множества сливаются объединением и всегда дают один ответ.
- **Серия считается по «закрытым» дням** — занимались ИЛИ день закрыт щитом
  (`coveredDays(days, shields)`). `activeOn` остался про НАСТОЯЩЕЕ занятие: по нему
  горит огонёк и работает напоминание по расписанию, иначе щит «отмечал» бы
  учебный день выполненным.
- **Срабатывает при возвращении, а не в полночь.** `markActiveToday` → `useShield`:
  щит закрывает вчерашнюю дыру в момент первого занятия. Статический сайт не может
  выполнить код, пока он закрыт (та же причина, что у напоминаний), и так честнее —
  щит награждает возвращение. Пока ребёнок не сел заниматься, серия показывает 0,
  поэтому на «Сегодня» отдельно пишется «щит наготове» (`shieldWouldSave`) — без
  этого экран выглядел бы как «всё пропало», хотя серия спасётся первым же уроком.
- **Закрывает ровно ОДИН день** и только если позавчера серия была: два пропуска
  подряд — серия честно начинается заново. Закрытый день показан щитом 🛡 в
  недельной полоске; запас — блоком под ней; после спасения один раз всплывает
  «Щит закрыл пропуск ДД.ММ» (`takeShieldNote`, показывается на первом же экране —
  «Сегодня» или окно победы).
- **Куда добавлено `S.shields`:** `mergeProgress` (объединение), `resetProgressLocal`,
  оба сброса в панели наставника, `pruneDays`. В панели наставника стрик теперь
  считается по закрытым дням и добавлена строка «Щиты: N из 2 (потрачено M)».
- **Покрыто тестом** `full-run.js` (блок «щит для стрика»): арифметика запаса
  (щит за 5 дней, потолок 2, списание потраченного), сквозной путь «вернулся после
  пропуска → щит сработал сам → серия 7», отказ на дыре в два дня, отказ при пустом
  запасе, объединение потраченных щитов при слиянии, блок на экране «Сегодня».
  Тест проверен мутацией: если убрать из `useShield` проверки, он падает тремя
  сообщениями. Все пять наборов зелёные.
- **1.15.1 — правка значка.** Щит выводился тусклым серым контуром: символ 🛡
  (U+1F6E1) по умолчанию рисуется ТЕКСТОМ, а не цветной иконкой, — ему нужен
  селектор варианта U+FE0F. Нашлось только визуальной проверкой в браузере, тесты
  такое не ловят в принципе. Если добавляешь новый значок и он выглядит блёкло —
  причина, скорее всего, эта же (у 🔥 и 📌 цветное начертание по умолчанию, у 🛡 нет).
- Заодно поправлены две устаревшие вещи в этом файле: раздел про обновление сайта
  (теперь Терминал, а не GitHub Desktop) и пункт бэклога про мусор `a.txt`/`t.csv` —
  этих файлов в репозитории давно нет.

**Раздел «Ты и ИИ» (1.14.0).** Новый раздел рядом с «Играми» и «Разминкой»
(кнопка «Ты и ИИ» в шапке, адрес `#ai`), вне сотни уроков. Восемь упражнений
(в 1.18.0 стало одиннадцать)
про то, как быть человеком над ИИ: точно ставить задачу, читать чужой код,
проверять результат. **ИИ сценарный** — «ответ ИИ» это заранее написанный нами
код прямо в упражнении, живую модель не зовём (офлайн, безопасно, ноль затрат
на API). Судья правды — наш мини-движок: он реально запускает «код от ИИ» и
показывает, где тот падает или врёт. Контент собран по заметке `docs/ai-module.md`.

- **Гибрид механик.** Раздел свой (прогресс в `S.ailab`, звёзд и XP не даёт,
  в счётчик «N из 100» не входит), но внутри упражнения трёх типов:
  `predict` (как разминка — открывается `makePredictStudio`, правильный ответ =
  вывод программы `code`), `code` и `fix` (как уроки — открываются `makeStudio`,
  сверка вывода с `solution`, у `fix` ещё бюджет правок `fixBudget`). Диспетчер —
  `openAILesson` в `app.js`; проверки — `runAIPredict` и `runAICheck`;
  финал — `winAI`. Экран-витрина — `screenAILab`.
- **Прогресс синхронизируется** (`S.ailab` добавлен в `mergeProgress` —
  объединение, как у разминок; и в `resetProgressLocal`). Пройденное упражнение
  держит дневной стрик живым (`markActiveToday`).
- **Файл `js/ailab.js`** (`window.AILAB`) подключён в `index.html` и в список
  сборки `build.js` (после `warmups.js`). Кнопка в шапке — `btn-ai`, роут `#ai`,
  экспорт в `window.__game` для тестов.
- **Покрыто всеми тремя тестами** (перебор `AILAB`): схема в `lessons.js`
  (predict запускается и печатает; у code заготовка работает; у fix заготовка
  СЛОМАНА, есть symptom, решение на 2..8 правок, needCode проходит эталон);
  сверка вывода с python3 в `content-vs-python.js` (predict → `code`, code/fix →
  `solution`, у code ещё `starter`); сквозной прогон в `full-run.js` (верный ответ
  засчитывается, исходная заготовка — нет). Все пять наборов зелёные, ноль ошибок JS.
- **Редакторский проход** (отдельный агент, свежий взгляд) подтвердил: арифметика
  во всех разборах верна, симптомы совпадают с поведением заготовок, прямых
  спойлеров в predict нет. Поправлено по замечаниям: несколько «последних
  подсказок» диктовали решение дословно — переписаны в наводки; в боссе список
  требований раскрывал, какая ветка условия сработает — убрано.
- **Тип `review` (шаг-вердикт «верно/неверно» перед починкой) не делали** —
  по рекомендации самой заметки: раздел выпущен на существующих `predict`/`fix`/
  `code`, а `review` стоит добавлять второй итерацией, если на живом ребёнке
  увидишь, что уроки 4 и 6 просят более явного шага-вердикта.

**Регистрация по имени (1.13.0).** При первом заходе (сервер подключён, ученик
не выбран) открывается экран регистрации: ребёнок вводит имя → `slugFromName`
делает из него код ученика (транслитерация + случайный суффикс) → аккаунт создан.
Есть вход по готовому коду (с другого устройства) и экран «Профиль» (кнопка 👤):
код, ссылка-приглашение `?kid=код`, «выйти/сменить». Имя — в `S.name`, едет с
прогрессом (в `mergeProgress` свежее побеждает). Смена ученика чистит локальный
прогресс (`resetProgressLocal`), чтобы не смешать двух детей на одном устройстве.
Настоящих паролей нет намеренно — всё на существующем «коде ученика» и синхро,
новый бэкенд не нужен. Функции в `app.js`: `screenRegister`, `screenAccount`,
`doRegister`, `doLogin`, `doLogout`, `slugFromName`, `needsRegister`. Гейт на
старте: `needsRegister()` → `screenRegister`. Покрыто тестом `full-run.js`
(валидный код из имени, случайность суффикса, слияние имени, рендер экрана,
полный путь регистрации). **Грабля теста:** старт теперь ведёт на регистрацию,
если кода нет, поэтому в `full-run.js` перед загрузкой подставляется
`localStorage kodokvest_code = "test-kid"` (эмуляция вернувшегося ученика), а
блок теста регистрации сам включает `CLOUD_CONFIG.url` обратно (выше его гасят
для проверки офлайна).

**Расписание занятий (1.12.0).** На экране «Сегодня» ребёнок отмечает дни недели,
когда планирует заниматься (переключатели Пн…Вс). В учебный день — напоминание
**только внутри сайта**: баннер на «Сегодня», отметка 📌 в недельной полоске и
колокольчик 🔔 на кнопке 🔥 в шапке. Хранится в `S.schedule.days` (номера дня
недели как у `getDay()`, 0=Вс…6=Сб). Расписание — настройка, поэтому в
`mergeProgress` побеждает более свежее сохранение (`fresher`/`older`), а не
объединение — иначе снятый день возвращался бы с другого устройства. Функции в
`app.js`: `scheduleDays`, `isStudyDay`, `toggleStudyDay`, `studyDue`. Плюс строка
«Дней подряд: N (рекорд M)» добавлена в сводку панели наставника (считается и для
чужого ученика — базовые функции стрика теперь принимают набор дней: `…In(days)`).
Покрыто тестом `full-run.js` (выбор/снятие дня, `isStudyDay`, слияние настройки).

**Дневной стрик и задача дня (1.11.0).** Новый экран «Сегодня» (кнопка
«🔥 Сегодня» в шапке, `#today`), вне сотни уроков, звёзд и XP не даёт. Смысл —
привычка заходить каждый день.

- **Стрик** считается не счётчиком, а по множеству дат в `S.days`
  (`"ГГГГ-ММ-ДД"` по местному времени). Так слияние двух устройств — это просто
  объединение множеств дней, и никакой перевод часов счётчик не сломает. День
  засчитывается за любое полезное действие: пройденный урок (`winLesson`) или
  решённую разминку (`winWarmup`). Функции в `app.js`: `dayKey`, `shiftDay`,
  `streakCurrent`, `streakBest`, `markActiveToday`, `weekStripHTML`.
- **Задача дня** — одна разминка на день, детерминированно выбранная по хэшу
  даты из уже проверенного пула `js/warmups.js` (`dailyPick`). На всех устройствах
  в один день задача одна и та же, каждый день новая — **сервер не нужен**.
  Открывается тем же движком разминок: `openWarmup(id, { daily:true })`; флаг
  меняет навигацию и финал. Выполнение — в `S.daily["ГГГГ-ММ-ДД"]`.
- **Грабля:** стрик держится на `S.days`, а не на числе — отдельный счётчик
  разъехался бы при слиянии. `S.days` и `S.daily` добавлены в `mergeProgress`
  (объединение), `S.days` ограничен ~800 днями (`pruneDays`). Даты для арифметики
  берём в полдень (`key+"T12:00:00"`), иначе летнее время перепрыгивает сутки.
- Покрыто тестом `full-run.js`: задача дня детерминирована по дате, её решение
  засчитывается и поднимает стрик. Плюс отдельная сверка математики стрика
  прогонялась в изоляции (пустая история, серия, дыра, рекорд из прошлого,
  прерванная серия, слияние двух устройств) — всё сходится.

**Визуализатор «машина времени» (1.10.0).** Новый раздел «Визуализатор» (кнопка
в шапке, `#viz`), вне сотни уроков. Ребёнок пишет/выбирает пример, жмёт «Показать
по шагам» — программа прогоняется, и на каждом шаге снимается состояние памяти.
Дальше листает историю вперёд/назад ползунком (и автопроигрывание). Видно:
подсвеченную текущую строку; переменные и контейнеры (списки/словари/кортежи/
множества) коробками; **стрелки-ссылки** — когда `b = a`, от обеих переменных
стрелка к ОДНОЙ коробке (одним цветом, с обоими именами в шапке). Это делает
зримой главную ловушку курса (алиасинг), которую словами объяснить почти нельзя.

- В движке добавлена `heapSnapshot(env, idMap, skip)` (экспортирована в `MiniPy`):
  неизменяемый снимок памяти с идентичностью объектов. `idMap` общий на прогон —
  один объект держит один id на всех шагах, поэтому алиасинг рисуется стрелками.
- **Грабля:** словарь в движке — это `Map`, где значение это пара `[ключ, значение]`,
  а ключ Map закодирован (`keyOf`). В снимке берём пару, иначе ключи выглядят как
  `'s:м'`. Уже починено, но помни при доработках.
- Отрисовка и плеер — в `js/app.js`: `screenViz`, `vizRecord`, `vizPlayer`,
  `vizMemoryHTML`, `vizObjHTML`, `vizDrawArrows`, примеры в `VIZ_EXAMPLES`.
  Стрелки — SVG поверх по реальным координатам; нет геометрии (jsdom/скрытый блок) —
  стрелки молча пропускаются, связь всё равно видна по цвету и именам в шапке.
- Покрыто тестом `full-run.js`: открыть, прогнать пример, пролистать, проверить
  распознавание алиасинга (a и b → один id) и настоящие ключи словаря.

**Мелкие правки той же сессии:** (1) починен баг, из-за которого на сайте миры 2–5
показывались «в работе», пока их файлы не подгрузятся — теперь `allWorldsContent()`
грузит все миры до первой отрисовки; (2) вход в панель наставника без `#`: работают
`.../kodokvest/admin` (через новый `404.html`), `?admin` и `#admin`.

**Механики `predict` (1.8.0) и `blocks` (1.9.0) — этап «больше интерактива».**
Добавлен раздел **«Разминка»** (кнопка в шапке, адрес `#warmup`) — рядом с
«Играми», **вне сотни уроков**. Оба вида упражнений живут в `js/warmups.js`
и различаются полем `type`:

- **`predict` («угадай вывод»)** — ребёнок читает готовую программу и до запуска
  пишет, что она напечатает; потом сверка, при ошибке показывается настоящий
  вывод и место расхождения. Пять упражнений.
- **`blocks` («собери из перемешанных строк»)** — строки программы перемешаны,
  ребёнок собирает их обратно перетаскиванием или кнопками ▲ ▼. Отступы у строк
  зафиксированы (их менять нельзя, только переставлять строки целиком) и служат
  подсказкой о вложенности. Верным считается порядок, дающий такой же вывод,
  как `code`. Пять упражнений. Станция — `makeBlocksStudio` в `app.js`.
- **Грабля blocks (важно):** проверка идёт по выводу, поэтому у задания должен
  быть ЕДИНСТВЕННЫЙ верный порядок — каждая содержательная строка обязана влиять
  на вывод. В `blocks-maxfn` первый вариант с `if` имел мёртвую ветку (тестовый
  вызов не задевал `return a`), и сломанную сборку засчитывало. Починено печатью
  двух вызовов с разными парами.

- Почему отдельным разделом, а не уроками: сотня жёстко устроена как «5 миров
  по 20», нумерация и прогресс завязаны на это. Трогать её ради нескольких
  упражнений — риск сломать «N из 100». Разминки — свой список, свой прогресс
  (`S.warmups`), звёзд и XP не дают, в счётчик сотни не попадают. Это решение
  фаундера из трёх предложенных вариантов.
- Прогресс разминок **синхронизируется** (добавлен в `mergeProgress`): разгаданное
  на одном устройстве не пропадёт на другом. Это просто «разгадано/нет».
- Обе механики закрыты тестами: схема в `lessons.js`, сверка вывода с python3
  в `content-vs-python.js` (вывод программы — это и есть правильный ответ),
  сквозной прогон в `full-run.js` (верный ответ засчитывается, неверный — нет;
  для blocks «неверный» = перевёрнутый порядок). Как добавить разминку — в
  `README.md`, раздел «Раздел „Разминка“».
- Редакторский проход чужими глазами (отдельный агент) прочитал все 10 упражнений
  двумя заходами. По predict: `intro`/подсказки выдавали ответ, `note` пересказывал
  вывод — исправлено, усилены ловушки (`predict-order`, `predict-division`), сняты
  спойлеры (`predict-alias`). По blocks: нашёл дыру с мёртвой веткой в `blocks-maxfn`
  (см. граблю выше) — починено; подсказки переписаны с «диктовки порядка» на «почему».

**Ещё раньше был добавлен раздел «Игры» (версия 1.7.0):** пять играбельных игр
в `js/games.js` (угадай число, камень-ножницы-бумага, крестики-нолики, викторина,
текстовое приключение). Работают в интерактивном режиме движка (input() ждёт ход),
код виден и его можно править. Все пять проходятся в `full-run.js`.

# ГДЕ ВСЁ ЛЕЖИТ

Репозиторий: https://github.com/fionov365-ai/kodokvest (публичный)
Живой сайт: https://fionov365-ai.github.io/kodokvest/ (GitHub Pages, ветка main, корень)
Панель наставника: адрес с `admin` на конце — работают `.../kodokvest/admin`
(через `404.html`), `?admin` и `#admin`. Код доступа `kodokvest-2026`.

Рабочая папка — клон репозитория (см. раздел про GitHub Desktop выше). Если она
не привязана к чату — попроси привязать `Documents/GitHub/kodokvest`.
**Первым делом прочитай `README.md` в корне** — там архитектура, схема урока и правила.
Про сервер прогресса — `cloud/README.md`.

**Проверка при заливке.** Свежая версия в `package.json` — сейчас 1.35.0.
**С 1.33.0 в корне лежат ещё три файла — `manifest.webmanifest`, `icon.svg`
и `sw.js`.** Их надо заливать вместе с остальными, иначе установка на
домашний экран и работа без сети просто не включатся (сайт при этом
работает как раньше).
Раздачей сайта занимается git (см. раздел про GitHub Desktop выше): фаундер
коммитит и пушит, Pages пересобирает. После пуша Pages обновляется не мгновенно —
подожди минуту и проверь `.../kodokvest/package.json` с обходом кэша.

# ЧТО УЖЕ СДЕЛАНО

**Все пять миров, 100 уроков.** Курс полный: от первого `print` до переноса
собственного проекта на настоящий компьютер.

**Движок** (`js/engine-mini.js`, мини-Python на JavaScript, ~3400 строк) умеет:
переменные, int/float, строки и f-строки с полным форматом (`{x:8}`, `{x:.2f}`,
`{x:02d}`, `{x:*^9}`, `{n:,}`, `{x!r}`, **вложенная ширина `{x:<{ширина}}`**),
старое форматирование через `%` и **`.format()` с номерами, именами и вложенными
скобками**, списки, словари, кортежи, множества, срезы, `if/elif/else`, `for`,
`while`, `break/continue`, `def` со значениями по умолчанию, `*args`/`**kwargs`,
распаковку `f(*список)`/`f(**словарь)`, `lambda`, включения всех видов,
**выражения-генераторы** (`sum(x for x in ...)` и `(x for x in ...)`),
цепочки сравнений, `sorted(key=...)`, `try/except/else/finally`, `raise`,
`assert`, свои исключения, полную иерархию исключений, классы (`__init__`,
`self`, методы, поля класса и объекта, `__repr__`, `__str__`, `__eq__`,
наследование, `super()`, `isinstance`), честный `type()`, `import`, декораторы,
`@dataclass`, файлы (`open`, `with`, чтение построчно, запись, дописывание,
`encoding`, `newline`), генераторы (`yield`, `yield from`, `next`),
**модули**: `math`, `random`, `statistics`, `time`, `json`, `csv`, `datetime`,
`collections`, `itertools`, `re`, `pathlib`, `dataclasses`.
Ошибки по-русски с номером строки, а `str(e)` внутри `except` даёт ровно
питоновский английский текст. Защита от вечных циклов и бесконечной рекурсии.
Пошаговое выполнение через генераторы.

**Добавлено в этой сессии (пятая волна, под Мир 5):**

- **`input()`** — читает не с клавиатуры, а из заранее записанных ответов.
  У задания поле `task.stdin` (список строк), у примера теории — `theory[i].stdin`.
  Ученик видит и правит их в панели «ответы для input()» справа. Приглашение
  печатается без перевода строки, ответ в вывод не попадает — ровно как у
  python3, когда ввод приходит по трубе, поэтому сверка остаётся точной.
  На конце ответов — `EOFError` с питоновским текстом.
- **`__name__`**: у главной программы `"__main__"`, у подключённого файла — его
  имя. Работает `if __name__ == "__main__"`.
- **`__doc__`** — строки документации у функций, методов, классов и модулей.
- **`dir()`** — только для того, что написал ученик (свой класс, свой объект,
  свой файл-модуль). У встроенных типов набор служебных имён свой в каждой
  версии Python, поэтому там `dir()` честно отказывается работать.
- **`getattr`, `hasattr`, `callable`, `any`, `all`**.
- **`json.JSONDecodeError`** — наследник `ValueError`, не встроенное имя.
- **`split(sep, maxsplit)` и `rsplit`**.
- **`.format()`** переписан целиком: раньше понимал только пустые `{}` и молча
  оставлял `{имя}` в тексте как есть — строка выглядела правильной, а внутри
  был шаблон.
- **Выражения-генераторы** — раньше не разбирались вообще, и ошибка врала
  про незакрытую скобку.
- **Вложенная ширина в f-строке** `f"{x:<{ширина}}"` — раньше молча игнорировалась.

**Слой Runtime** (`js/runtime.js`): работает адаптер `mini`, адаптер `pyodide` —
честная заглушка. **Пометок `engine:"pyodide"` в программе больше нет нигде.**

**Сервер прогресса** — работает. Yandex Cloud, каталог `kodokvest`:
Cloud Function `kodokvest-progress` (`d4eog8isvkbphfav8n76`) плюс приватный бакет
`kodokvest-progress-7f3a91`, смонтированный к функции как папка
`/function/storage/progress`. Адрес прописан в `js/cloud-config.js`.
Прогресс **сливается**, а не перезаписывается. Код ученика — настройка устройства.

**Панель наставника**: сводка, таблица всех 100 уроков со статистикой,
открыть/зачесть/сбросить урок, снятие замков, XP и бейджи, просмотр чужого
прогресса по коду, список учеников по ключу наставника, перенос файлом.

**Механики заданий**: `code`, `fix`, задание **из нескольких файлов**,
**файлы с данными** (`task.data`), **ответы для `input()`** (`task.stdin`),
**показанный, но не запускаемый код** (`theory[i].show` + `theory[i].showNote`) —
для настоящего Flask, pandas, matplotlib и команд терминала, проверка
**скрытыми тестами** (`check.kind = "tests"`), проверки `needCode` / `needText` /
`noCode` и свои функции проверки в `CUSTOM` (`app.js`).
`predict` («угадай вывод») и `blocks` («собери из перемешанных строк») **сделаны** —
живут не в уроках, а в разделе «Разминка» (`js/warmups.js`, типы `type:"predict"`
и `type:"blocks"`). Внутри самих 100 уроков этих типов пока нет.

**Пять наборов тестов**, все зелёные, одной командой `npm test`:
1. `tests/engine-vs-python.js` — 151 программа через `python3` и через движок
   (точное число названо в README, и тест сам следит, чтобы они не разошлись)
2. `tests/lessons.js` — решения, заготовки, примеры, схема всех уроков + схема разминок
   и «Ты и ИИ»; у типа `review` вердикт и `badLine` вычисляются, а не берутся из
   задания; каждый мир обязан назвать `engine` явно
3. `tests/content-vs-python.js` — 598 сверок содержания с python3 (в т.ч. 14 разминок,
   11 заданий «Ты и ИИ» и 5 программ проекта)
4. `tests/cloud-function.js` — серверная функция на временной папке
5. `tests/full-run.js` — вся игра, 5 игр, 19 разминок, визуализатор, шесть проектов и 15 упражнений
   «Ты и ИИ» в jsdom (у трёх `review` проходится вся цепочка: неверные вердикты,
   верный вердикт, неверная строка, верная строка, доказательство), ноль ошибок JS

Плюс служебный `tests/try-one.js файл.py` — сверить одну программу вручную.
Он понимает директивы `#!stdin: текст` (ответ для `input()`) и `#!data: имя.csv`
(файл с данными рядом с программой).

# ЧТО СДЕЛАНО В ПОСЛЕДНЮЮ СЕССИЮ

**Мир 5 «Профессия» целиком, 20 уроков, плюс работа по движку под них.**
Порядок был обратный привычному: сначала движок и механики, потом уроки.

**Главное решение сессии: настоящий Pyodide не подключаем.**
Разбор был такой. Обещание «Pyodide даст pandas и Flask» оказалось неверным:
внутри вкладки браузера нет сетевых розеток и нельзя слушать порт, поэтому
Flask не запустится, `requests` не работает, а git к Python вообще отношения
не имеет. То есть 35–40 МБ (не 10–12: ядро плюс numpy плюс pandas) и 15–40
секунд ожидания покупали ровно четыре урока про данные, а двенадцать остальных
всё равно пришлось бы объяснять, а не запускать. Ценой были бы: игра одним
файлом офлайн, сквозной тест в jsdom для двадцати уроков и ручная заливка
на GitHub. Вместо этого Мир 5 разбирает, **что эти инструменты делают внутри**,
а настоящий код показывает рядом через `theory[i].show`:

- **quest-1..4** — текстовая игра на классах и настоящем `input()`;
- **data-1..4** — таблица как список словарей, чистка, группировки, гистограмма
  палочками; рядом показан тот же код на pandas и matplotlib;
- **api-1..4** — ответы сервисов записаны в файлы, клиент с кэшем;
  рядом показан `requests.get`;
- **web-1..4** — ученик пишет свой мини-Flask: декоратор `@маршрут`, адреса
  с переменной частью, разбор строки запроса, сборка статического сайта
  в файлы (то есть ровно то, как выложен сам «Кодоквест»);
- **git-1, git-2** — свой мини-diff и слияние веток с конфликтом; рядом
  показаны настоящие команды;
- **docs** — свой маленький `help` на `__doc__`, `dir`, `getattr`, `callable`;
- **final** — проект из трёх файлов с `if __name__ == "__main__"` и разбором,
  что именно сделать на компьютере.

**Найдены и исправлены расхождения движка с настоящим Python** — каждое
закрыто программой в корпусе сверки: выражения-генераторы не разбирались
вовсе; `.format()` понимал только пустые `{}`; вложенная ширина `{x:<{ш}}`
молча игнорировалась; `split` не принимал второй аргумент; `rsplit` не было;
`__doc__` всегда возвращал `None`.

**Исправлены две ошибки в самой механике проверок:**

1. `needCode`/`noCode` подставлялись в регулярное выражение без экранирования.
   Требование `"max("` рушило проверку целиком, а после русской буквы граница
   слова `\b` не срабатывает — в JavaScript `\w` это латиница. Теперь есть
   `codeHas()`: спецсимволы экранируются, `\b` приклеивается только к латинским
   краям. **Записывай в `needCode` обычный текст, а не регулярку.**
2. `needCode`/`needText`/`noCode` смотрели только главный файл. В многофайловом
   уроке нужная строчка законно живёт в подключённом — теперь проверяются все
   файлы задания.

**Тест `lessons.js` научился ловить ещё три вида ошибок автора:**
эталонное решение обязано проходить собственные `needCode`/`needText` и не
нарушать `noCode`; `input()` без `task.stdin` (и наоборот); карточка теории
без `demo` обязана иметь `show`.

**Редакторский проход чужими глазами** (отдельный агент прочитал все 20 уроков)
нашёл 35 замечаний, исправлено 30. Главное:

- **семь уроков были списаны с теории**: функция из задания слово в слово
  лежала в примере выше (`data-1`, `data-3`, `api-1`, `api-2`, `web-1`,
  `web-4`, `git-1`). Все семь примеров переписаны на другую форму данных:
  идея передаётся, а строчку списать нельзя;
- **восемь требований не проверялись**: в сценариях `input()` не встречались
  ни грязные команды, ни незнакомые глаголы, ни пустой инвентарь; в данных
  не было ни одной ничьей, из-за чего сортировка по алфавиту ничего не меняла;
  «не порти исходный список» в `git-2` вообще нельзя было заметить по выводу.
  Исправлено данными и сценариями, а не смягчением требований;
- **четыре числа и цитаты расходились с эталоном** (`data-4`, `api-4`, `final`,
  `api-2`) — ребёнок искал бы несуществующую ошибку;
- в `api-4` две разные температуры давали одинаковую полоску из-за банковского
  округления — ширина шкалы изменена;
- утверждение в `data-3` про порядок словаря было неверным, а `isdigit()`
  в `data-2` подавался без оговорки, что он не примет ни минус, ни точку.

# ЧТО МОЖНО ДЕЛАТЬ ДАЛЬШЕ

**~~Дожать понятность подсказок вне сотни уроков~~ — СДЕЛАНО в 1.68.0.**
Проход по отзыву ребёнка (05.09) закрыт целиком: уроки в 1.66–1.67,
тренировочные разделы в 1.68. Стандарт на будущие подсказки: объясняет
явно, а не намекает; код в \`обратных кавычках\`; пример `code` — образец
приёма на других числах, не ответ; без жаргона. За форматом следит
`checkHints()` в tests/lessons.js (все разделы) и tests/homework.js.

Этап «больше интерактива». По плану фаундера, в порядке приоритета:

1. ~~Механика `predict` («угадай вывод»)~~ — **сделано** (раздел «Разминка»).
2. ~~Механика `blocks` («собери из перемешанных строк», drag-and-drop)~~ —
   **сделано** (раздел «Разминка»).
3. ~~Визуализатор списков и словарей в пошаговом режиме~~ — **сделано**
   (раздел «Визуализатор», с перемоткой и стрелками).
4. ~~Дневной стрик и ежедневная задача~~ — **сделано** (раздел «Сегодня»:
   стрик по множеству дней `S.days` и детерминированная задача дня из пула
   разминок). Это был последний пункт исходного плана «больше интерактива».
5. ~~Расписание занятий (дни недели + напоминание в приложении)~~ — **сделано**
   (раздел «Сегодня», `S.schedule.days`).
6. ~~Регистрация по имени~~ — **сделано** (1.13.0, средний вариант, см. выше).

**Если вернётся тема полных аккаунтов (логин/пароль).** Фаундер сначала просил
именно их, но после разбора выбрал средний вариант (регистрация по имени). Полный
вариант сознательно не делали, и **в 1.18.0 это подтверждено ещё раз**: вопрос
задан прямо, ответ — оставить как есть. Что важно помнить, если решат вернуться:

- **Кросс-устройство уже работает** без паролей — через код ученика + сервер
  (прогресс сливается), а регистрация по имени дала этому дружелюбный вход.
- **Полный вариант** = переписать Cloud Function под signup/login с хешем пароля
  и токенами, защитить данные токеном, добавить сброс пароля. Заметно больше
  работы и — главное — ответственность за пароли детей и безопасность (rate-limit,
  утечки, приватность). Делать только после явного подтверждения, что нужен именно
  он. И это меняет публичную инфраструктуру — сперва подтверждение у фаундера.

Идеи на будущее: пополнять «Разминку» (это только контент в `js/warmups.js`) —
задача дня автоматически подхватит новые упражнения из пула; пополнять «Ты и ИИ»,
в том числе новыми `review` (механика есть, нужен только контент: `claim`, `code`,
`truth`, `probe`, `verdict`, `badLine`); ~~дать за длинный стрик бейдж~~ —
**сделано (1.19.0)**, `week` и `month`; довести визуализатор — например,
показывать стек вызовов внутри функций, подсвечивать, что именно изменилось на
шаге — ~~**сделано (1.20.0)**~~, см. «ЧТО СДЕЛАНО». Пополнять **шпаргалку** —
это тоже только контент (`js/cheatsheet.js`) — ~~сейчас 86 записей и почти нет
Мира 5~~ **сделано в 1.26.0: 104 записи, закрыт Мир 4.** Про Мир 5 см. разбор
в «ЧТО СДЕЛАНО»: его там почти не может быть.

**Что осталось открытым после 1.24.0:**

- ~~**Сертификат и портфолио из проектов.**~~ — **сделано (1.24.0),** раздел
  «Портфолио». ~~Сертификаты за разделы вне сотни~~ — **сделано (1.37.0):**
  «Разминка» и «Ты и ИИ», всего сертификатов восемь. Осталось одно: показ
  портфолио тому, у кого нет этого устройства (публичная ссылка). Это упирается
  в приватность и решается только с фаундером.
- **Промежутки повтора выбраны на глаз** (`REVIEW_STEPS = [2, 7, 21]`, порог
  «трудного» `REVIEW_HARD = 3`). Как и `SHIELD_EVERY`, это одна строка в `app.js`.
  Менять имеет смысл после того, как ребёнок реально позанимается: пока живого
  наблюдения нет, любая другая цифра будет такой же выдумкой.
- ~~**Дыры в движке из 1.21.0:** `re.compile` и неявная склейка строковых
  литералов.~~ — **закрыты в 1.22.0.**
- **Пополнять новые механики — это только контент.** «Поймай ИИ» (`type: "catch"`
  в `js/ailab.js`: нужны `claim`, `code` со СПРЯТАННЫМ багом, `truth`, `probe`),
  «Предскажи память» (`type: "memory"` в `js/warmups.js`: `code`, `stop`, `ask`).
  Обе механики и обе сверки с python3 уже написаны, трогать их не надо.
- **Ни одна новая механика не проверена на живом ребёнке.** «Предскажи память»
  может оказаться слишком трудной, «поймай ИИ» — наоборот. Сначала дать
  позаниматься, потом решать, чего добавить. **С 1.30.0 в этом списке ещё три:**
  цель по шагам (не окажется ли «не дороже автора» слишком легкой на коротких
  уроках и недостижимой на длинных), разбор своей программы (будут ли им
  пользоваться без напоминания) и «Своё задание» (главный вопрос — есть ли
  кому отправить ссылку; без адресата раздел мёртв). Все три настраиваются
  одной цифрой или одной строкой текста, поэтому менять что-то до наблюдения
  за ребёнком незачем.
- ~~**Экран уроков не хранит черновик.**~~ — **закрыто (1.25.0), и в 1.30.0 это
  окупилось:** именно черновики позволили увести ребёнка с урока в визуализатор
  и вернуть обратно с сохранённым кодом. ~~Ещё не покрыты черновиками **шаги
  проектов**~~ — **закрыто (1.37.0):** у каждого шага свой черновик, ключ
  `proj-<id>-<шаг>`. Кнопки «Разобрать» в проектах по-прежнему нет — это
  отдельное решение, а не следствие черновиков.

Отдельно, не из основного плана:

- ~~**Пробел в программе: `enumerate`.**~~ — **закрыт (1.16.0).** Карточка в
  `dict-loop` (Мир 2) плюс две разминки.
- ~~**`zip` объясняется поздно** — карточкой в `web-2` (Мир 5).~~ — **закрыт
  (1.18.0).** Решение фаундера: перенести в Мир 2 (урок `tuples`) И переписать
  карточку в Мире 5 из «Новое» в «Вспомним», чтобы она не стала повтором.
  Подробности — в «ЧТО СДЕЛАНО В ПОСЛЕДНИЕ СЕССИИ».
- **Папок-дублей на диске несколько** (`kodokvest`, `kodokvest-site`,
  `kodokvest-site 2`). Рабочая — та, где `package.json` показывает свежую
  версию и есть `js/warmups.js`. Легко открыть старую копию — проверяй версию.

## Из анализа конкурентов (2026-08-27)

Разбор конкурентов — Codédex, CodeCombat, Mimo, SoloLearn, Алгоритмика/Кодабра.
Контекст фаундера: сначала на нём занимается ребёнок, в перспективе — онлайн-школа
(веб, SQL и дальше), позиционирование не «работа джуна», а «думать, создавать и
командовать ИИ». Отсюда, в порядке приоритета:

1. ~~**Проект в конце каждого мира.**~~ — **закрыт полностью (1.21.0).**
   Механика с 1.17.0, все пять проектов собраны в 1.21.0. Форма выбрана
   фаундером: отдельный раздел вне сотни, но карточка на карте мира.
2. ~~**Раздел «Ты и ИИ».**~~ — **сделано (1.14.0), расширено в 1.18.0.** Одиннадцать
   упражнений вне сотни, ИИ сценарный, собран из `docs/ai-module.md`. Файл
   `js/ailab.js`, экран `screenAILab`/`openAILesson`, раздел `#ai`. Типов теперь
   ПЯТЬ: к `predict`/`code`/`fix` добавился ~~`review`~~ — **сделано (1.18.0)**,
   а в 1.23.0 ещё и `catch` («докажи, что ИИ неправ»),
   решение фаундера. Что осталось на будущее: пополнять раздел новыми упражнениями
   (это только контент в `js/ailab.js`).
3. ~~**Щит для стрика** (у Mimo — «streak shield»).~~ — **сделано (1.15.0).**
   Щит за каждые 5 дней занятий, в запасе до двух, закрывает один пропуск в момент
   возвращения. Хранится множеством дат `S.shields`, а не счётчиком. **Щедрость
   обсуждена в 1.18.0: оставляем 5.** Причина — живого наблюдения ещё нет, а
   `SHIELD_EVERY` в `app.js` это одна цифра, поправить можно за минуту. Менять
   имеет смысл после того, как ребёнок позанимается.
4. ~~**Живые подсказки по конкретной ошибке ученика** (как у CodeCombat).~~ —
   **сделано (1.19.0), но не там, где ожидалось.** Падения движок и так объяснял
   отлично (проверил 18 типовых ошибок). Дыра была в другом: код, который НЕ
   падает, а отвечает не то. Теперь `whyDiffer` называет причину словами и
   показывает невидимые пробелы значками. Подробно — в «ЧТО СДЕЛАНО».
5. ~~**Довести Pyodide до рабочего состояния**~~ — **решено 02.09.2026: НЕ
   подключаем.** Спор двух документов закрыт решением фаундера, цифры и условие
   пересмотра — в «ЧТО СДЕЛАНО», запись 1.37.0. Вопрос закрыт: не переоткрывать
   без его слова.
   Самодельный `engine-mini.js` — отличная офлайн-фишка и для ребёнка её хватает,
   но для коммерческой школы он станет гирей и будет расходиться с настоящим
   Python. Перед продажей нужен настоящий Python как основной или «продвинутый»
   режим. Мини-движок оставить как офлайн/учебный режим.
6. ~~**Сертификат и портфолио из проектов.**~~ — **закрыт (1.24.0).** Раздел
   «Портфолио»: сводка, шесть проектов с кодом ребёнка и шесть печатаемых
   сертификатов. Сертификат даётся за уроки мира ПЛЮС собранный проект — в этом
   и был смысл пункта 1. Для школы осталось одно: показать портфолио тому, у кого
   нет этого устройства (публичная ссылка), а это вопрос приватности — к фаундеру.

7. ~~**Кабинет родителя.**~~ — **первый шаг сделан (1.21.0):** блок «Что было за
   неделю» в панели наставника. Для семьи этого достаточно. Для школы понадобится
   отдельный вход и несколько учеников — это уже про аккаунты, см. выше.

Долгосрочное (не делать сейчас, только держать дверь открытой): настоящие
аккаунты с оплатой и кабинет учителя/родителя — это уже про школу, а не про
продукт. См. раздел про полные аккаунты выше. Архитектура к этому готова: контент
отделён от движка, у миров есть поле `engine`, id уроков зафиксированы (прогресс
переживёт переезд на аккаунты). Веб и SQL как новые предметы текущая структура
тянет почти без изменений — принципиально другие предметы (не код) потребуют
новых типов проверки (`check`).

# ГРАБЛИ, НА КОТОРЫЕ УЖЕ НАСТУПАЛИ

Прочитай это до начала работы — сэкономит полсессии.

0. **Всё, что в коде запуска трогает разметку, пишется с проверкой `if (b)`.**
   Одна переименованная кнопка иначе роняет старт целиком — причём не у того,
   кто правит, а у того, у кого в кэше вчерашний скрипт. Так и случилось после
   1.34.0: пустой экран на живом сайте (разбор — в «ЧТО СДЕЛАНО» сверху).
1. **Порядок элементов множества совпасть не может.** Правило проекта: множество
   печатается только через `sorted()`. В корпус сверки такие случаи не добавлять.
2. **`build.js` собирает «голову» страницы из `index.html`.** Не собирай файл
   без `<meta charset="utf-8">` — при открытии с диска весь русский текст
   превращается в «РљРѕРґРѕРєРІРµСЃС‚». В `tests/full-run.js` на это есть проверка
   по байтам, не убирай её.
3. **Проверяй тот файл, который открывает человек.** Один раз тест сам оборачивал
   сборку в правильный документ и поэтому пропустил поломку кодировки.
4. **Сортировка в Python устойчивая.** `reverse=True` — смена знака сравнения,
   а не переворот готового массива.
5. **Формат в f-строке отделяется двоеточием только на верхнем уровне.**
   Внутри скобок это срез, внутри кавычек — просто символ.
6. **У заданий `fix` есть бюджет правок** (`fixBudget`). Если у задания есть
   короткий обходной путь, поставь бюджет вручную **и добавь путь в `BYPASS`
   в `tests/full-run.js`**.
7. **Задания со случайностью нельзя проверять сравнением вывода** — для них
   `check: { kind: "custom", fn: "..." }` в объекте `CUSTOM` в `app.js`.
8. **Порядок уроков — это ограничение.** В уроке можно использовать только то,
   что объяснено раньше. Если конструкция нужна раньше своего урока — скажи об
   этом прямо в тексте.
9. **`id` уроков менять нельзя** — по ним хранится прогресс.
10. **Печатать функцию или объект без `__repr__` нельзя** — Python показывает
    адрес в памяти, повторить его невозможно.
11. **Целые числа больше 2^53 считаются неточно** — это JavaScript.
    `math.factorial` отказывается работать выше 20!.
12. **Заготовка обычного задания обязана запускаться.** Ребёнок открыл урок,
    нажал «Запустить» — он должен увидеть работающую, пусть и неправильную,
    программу. Заготовка задания `fix`, наоборот, обязана быть сломанной.
13. **Задание не должно быть списано с теории того же урока.** Это самая частая
    ошибка: в прошлой сессии таких нашлось четыре, в этой — семь. Проверяй сам:
    если решение собирается копированием строк из примеров сверху — урок пустой.
    Приём, который сработал: показывать в теории **ту же идею на другой форме
    данных** (кортежи вместо словарей, список вместо словаря, `.txt` вместо `.csv`).
14. **Требование, которое не проверяется, — обман.** Причём проверка вывода
    ловит требование, только если его нарушение **меняет вывод**. Требование
    вида «сделай через такую-то конструкцию» проверяется исключительно через
    `needCode`/`needText`/`noCode`. И отдельно: **сценарий обязан задевать
    каждую ветку**. Если в `task.stdin` нет ни одной грязной команды, требование
    «убирай пробелы» не проверяется ничем.
15. **Ничего, что зависит от текущего времени** (`date.today()`, `datetime.now()`,
    `time.time()`), в примерах и решениях не печатаем.
16. **В уроках про файлы всегда пишем `encoding="utf-8"`,** а для csv ещё и
    `newline=""`.
17. **В `needCode` и `noCode` пиши обычный текст, а не регулярку** — он
    экранируется. Раньше это молча ломало проверку целиком.
18. **Многофайловый урок: `ed.getCode()` — это только главный файл.** Проверки
    требований смотрят все файлы, но если пишешь новый код в `app.js` — помни.
19. **Выражения-генераторы вычисляются сразу, а не по одному.** Для конечных
    данных разницы нет; на бесконечном источнике настоящий Python отдал бы
    первое значение мгновенно, а тренажёр упрётся в защиту от вечного цикла.
20. **`help()` в тренажёре нет намеренно**, а `dir()` работает только со своими
    объектами: вывод обоих у каждой версии Python свой, подделывать его нечестно.
21. **Разминка (`predict`): `code` — это и есть правильный ответ.** Верным считается
    вывод этой программы, поэтому она обязана запускаться без ошибок, что-то
    печатать и совпадать с python3. Сравнение снисходительно к хвостам: хвостовые
    пробелы в строках и пустые строки в конце игнорируются — упражнения, весь смысл
    которых в хвостовых пробелах, сюда не годятся.
22. **Прогресс разминок — отдельно от звёзд.** Он в `S.warmups`, добавлен в
    `mergeProgress`. Не клади разминки в `S.stars`: туда смотрит счётчик «N из 100».
23. **Разминка (`blocks`): у задания должен быть единственный верный порядок.**
    Проверка идёт по выводу, значит каждая содержательная строка обязана влиять
    на результат — иначе сломанную по логике сборку засчитает. Классический
    промах: функция с `if`, у которой тестовый вызов не задевает одну из веток
    (проверяй на входе, где `if` реально срабатывает, или печатай два вызова).
    Отступы у строк зафиксированы — собери задание так, чтобы по отступам
    структура читалась однозначно.
24. **Визуализатор: словарь — это `Map` из пар `[ключ, значение]`, ключ Map
    закодирован (`keyOf`).** В `heapSnapshot` берём `pair[0]`/`pair[1]`, а не сырой
    ключ Map, иначе ключи выглядят как `'s:м'`. Снимки должны быть неизменяемыми
    (сериализуем значения на каждом шаге копией) — иначе поздние мутации испортят
    уже показанные кадры.
25. **Дневной стрик держится на множестве дат `S.days`, а не на счётчике.**
    Отдельное число разъехалось бы при слиянии двух устройств; множество дней
    сливается объединением. `S.days` и `S.daily` обязаны быть в `mergeProgress`.
    Даты для арифметики бери в полдень (`key+"T12:00:00"`), иначе переход на летнее
    время перепрыгивает сутки. Задачу дня выбирай из уже проверенного пула
    разминок (`dailyPick`), своего непроверенного контента не добавляй.
26. **Расписание — настройка, а не результат: сливай «свежее побеждает».**
    `S.schedule` в `mergeProgress` берётся из более свежего сохранения
    (`fresher`/`older`), как песочница, а НЕ объединением. Объединение вернуло бы
    снятый на одном устройстве учебный день с другого. Напоминания только внутри
    сайта — статический сайт не пришлёт уведомление, когда он закрыт (для этого
    нужен был бы Telegram-бот или web-push; фаундер выбрал «только внутри сайта»).
27. **Регистрация: смена ученика чистит локальный прогресс перед загрузкой.**
    `doLogin`/`doRegister` зовут `resetProgressLocal()` — иначе слияние при pull
    смешало бы двух детей на одном устройстве (текущая модель синхро всегда
    сливает). Код из имени (`slugFromName`) обязан пройти `Cloud.validCode`
    (3–32 знака, [a-z0-9_-], начинается с буквы/цифры) — транслитерация плюс
    случайный суффикс это обеспечивают. **Грабля теста:** старт теперь ведёт на
    регистрацию, если кода нет; в `full-run.js` перед boot подставлен
    `localStorage kodokvest_code`, а блок теста регистрации сам возвращает
    `CLOUD_CONFIG.url` (выше его гасят для проверки офлайна).
28. **«Ты и ИИ»: у `code`/`fix` правильный вывод задаёт `solution`, а не `check.lines`.**
    Проверка (`runAICheck`) сверяет вывод ученика с выводом эталона на лету —
    отдельного списка строк не держим. Значит `solution` обязан запускаться и
    совпадать с python3 (это ловит `content-vs-python.js`). Заготовку `fix` python3
    НЕ проверяем: она сломана нарочно (у №5 — выдуманный `.average()`). У `predict`
    внутри раздела всё как в разминке: правильный ответ — это вывод `code`, поэтому
    он обязан запускаться и что-то печатать. `S.ailab` — отдельно от звёзд и от
    `S.warmups`, добавлен в `mergeProgress` (объединение) и `resetProgressLocal`.
    Новый файл подключается в ТРЁХ местах: `index.html`, `build.js` (список
    `scripts`) и в `eval` внутри `lessons.js`/`content-vs-python.js` — забудешь
    любое, и раздел либо не откроется, либо выпадет из проверок.

29. **Щит для стрика — множество дат, а не счётчик.** `S.shields` хранит ДАТЫ,
    на которые щит потрачен; сколько заработано — вычисляется из числа дней в
    `S.days`. Счётчик щитов разъехался бы при слиянии двух устройств ровно так же,
    как разъехался бы счётчик стрика (грабля 25). `S.shields` обязан быть в
    `mergeProgress` (объединение), `resetProgressLocal`, обоих сбросах панели
    наставника и `pruneDays`. Серия считается по closed-дням (`coveredDays`), но
    `activeOn` остаётся про НАСТОЯЩЕЕ занятие — иначе щит «выполнял» бы учебный
    день по расписанию и гасил напоминание. Крайний случай слияния (оба устройства
    офлайн потратили последний щит на разные дни) даёт «потрачено больше, чем
    заработано» — это допустимо: запас уходит в ноль и восстанавливается занятиями.

30. **Проект: код переезжает между шагами — значит и грабли у него свои.**
    Стартовый код шага N+1 это код ребёнка с шага N (`projectStartCode`), поэтому
    (а) `solution` каждого шага обязан быть ПОЛНОЙ программой на конец шага, а не
    только добавкой; (б) вывод шага обязан отличаться от вывода прошлого — иначе
    шаг пустой, и это проверяет `lessons.js`; (в) при слиянии двух устройств `step`
    и `done` берём дальние, а КОД — из свежего сохранения, как песочницу: сложить
    две версии программы нельзя. `S.projects` обязан быть в `mergeProgress`,
    `resetProgressLocal` и обоих сбросах панели наставника. И `js/projects.js`
    подключается в ТРЁХ местах (index.html, build.js, eval в двух тестах) — та же
    грабля, что и с `ailab.js`.

31. **Новое поле прогресса дописывается в ОДИН список, а не в четыре места.**
    Форма прогресса живёт в `PROGRESS_MAPS` / `PROGRESS_NUMS` / `KEEP_ON_RESET`
    в начале `app.js`; `mergeProgress`, смена ученика (`clearAll`), сброс в
    панели (`clearResults`) и загрузка файлом (`ensureShape`) читают их сами.
    Раньше список был переписан руками четыре раза, и `S.games` с
    `S.gamesPlayed` выпали сразу из двух копий. В `full-run.js` стоит инвариант:
    каждое поле обязано сливаться и обнуляться — он поймает пропуск сам.
    Единственный осознанный «не как все» — `KEEP_ON_RESET`: сброс прогресса в
    панели наставника НЕ трогает имя, расписание, песочницу и свои версии игр,
    потому что это не результат, а работа ребёнка. Смена ученика чистит и их.

32. **Всё, что тикает, гасится в `enterScreen()`.** Экраны меняются заменой
    `app.innerHTML`, и ни один `setInterval` / `requestAnimationFrame` сам от
    этого не умирает: он продолжает работать по разметке, которой на странице
    уже нет. Так прожил таймер визуализатора. Завёл таймер — добавь его
    остановку в `enterScreen`, а внутрь тика вставь проверку
    `document.body.contains(...)` как вторую страховку. Слушателей на `window`
    в экранных функциях не вешать вообще: они копятся по одному на каждый
    заход. Общий обработчик `resize` лежит внизу `app.js` — дописывай в него.

33. **Экран, который рисуется асинхронно, обязан сверить номер захода.**
    Урок, мир и проект ждут `content/worldN.js`. Пока файл едет, ребёнок может
    уйти в другой раздел — и запоздавшая отрисовка затрёт новый экран. Перед
    асинхронным вызовом берётся `claimScreen()`, в колбэке первая строка —
    `if (screenStale(seq)) return;`.

34. ~~**Экран урока не хранит черновик — значит из урока нельзя никуда уходить.**~~
    **ЗАКРЫТО в 1.25.0:** черновики есть (`S.drafts`, см. «ЧТО СДЕЛАНО»), уход с
    урока больше ничего не стирает, и новый экран посреди урока теперь допустим.
    Шпаргалку оверлеем оставляем — так вышло лучше. В силе остаётся хвост этой
    грабли: тест на сохранность писать надо по ДОКУМЕНТУ:
    `getSession().studio` продолжает возвращать живой объект даже после
    `app.innerHTML = ""`, поэтому первая версия проверки мутацию проспала.
    Правильная проверка — `doc.getElementById("studio")` всё ещё в документе.

35. **Мутации проверяй ПОСЛЕ `node build.js`.** `full-run.js` открывает
    `dist/kodokvest.html`, а не `js/app.js`. Ломаешь `app.js`, запускаешь тест без
    пересборки — тест зелёный, и кажется, будто проверка ничего не ловит. Дважды
    наступил на это в 1.21.0.

36. **Значок в подписи ломает проверку значка в данных.** Тест на отчёт искал 🛡
    в HTML и проходил даже со сломанной полосой дней: тот же 🛡 стоит в поясняющей
    подписи под ней. Проверять надо класс клетки (`wrday shield`) плюс количество
    значков, а не факт присутствия.

37. **Мутация, которая ничего не ломает, — не проверка теста, а самообман.**
    Проверяя «поймай ИИ», я сломал сравнение с правильной версией — тест упал,
    хорошо. А проверяя, что проект вне миров не уедет на карту мира, написал
    мутацию `|| n === 1` в `projectOfWorld` — и она оказалась пустышкой: цикл
    всё равно первым находил `project-w1`. Тест зелёный, а вывод «проверка не
    работает» был бы ложным. Если мутация не роняет тест — сначала убедись,
    что она вообще меняет поведение.

38. **Тест, которому «нечего проверять», обязан падать.** Блок про проект
    «Напарник» был обёрнут в `if (aiProj && ...)`. Убери `world: 0` — блок
    молча пропускается, в сводке пишется «нет», код выхода ноль. Теперь перед
    ним стоит `if (!aiProj) bad(...)`: отсутствие проверяемого — это поломка,
    а не отсутствие работы.

39. **`random` в движке засевается одинаково на каждый запуск.** Две подряд
    идущие `run()` одной программы дают одни и те же случайные числа — это
    сделано ради игр (перезапуск с накопленными ходами не должен «плыть»).
    Помни об этом, когда пишешь защиту от недетерминированности: от `random`
    она не спасает и не нужна, а вот `time.time()` между двумя запусками
    может перевалить через миллисекунду.

40. **Печать берёт документ целиком, а не экран.** Поэтому сертификат сделан
    оверлеем (`#cert` в `index.html`), а в `@media print` скрыто ВСЁ, кроме
    листа: `body>*{display:none!important}` плюс `body>.cert:not([hidden])`
    обратно. Убери одну эту строку — и на бумагу уйдёт вся тёмная страница
    вместе с шапкой; в `full-run.js` на неё стоит проверка по тексту стилей.
    И `[hidden]` в селекторе обязателен: без него закрытый сертификат печатался
    бы всегда.

41. **Дата на документе не имеет права плыть.** Первая мысль — ставить на
    сертификат сегодняшнее число; тогда распечатанный вчера лист и распечатанный
    сегодня расходятся, и документ перестаёт быть документом. Дата считается из
    журнала: самое позднее из `S.log[id].solvedAt` последнего урока мира и
    `S.projects[id].doneAt`. `doneAt` ставится ОДИН раз (`st.doneAt || Date.now()`),
    иначе «пройти заново» переписало бы дату, а при слиянии берётся РАННЯЯ из
    двух (`minPos`) — проект собран тогда, когда собран впервые.

42. **`enterScreen` — не единственный вход на новый экран.** `openLesson` и
    `openProject` зовут `claimScreen` НАПРЯМУЮ, минуя `enterScreen` (таймер
    урока они гасят сами, через `startTimer` → `stopTimer`). Поэтому всё, что
    обязано случиться при ЛЮБОМ уходе с экрана — как сохранение черновика, —
    вешается на `claimScreen`, а не на `enterScreen`: иначе переход с урока
    сразу на другой урок пройдёт мимо. Проверка на это в `full-run.js` есть.

43. **Код ребёнка теряется там, где сохранение висит на кнопке.** У песочницы
    оно висело на «Запустить» и на нижней кнопке «Ко всем мирам» — и уход
    кнопкой верхней панели стирал написанное; заметил это только потому, что
    рядом чинил ту же болезнь на уроке. Если на экране есть редактор, сохранение
    обязано быть на УХОДЕ (`draftFlush`), а не на одной конкретной кнопке.

44. **Поиск по подстроке в JSON врёт про имена функций.** Искал `all(` — нашёл
    `apply_all(`, `sum_all(` и `re.findall(` и сделал вывод, что `all` в курсе
    не объясняется. Вывод был неверный, и он попал в HANDOFF. Ищи по границе
    имени (`/(^|[^A-Za-z_А-Яа-яЁё])(all|any)\(/`) и глазами смотри на найденное,
    прежде чем записывать вывод в документы.

45. **Всё, что берётся «из общего пула», обязано считаться с прогрессом.**
    Задача дня выбиралась из ВСЕХ разминок, а разминки не были ничем закрыты —
    и ребёнку из Мира 1 выпадало упражнение про `zip`. Заводишь новый пул
    (разминки, «Ты и ИИ», что угодно) — сразу решай, с какого урока каждый
    элемент имеет смысл. И осторожнее с откатами вида «если открытых нет, берём
    из всех»: такой откат выдаёт ровно то, от чего защищает замок. Честное
    «пока нечего, пройди первые уроки» лучше.

46. **Подсказка — не объяснение: она стоит звезду.** Если команда названа
    только в `hints`, ребёнок может узнать её лишь заплатив. Проверка «порядок
    объяснений» в `tests/lessons.js` подсказки НЕ засчитывает, и это не
    придирка: с засчитанными подсказками она молча прошла на `.title()` —
    на том самом дефекте, ради которого писалась. Бесплатны теория, условие
    задания и `symptom` у «починить».

47. **Метрика, у которой нельзя поставить порог, тестом быть не должна.**
    Доля списанных из теории строк — полезная подсказка и никакой не приговор:
    у «починить» совпадение обязано быть полным, у уроков с каркасом совпадают
    ровно те строки, которые урок и должен дать готовыми. Такое живёт
    служебным инструментом (`tests/echo-check.js`, `try-one.js`) и смотрится
    глазами, а в `npm test` идёт только то, у чего есть однозначный ответ.

48. **`needCode` живёт внутри `check`, а не рядом с ним.** `runCheck` читает
    `body.task.check.needCode`; написанное как `task.needCode` не ломается и не
    работает — урок просто выглядит защищённым. Я так ошибся и не заметил бы
    без `tests/bypass-check.js`. Теперь на это есть жёсткая проверка в
    `tests/lessons.js`. То же касается `noCode`, `needText`, `needMsg`, `noMsg`.

49. **Ожидаемый вывод показывается ребёнку при первой же неудаче.** `diffBlock`
    печатает «ждали / получили» — это хорошая помощь при отладке и одновременно
    готовый ответ. Поэтому любое задание, которое сверяет только вывод, можно
    пройти списком `print`'ов, и защищает от этого исключительно `needCode`.
    Добавляя урок, сразу спроси себя: что помешает напечатать ответ руками?
    Проверить — `npm run audit:bypass`.
50. **В тексте урока разрешены только теги оформления** (`<b>`, `<code>`,
    `<br>`, `<i>`). Текст попадает на страницу как HTML, поэтому `<p>` в условии
    браузер ИСПОЛНИТ, а не покажет — а в уроках про Flask `<p>` это как раз тот
    код, который ребёнок обязан вернуть. Литерал пишется через `&lt;` и `&gt;`.
    Так было сломано в трёх уроках пятого мира; теперь на это есть проверка
    в `tests/lessons.js`.
51. **Пробелы внутри `<code>` — это обещание, а не оформление.** Стили их
    сохраняют (`white-space: pre-wrap`), потому что иногда сами пробелы и есть
    смысл примера: выравнивание таблицы по ширине или «лишние пробелы снаружи
    и внутри». Аудит сверяет их ДОСЛОВНО. Показал выровненную строку — она
    обязана совпасть с выводом эталона до пробела.
52. **Литеральное обещание вывода пиши в `<code>`.** Аудит сверяет с эталоном
    только то, что взято в `<code>`; описание словами («напечатай `строк:`
    и число строк») сверять не с чем, и это нормально. Но если пишешь
    конкретную строку — оберни её, иначе ошибку в ней никто не поймает.
    Так полгода жила неправда в уроке 39.
53. **Работа многофайлового задания лежит в `task.files[].solution`,
    а не в `task.solution`.** В `task.solution` — `main.py`, который ребёнок
    обычно и не трогает. На этом ослепли ДВЕ проверки аудита сразу: и «первая
    подсказка выдаёт решение», и «задание списано с теории». Пишешь проверку
    по содержанию задания — бери оба места.
54. **Что выдано в заготовке, ребёнок не пишет.** Любая метрика вида «сколько
    выдала подсказка» или «списано ли с теории» обязана вычитать заготовку.
    Иначе выданная обвязка разбавляет долю до нуля, а повтор выданного класса
    в теории (`Fighter` в уроке 59) считается списыванием, хотя повторять его
    там как раз нужно.
55. **Однобуквенные кириллические имена в коде запрещены.** `с`, `о`, `а`, `е`,
    `р`, `х` неотличимы от латинских `c`, `o`, `a`, `e`, `p`, `x`: ребёнок
    перепечатывает пример и получает `NameError` без понятной причины. И если
    требуемый ФОРМАТ содержит такую букву (`7 x 1 = 7`), скажи в условии,
    какая она.
56. **Русские имена переменных вводятся в уроке 81** — там про это сказано
    словами. Раньше восемьдесят первого урока их быть не должно.
57. **Числа в README сверяются тестами.** Размер корпуса движка, число сверок
    с python3 и размер корпуса линтера названы в README словами — и три
    проверки следят, чтобы текст не разошёлся с делом. Пополнил корпус —
    поправь README, иначе `npm test` покраснеет и скажет, какое число.
58. **Новый жёсткий цвет в правиле CSS ломает светлую тему.** Всё, что
    меняется между темами, живёт переменными в `:root`. Тест ищет вбитые
    тёмные цвета в собранном файле.
59. **`offsetParent` в jsdom всегда `null`** — раскладки там нет. Фильтр по
    нему МОЛЧА выключает проверку целиком: она зелёная и не проверяет ничего.
    Так чуть не уехала ловушка фокуса в окне помощи.
60. **Цвет доски для рисования берётся из `--board`, а не числом в коде.**
    Раньше CSS и `drawTurtle` красили её по-разному, и до первого запуска
    доска была одного цвета, после — другого.
61. **Свёрнутый блок — это блок, который не прочитают.** Требования задания
    и обещанный вывод жили за словами «показать целиком», ради трёх строк
    экономии места. Ребёнок мог пойти дальше, не открыв единственное место,
    где написано, что засчитается. Правило: то, без чего задание нельзя
    сделать, на экране открыто. Сворачивать можно только повтор того, что уже
    сказано рядом.
62. **Одна и та же мысль в двух местах подряд читается как ошибка вёрстки.**
    Свёрнутая строка задания и раскрытый текст — это одна цель, и вместе они
    печатали её дважды. Заводя пару «коротко / целиком», сразу решай, кто из
    них прячется, — и прячь атрибутом `hidden`, а не правилом CSS: `hidden`
    видно тесту, а вычисленный `display` в jsdom — нет (см. грабля 59).
63. **Кнопка «назад» внизу страницы — это кнопка за экраном.** На уроке
    единственная дорога назад лежала под редактором, то есть ровно там, куда
    ребёнок не смотрит, пока пишет код. Хлебные крошки её не заменяют: они
    выглядят подписью. И у любой такой кнопки спрашивай, куда она ведёт во
    время ЗАНЯТИЯ: мимо плана уносит не только «Дальше →».
64. **`\b` и `\w` в JavaScript не знают кириллицы.** Шаблон `class\s+\w+\s*\(`
    не ловил `class Кот(Зверь)` — а в курсе русские имена переменных законны
    с 81-го урока. Пишешь регулярку по коду — бери класс символов руками:
    `W_ID = "A-Za-z_0-9А-Яа-яЁё"`. Так же сделано в `tests/lessons.js`.
65. **Ищешь конструкцию в коде — сначала выкинь строки и комментарии.**
    Иначе «for» находится в слове «форма», а `%` — в формате даты. Для этого
    есть `codeSkeleton`, и он оставляет пустые кавычки, чтобы не склеить
    соседние знаки.
66. **Сигнал про ребёнка обязан отличать своё копирование от чужого.**
    Кнопка «→ В редактор» и обычное копирование примера урока — нормальная
    работа. Считать их «работой, пришедшей готовой» значит пометить каждый
    второй урок, и взрослый перестанет верить записи целиком. Материал урока
    кладётся в `editor.knownText`, и вставка из него считается отдельно.
67. **Новое поле журнала обязано быть названо в `mergeProgress`.** Слияние
    собирает запись `S.log[id]` заново, поле за полем: не назвал — поле молча
    теряется на первой же синхронизации, и заметит это не тест, а фаундер
    через месяц. На это теперь есть проверка.
68. **Запись авторства пишется один раз и не переписывается.** Повторное
    прохождение урока запись о ПЕРВОЙ сдаче не трогает, и при слиянии
    побеждает ранняя. Иначе пометку «часть пришла готовой» стирает второй
    заход, и грош ей цена.
69. **Проверка, ответ на которую ребёнок уже видел, ничего не проверяет.**
    Спросить «что напечатает твоя программа» сразу после того, как он её
    запустил, — это проверка памяти на две минуты. Поэтому в программе
    меняется одно число, и изменённый вывод ОБЯЗАН отличаться от прежнего —
    иначе вопрос не задаётся вовсе.
70. **Хочешь править код по найденному месту — ищи в скелете, сохраняющем
    длину.** `codeSkeleton` заменяет содержимое строк и комментариев
    пробелами, а кавычки оставляет: индекс в скелете совпадает с индексом в
    настоящем коде. Без этого правка «одного числа» попала бы в цифру внутри
    текстовой строки, и вопрос стал бы про кавычки.
71. **`unlockAll` в панели наставника открывает ВСЁ, включая разминки.**
    Проверка «что видит новичок» без сброса этого флага молча тестирует не
    новичка. Я так и написал тест с первого раза, и он честно упал.
72. **Блок плана, который нечем открыть, хуже отсутствующего блока.** Поэтому
    «проверка понимания на своём коде» дописывается в план не заранее, а в тот
    момент, когда из программы и правда получился вопрос.
73. **Накопленное сливается объединением, а не «свежее побеждает».**
    Черновик — это одна программа, и там свежее правильно. Полка деталей —
    это НАКОПЛЕНИЕ: правило «свежее побеждает» стёрло бы работу, сделанную на
    другом устройстве. Заводя новый список, сразу спроси, что он такое:
    состояние или накопление.
74. **Неизменяемый файл в редакторе — ловушка «пишу, а не пишется».**
    Поэтому мастерская собрана в один файл, а не в модуль «полка.py»: правки
    в модуле некуда было бы сохранять. Если очень нужен модуль — сначала
    ответь, где живут правки в нём.
75. **Не называй чужое своим.** Показанное решение и функция из заготовки на
    полку деталей не идут. Продукт держится на том, что «моё» значит моё; одна
    подмена здесь — и накопление перестаёт что-либо значить для ребёнка.
76. **Половина петли — это не петля.** «Ребёнок отправляет задачу взрослому»
    существовало давно и не работало: автор никогда не узнавал, решили её или
    нет. Заводя механику со зрителем, сразу спроси, каким путём результат
    возвращается автору. Без обратного пути зритель нем, и механика мертва.
77. **Обратный путь делается тем же способом, что прямой.** Ссылка с base64 в
    адресе: ни сервера, ни хранения, ни ПДн. И в квитанции не должно быть
    имени — ребёнок знает, кому отправлял, а имя взрослого в детском прогрессе
    это персональные данные рядом с детскими.
78. **Событие, которое приходит ссылкой, можно открыть десять раз.** Ключ
    квитанции обязан быть таким, чтобы повтор не превратился в десять событий,
    а настоящее второе событие не слилось с первым. У нас это задача плюс
    число попыток.
79. **Витрина не должна выкладывать ответы курса.** Родителю нужен результат
    программы, а не её код. Код непройденного проекта на витрине — это готовое
    решение рядом с заданием; на это есть проверка, которая ищет строки
    решения в разметке страницы.
80. **`drawTurtle` меряет ширину холста по факту — значит рисовать надо после
    того, как вся полоска встала в документ.** Рисунок, нарисованный сразу
    после вставки ячейки, мерил ширину недозаполненной сетки: первый вышел
    2000 точек, последний 480. Собери ячейки, вставь, и только потом рисуй.
81. **Починив дорогу назад на одном экране, проверь все остальные.** В 1.43.1
    кнопку «Назад» завели на уроке, и на этом успокоились. Через несколько
    волн выяснилось, что ровно та же дыра была на витрине, в мастерской, в
    портфолио — везде, где экран длинный. Одна кнопка в панели вместо
    двадцати пяти в подвалах.
82. **История браузера в одностраничном приложении врёт.** Мы не пишем в неё
    каждый экран, поэтому «назад» надо считать по карте мест (`backTarget`),
    а не звать `history.back()`. Заодно это предсказуемее: «на уровень вверх»
    всегда одно и то же место.
83. **Довод для родителя, поставленный на детский экран, — это мусор на
    детском экране.** «Сто выброшенных вечеров», «чужих детей и имён там нет»,
    «покажи взрослым» — всё это правда и всё это ребёнку нечитаемо. Написав
    текст, спроси, КТО его прочитает на ЭТОМ экране, и перепиши под него.
84. **Название кнопки — это обещание, и страшное название пугает.** «↺ Сброс»
    чистит вывод, но читается как «сотрёт мою программу», и ребёнок такую
    кнопку не нажимает вовсе. Стало «↺ Очистить» плюс подсказка «код не
    трогает».
85. **Первое, что видит новичок, — самое дорогое место экрана.** Там стояло
    предложение поставить приложение: «адресная строка», «меню браузера».
    Ребёнок пришёл писать код. Всё служебное показывай тому, кто уже остался.
86. **Свой внутренний термин на детском экране — это шум.** «Вне сотни уроков»
    понятно нам и никому больше. В подписи без объяснения — только те слова,
    которые ребёнок читает сразу: «звёзд не даёт», «по желанию».
87. **Сводка по ученику обязана считаться из ЛЮБОГО снимка прогресса.**
    Наставник смотрит чужие данные, взятые с сервера, а не своё `S`. Функция,
    которая лезет в `S` (как `authorPredict`), для группы не годится —
    у `groupRow` всё приходит параметром.
88. **Кэшировать человеческую подпись в строке нельзя.** Наставник подписывает
    ученика уже ПОСЛЕ того, как группа пришла с сервера, и строка с
    запомненным именем показывала бы старое. Читай подпись живой на отрисовке.
89. **Тридцать одновременных запросов к облачной функции — это очередь.**
    Тянем по одному и показываем, сколько уже пришло. Медленнее и честнее.
90. **Свой язык обязан переводиться в чужой — и перевод обязан запускаться.**
    Нотация приёмки компилируется в настоящий Python, и тест не просто ищет в
    переводе `assert`, а ВЫПОЛНЯЕТ его на эталоне и на коде напарника. Перевод,
    который не запускается, — это не перевод, а картинка, и ребёнок унесёт с
    собой диалект вместо навыка.
91. **Проверку, которую можно пройти ложью, надо сначала проверить на правде.**
    Приёмку сдавали бы строкой «пример f(1) = 999»: заведомая ложь, код её не
    проходит, засчитано. Поэтому спецификация сперва гоняется на эталоне, и
    только потом — на коде напарника.
92. **Смена `location.hash` в тесте перерисовывает экран с задержкой.**
    hashchange встаёт в очередь; следующая проверка успевала открыть свой
    экран, и его затирал запоздавший `screenWorlds`. После правки хэша —
    `await tick()`.
93. **Группировку строй из данных, а не из списка в коде.** Ступени раздела
    «Ты и ИИ» собираются по метке самого задания, и задание с новой меткой
    попадает в «Остальное» — видное и раздражающее. Список ступеней в коде
    молча потерял бы его, и заметил бы это ребёнок.
94. **`.блок b {display:block}` ловит ЛЮБОЙ `<b>` внутри, включая выделение
    в тексте.** Выделенное «принять работу» отвалилось на свою строку. Для
    заголовка карточки нужен прямой потомок: `.блок > div > b`.
95. **Скрипт из нескольких замен пишет файл только целиком.** Упавший assert
    на четвёртой замене откатывает и первые три — а следующий прогон видит
    непонятную половину. Правь по одной замене с печатью счётчика, или пиши
    файл после каждой.
96. **Обещание, которое проверяется в мае, стоит дороже любого другого.**
    Раздел про ОГЭ обязан прямо сказать, какое задание он закрывает (15.2) и
    какие НЕ закрывает. Родитель, купивший «подготовку к ОГЭ» и получивший
    одно задание из пятнадцати, не вернётся никогда.
97. **Наш козырь в алгоритмике — счётчик шагов, а не буква O.** Движок считает
    шаги ради защиты от вечного цикла, и это же превращает сложность из
    рассказа в измерение. Четырнадцатилетнему «1000 против 10» говорит больше,
    чем любая формула.
98. **Русские имена переменных до 81-го урока — нарушение, даже вне сотни.**
    Я завёл их в js/specs.js и поймал сам: в разделы «Ты и ИИ», приёмки и
    алгоритмов заходят с любого места курса. Имена в коде латинские, условие
    по-русски.
99. **Прежде чем «расширять движок», измерь, чего в нём нет.** Прогон 76
     конструкций Python занял пять минут и дал точный список из 16 дыр вместо
     догадок. Гадание тут дороже всего: половина «недостающего» школьнику не
     нужна вовсе.
100. **Новое в движке идёт в корпус сверки с python3 — и корпус ловит.**
     `nonlocal` падал с UnboundLocalError, потому что разбор «своих имён» не
     знал про это слово. Я бы этого не заметил: мои собственные примеры
     проходили.
101. **Не добавляй в движок то, что может молча разойтись с Python.**
     Множественное наследование требует правильного порядка разрешения
     методов; сделать «примерно» — значит завести ложь, которую никто не
     поймает. Лучше не уметь, чем уметь неправильно.
102. **Формат чужого экзамена берут у источника, а не из памяти.** Задачи «в
     формате ОГЭ» я сперва написал по памяти — и пропустил половину каркаса:
     систему счисления в признаке, обязательное «NO», ограничения числами,
     таблицу примера. Каркас снимается с открытого банка, формулировки пишутся
     свои.
103. **Не называй номер, которого не проверил.** Нумерация заданий экзамена
     меняется между годами. Пишем «в формате ОГЭ» и код элемента содержания —
     то, что можно подтвердить. На это есть проверка.
104. **Вызов метода ученика — всегда генератор.** Поэтому property считается
     в `case "Attribute"`, а не в `bindMethod`, и `len` пришлось сделать
     генератором с меткой `pyGen`. Любая новая «магия», зовущая код ученика,
     обязана жить там, где доступен `yield*`.
105. **Возможности движка нужно охранять отдельным тестом.** Выкинь системы
     счисления — сборка соберётся, уроки пройдут, а половина раздела
     «Алгоритмы» тихо умрёт. Теперь на четырнадцать возможностей есть прямая
     проверка, и на три «принципиально не умеем» — обратная.

# КАК РАБОТАТЬ

- Фаундер нетехнический. Объясняй пошагово, без жаргона. Не льсти —
  конструктивная критика лучше «отличная идея». Ошибки называй ошибками,
  в том числе свои.
- Тон уроков: коротко, конкретно, без сюсюканья. Каждый урок 5–8 минут.
- Сначала движок и механики, потом уроки. Не пиши уроки, пока то, на чём они
  держатся, не закрыто тестами.
- Работай волнами; в конце каждой оставляй репозиторий в зелёном состоянии,
  чтобы фаундер мог закоммитить и запушить.
- Обязательно прогони `npm test` перед сдачей. Все пять наборов зелёные.
- **Отдельный редакторский проход по готовым урокам обязателен.** Тесты
  проверяют, что код работает, и не проверяют, что урок хороший. Хорошо
  работает отдельный агент со свежим взглядом и списком критериев.
- Перед удалениями и изменениями публичной инфраструктуры — подтверждай.
- Обновление сайта: правишь файлы в клоне репозитория → фаундер выполняет в
  Терминале `find .git -name '*.lock' -delete` и `git add -A && git commit -m "…" && git push`.
  Сам push ты не делаешь без явного подтверждения.
